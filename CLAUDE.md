# genekibu-kanri プロジェクト向けメモ

Claude Code セッション間で共有したい規約や「やらないこと」を記録する。

## 却下された提案 (再提案しないこと)

以下は検討の上で不要と判断された機能・改善案。同じ趣旨の提案は再提示しない。

- **A9: 最近見た講師 / ビュー履歴を Cmd+K フィルタ上位に出す**
  - 却下日: 2026-04-24
  - 理由: 不要との判断

- **H1c: 追加授業 (extraLessons) を授業回数カウント (第N回) に通算する**
  - 却下日: 2026-07-03
  - 理由: 不要との判断。追加授業は回数に数えない仕様で確定
    (既存コマへの slotId 紐付けや sessionCount の日付ゲート拡張も不要)

- **A18 系: 使用頻度・履歴ベースの自動学習機能全般**
  - 却下日: 2026-04-24
  - 理由: 不要との判断
  - 対象の具体例（これらに類する提案は今後一切しない）:
    - "最近使った g-chord を学習して順序を並べ替える"
    - "よく選ぶ講師をサジェスト上位に固定する"
    - "アクセス頻度に応じてサイドバー項目を並べ替える"
    - "直近の入力履歴から補完候補を生成する"
  - 一般ルール: **ユーザ行動の統計を LocalStorage 等に保存して UI を自動変形する系は提案禁止**

- **RB26: 通常時間割作成の自動生成 (ソルバ)**
  - 却下日: 2026-08-04
  - 理由: 使わないとの判断。通常時間割は手動設計で完結する
    (講師NG・上限・集計・スナップショット等の設計補助は実装済み)。
    講習ソルバの移植・生成 UI・生成パラメータ設定は今後提案しない

## 削除 UX のルール (重要)

リソース削除には 2 つのフックを使い分ける。**新しい削除導線を増やす際は
必ずこのルールに従うこと。**

- **`removeWithUndo` / `useRemoveWithUndo`** を使う条件
  - 連鎖更新（cascade）が **無い** 単純削除のみ。
  - 即削除して 6 秒間 toast に「元に戻す」を出す。
  - 該当例: 代行記録、時間割調整、回数補正、休講日、授業セット。
  - 複数件をまとめて消すときは同じ条件で **`useRemoveManyWithUndo`**
    （`useAdjustmentsCrud.delMany` が実装例。日まるごと振替の一括解除）。

- **`confirmedRemove`** を使う条件
  - **cascade を伴う**削除。
  - 該当例: バイト削除（→ 特訓シフト）、時間割削除（→ コマ再割当）、
    コマ削除（→ 関連 sub）、テスト期間削除（→ 特訓スケジュール）。
  - 理由: Undo で連鎖更新まで戻すロジックが複雑かつバグの温床になる。
    確認ダイアログで誤削除を防ぐ方が安全。

判断に迷う場合は cascade ありとして `confirmedRemove` を使うこと。

## 複数講師の区切り文字 (重要)

Slot / ExtraLesson の `teacher` フィールドで複数講師を表す区切りは
**"·" (U+00B7) が正史**。ただし日本語 IME の素の入力は "・" (U+30FB) に
なるため、以下を必ず守ること (2026-07-03 の H1f バグの再発防止):

- **分解 (読み取り) は必ず `utils/biweekly.splitTeacherField` を使う**。
  `teacher.split("·")` の手書きは禁止 ("・"/"･" 入力を取りこぼす)
- **teacher を保存する入力フォームは、保存時に
  `splitTeacherField(value).join("·")` で正規化する**
  (SlotForm / ExtraLessonManager が実装例)
- 講師名そのものに中黒は使わない (区切りと区別できないため)

## 隔週コマの担当週 A/B (重要)

隔週コマ (note の「隔週(◯◯)」) の担当週は全システム共通で

- **A 週 = 講師欄 (`teacher`) の主担当**
- **B 週 = note「隔週(◯◯)」のパートナー**

(`biweekly.isTeacherActiveOnDate` / `biweeklyDisplaySubject`、通常時間割
作成の 📊 集計・👁 週間ミニビュー・Excel の「隔週A/隔週B」がこの定義)。

**「関わる講師」を週を問わない名前の集合として扱ってはいけない。**
主担当とパートナーが左右で入れ替わる組 (AB 列 `堀上/河野` ↔ C 列
`河野/堀上`) は、どの週も同じ人が 2 か所に居ないので重複ではない
(2026-08-12 の誤検出の再発防止)。

- 重なりの判定 (`regular-builder/conflicts.js` の講師重複・校舎移動・
  「(重複)」予告) は **担当週が交わるときだけ**。隔週 ↔ 通常コマは通常側が
  毎週なので必ずどちらかの週で当たる = 警告する (これが本来の狙い)
- 逆に**週で絞ってはいけない**もの: 講師 NG (曜日単位なのでどちらの週でも
  出勤日)、教室重複 (隔週でもその時間その部屋は塞がる)、📊 集計の 0.5 重み
- note が「隔週」だけでパートナーを読めない場合は週を絞れないので
  両方の週に居る扱い (保守的に広く検出)

## 講師の並び順は「よみ」だけが頼り (2026-08-14、2026-08-17 に本体へ拡大)

**本体アプリと通常時間割作成の両方**で、講師名の並びはよみ (かな) 順。
正規化 (NFKC → カタカナをひらがなへ → 空白除去) と照合 (`Intl.Collator("ja")`,
sensitivity: base) は **`utils/teacherKana.js` に集約**されており、
`regular-builder/teacherOrder.js` もそこを参照する。**片方だけ書き換えない**
(同じよみで両画面の並びがずれる)。

- 本体側のよみは **`teacherKana` (名前 → よみのマップ、`LS.teacherKana`)**。
  `partTimeStaff` の項目に**しない** — 常勤講師 (時間割のコマにしか出て
  こない人) にもよみが要るため。`teacherSubjects` と同型で、
  エクスポート / インポート / 初期化にも配線済み
- **新しく講師名を並べる箇所を足すときは `sortTeacherNames` /
  `compareTeacherNames` を使う**。`sortJa` / `compareJa` は講師名に使わない
  (時間帯・学年・教室・科目・タグなど、講師以外には従来どおり使ってよい)
- 関連度で並べているところ (代行候補の教科一致順、月次集計の件数降順、
  特訓シフトの教科順) は**主たる並びを変えず、同点のときだけ**よみで割る

通常時間割作成の講師プルダウンは**担当科目ごとの optgroup + よみの
アイウエオ順** (`regular-builder/teacherOrder.js`)。以下を守ること:

- **漢字の名前を読み順に並べる方法は無い**。`localeCompare("ja")` /
  `Intl.Collator("ja")` は漢字を**部首・画数順**に並べるので「堀上・河野」を
  "ほりかみ / こうの" 順にはできない。並べ替えキーは講師マスタの `kana`
  (よみ) のみ。**姓の読みを推測する辞書を同梱する案は採らない**
  (河野 = こうの/かわの のような多読みを黙って間違える)
- **よみ未設定の講師は勝手に別の順序にせず、マスタ順のまま末尾**に置く。
  画面 (⚙ → 👤 講師 の「よみ未設定 N 名」) で気付けることを優先する
- 担当科目 (`teacher.subjects`) の語彙は**プロジェクトの科目マスタ**。
  誰も担当科目を持たない間は optgroup を出さず従来の平らな一覧のまま
  (機能を使わない人の見た目を変えない)
- 「📚 担当科目を推定」は**一度きりの入力補助**であって自動分類ではない
  (行動統計で UI を自動変形しない、の原則は維持)

## 印刷システムの二系統 (重要)

印刷機構は意図的に 2 系統あり、統合しない。**新しい印刷導線を増やす際は
どちらに寄せるかを必ず判断すること。**

- **`PrintButton` (`src/components/PrintButton.jsx`)** — メインドキュメントを
  そのまま `window.print()` する単純方式
  - 適用先: Dashboard / WeekView / EventCalendarView / ConfirmedSubsView /
    MasterView
  - 仕上げは App.jsx 末尾のグローバル `@media print` と各ビュー自前の
    印刷 CSS だけで完結。**popup を使わない。**

- **`handlePrint` (App.jsx, トップバー右の 🖨 ボタン)** — 新ウィンドウを
  開いて `#main-content.innerHTML` をコピーし、ビュー別の印刷スタイルと
  ヘッダ HTML を動的に注入してから印刷する方式
  - 適用先: 月次カレンダー (MonthView) / タイムテーブル (ExcelGridView)
  - 「ページタイトル h2」「印刷日」「フィルタ状態」「凡例」など、
    DOM に常設しづらい要素を紙面に載せる必要がある場合はこちら
  - CSS / HTML ビルダは `src/utils/printStyles.js` に切り出し、
    `printStyles.test.js` で純粋関数としてテストしている。popup の開閉
    (`openPrintWindow` / `writePrintDocument`) は `src/utils/printWindow.js`

  この popup 系統には、単発の `handlePrint` の他に**「対象を差し替えながら
  DOM をスナップショットして 1 ジョブに連結する」派生**が 2 つある。どちらも
  `flushSync` で描画を確定 → `requestAnimationFrame` 2 回待ち → `outerHTML`
  を取る、という同じ手順:
  - **月次の 📋 まとめて印刷** (`handleBatchPrint`): 講師 × 月を差し替え
  - **タイムテーブルの 🖨 全曜日** (`ExcelGridView` の `handlePrintAllDays`):
    曜日 (月〜土) を差し替え、`.excel-print-day` ブロックに包んで曜日ごとに
    改ページ。ボタンが App.jsx ではなく曜日タブの隣にあるのは、グリッドを
    出しているか / どの曜日かが ExcelGridView の内部状態だから。代行モード中
    (表示が代行日の 1 日に固定される) は無効化する

  紙面の中身は**画面に出るものをそのまま写す**のが原則 (曜日ごとの描画
  ロジックを別に書き起こさない)。スナップショットの単位はセクション欄
  (`.excel-print-day-body`) + 日付固有のバナー (`.excel-print-day-note`) で、
  講師パネルや曜日タブは紙面に載せない。

**両者を統合する案 (E-2: 全部 window.print() 方式に寄せる) は
2026-05-05 に検討の上で却下。** 月次の印刷ヘッダ (タイトル・印刷日・
フィルタ状態・凡例) を `window.print()` で出すには各コンポーネントに
print-only DOM (`@media screen { display:none }` / `@media print { display:block }`)
を仕込む必要があり、コンポーネントが汚れる割に得るものが薄い。
popup 方式は popup ブロック対応が必要だが、`handlePrint` 内で
`toasts.error` 表示まで実装済みなので運用コストは小さい。

## Excel 出力の印刷デフォルト (講習時間割作成の講師別 Excel)

- 講師別 Excel は **B4 縦 + 先頭行 (タイトル/ヘッダ) のページ繰り返し**を
  pageSetup に埋め込み済み (`timetable-builder/utils/excelExport.ts` の
  `applyTeacherPrintDefaults`、P1)。各講師シートの先頭行は
  「講師名 — プロジェクト名 / 期間 / 出力日」のタイトル。

## Excel 出力の印刷デフォルト (通常時間割作成)

- 曜日グリッド (`regular-builder/excelExport.js`) は **曜日別シート =
  B4 横・1 ページ丸ごと収め (fitToPage 1×1)**、その後ろに**全曜日を
  まとめた「全曜日」シート = A3 横・3 曜日ずつ横に並べた 2 段組
  (月火水 / 木金土。帯の切れ目で手動改ページ)**。
- **紙面は「画面の曜日ビューをそのまま写す」のが要件** (2026-08-12 確定)。
  セクションバーの部色 + コマ数・学年色の見出し・「ラベル + 教室(小)」の
  クラス見出し・「科目(太) / 講師(青) / 教室・備考(小灰)」の 3 段セル
  (exceljs の richText)・合同コマのセル結合・学年境の太罫まで画面に
  合わせる。**画面と食い違う独自レイアウトを Excel 側に足さないこと。**
  - 合同コマの**行内の配置は `mergedColumns.computeRowCells` を画面
    (RegularGrid) と共有**している。片方だけ書き換えない
  - セクションの配色は `regular-builder/sectionTone.js` (画面と共有)
  - **既定の教室はセルに出さない** (クラス見出しに出ているため)。セルに
    出るのはセル上書きの教室と、列見出しを持たない合同セルだけ
  - 行の高さは中身から見積もる (`estimateRowHeight` /
    `estimateClassHeadHeight`)。**見出し列 (時限・クラス) も見積りに
    入れること** — wrapText + 固定行高は溢れた行を表示しないため、
    本文セルだけで高さを決めると長いラベルの行で時刻や教室が切れる
  - 行送りは `LINE_HEIGHT_RATIO = 1.7`。**Excel の行の高さはフォントで
    決まり**、日本語環境の既定 (游ゴシック) は 11pt → 18.75pt = 1.7 倍
    と広い。足りないと折り返した行が黙って消えるので、**広い側に倒す**
    のが正 (行間の詰まったフォントでは少し余るだけ)
- **Excel は fitToPage を有効にすると手動改ページを無視する**ため、
  「全曜日」シートだけは fitToPage を使わず `estimatePrintScale` で
  見積もった固定倍率を入れる。倍率は全曜日共通 (紙面の字の大きさが揃う)。
  **この 2 つを両立させようとして fitToPage に寄せる案は成立しない。**
- 「全曜日」だけ A3 なのは固定倍率方式だから (紙が大きいほど字が大きく
  刷れる)。曜日別は Excel が実測で収めるので B4 のままでよい。
  **2 つの紙を揃える提案はしないこと** (要件が違う)。

### 「全曜日」の詰め方 (2026-08-14 確定)

- **曜日を横に 3 つ並べた帯 × 2 段** (A3 横 2 枚)。並べ方は画面の
  **◫ 曜日を並べるビューと同じ** — 曜日ごとにセクションを縦に積み、
  セクションの開始行を曜日間でそろえる (`stackSectionStarts`)。並び順は
  曜日非依存のキー `regular-builder/sectionOrder.js` (画面と共有。
  中学部 → 高校部 / タブ定義順 / 本校 → 亀井町)。**曜日ごとの検出順で
  並べると月曜は亀井町が先・木曜は本校が先、のように行の対応がずれる。**
- **Excel の行の高さはシート全体で共有**。曜日を横に並べると 1 行に複数の
  曜日が乗るので、行高は `makeRowHeights` で**その行に載る全ブロックの
  最大**を採ってから流す。書くたびに `ws.getRow().height` を上書きすると、
  先に書いた曜日の折り返した行が黙って消える (wrapText は溢れを描かない)。
- 何曜日ずつ並べるかは `ALL_DAYS_PER_BAND`。**縛るのは幅**なので増やすほど
  倍率が下がる。実データでの実測 (A3 横):

  | 詰め方 | 枚数 | 倍率 | 科目 10pt の実寸 |
  | --- | --- | --- | --- |
  | 1 日 = 1 ページ (旧) | 6 | 69% | 6.9pt |
  | **3 日 × 2 段 (現行)** | **2** | **51%** | 5.1pt |
  | 2 日 × 3 段 | 3 | 58% | 5.8pt |

- **狭いセクションを同じ曜日の右の空きに逃がす「日内詰め」は採らない**
  (2026-08-14)。土曜 30 行 → 23 行と効くが、画面にない並べ方になり
  「紙面は画面をそのまま写す」から外れる。必要になったら画面 (◫ ビュー)
  ごと変える話として要件定義から。

## 紙面の可読性まわりの確定事項 (2026-08-11)

印刷を「見る道具」として使えるようにするための決定。**これらを崩す変更は
しないこと。**

- **科目カラーは「教科」単位**。`regular-builder/subjectColor.js` の
  `makeSubjectColorResolver(subjects)` が教科マスタ (`pickSubjectId` =
  完全一致 → 名前を含む → 別名を含む) で正規化してから
  `getSubjectColor` を引く。**素の `getSubjectColor(cell.subj)` を
  直に呼ばないこと** — 「理科A」と「理科B」が別色になる。
  画面 / ブラウザ印刷 / Excel の 3 経路すべてこの resolver を通す。
- **🖨 白黒 (`regb-mono`)** は紙面だけの切替 (画面の色は変えない)。
  下書きチェックはトナーを使わない白黒、配布は色付き、という運用。
- **紙面のセルは画面より 1px 大きい** (`printStyle.js`)。字を大きくすると
  折り返しが減って紙はむしろ減る (週表示 13 → 12 ページで実測)。
  コンパクト表示 (`regb-compact`) だけは据え置き。
- **この画面の升目は罫線ではなくセルの塗り分けで見せている**。Tailwind の
  preflight を読み込んでいない (親アプリへの波及を避けるため) ので
  `border-*` ユーティリティは幅だけ付いて `border-style` が無く、
  **何も描かれない**。罫線が要る場面 (白黒印刷など) は
  `border: 1px solid ...` と style まで書くこと。

## 配布用 Excel は「完成版レイアウト」(2026-07-16 確定)

- 講習時間割の「🎒 配布用」出力は例年の掲示紙面 (2025 年夏期の完成版 xls)
  を再現する専用ビルダー `timetable-builder/utils/distributionExport.ts`
  (詳細は ROADMAP §R1)。**旧 L5c の「注記なし学年グリッド」形式
  (buildScheduleWorkbook の clean オプション) は廃止済み — 再導入しない。**
- モデルに無い情報 (教室番号・確認テストの出題範囲・イベント日の説明文・
  欄外のお知らせ) は空欄 / 結合済み空セルとして紙面に確保し、出力後に
  Excel 上で手書きする運用。これらのデータ管理機能を勝手に足さないこと
  (必要になったら要件定義から)。
- **両面 (長辺綴じ) は xlsx に保存できない** — OOXML の pageSetup に duplex
  属性が無く、プリンタドライバ固有の DEVMODE バイナリ
  (`xl/printerSettings/*.bin`) にしか載らないため。exceljs 非対応かつ、
  埋め込んでも開く端末・プリンタが変わると効かない。両面はプリンタ側の
  既定設定で運用する。**DEVMODE バイナリを後付け注入する案は再提案しない。**

## 期切替 (前期/後期) の運用 (2026-08-01 確定、2026-08-02 更新)

- 時間割をゼロから設計する場合は専用タブ**「通常時間割作成」**
  (`src/regular-builder`、chord `g r`) を使う。曜日 × 時限 (時刻付き) ×
  クラスのグリッドで組み、講師・教室の重複をタブ横断で自動チェックし、
  「⤴ 本体へ反映」で Timetable + Slot に書き出す (新規作成 / 既存
  置き換え)。逆方向の**「⬇ 本体から取込」**で現行の時間割をプロジェクト
  化でき、取込オプション「中3 の 2学期変更を適用する」で 2026 2学期の
  たたき台を一発生成できる (平日前倒し + 土曜内申の午前枠)。複数
  プロジェクト (1学期 / 2学期など) をプルダウンで切替可能。下書きは
  `appData/genyakubu-regular-builder-project` に本体データと独立して
  保存・同期される。
  - **講習時間割作成 (builder) をそのまま流用する案は却下のまま**
    (日付ベースモデルで教室・隔週・並列コマを表現できず、単一
    プロジェクト前提のため)。通常時間割作成は操作感だけを踏襲した
    別サブシステム。
  - 反映は「作成フェーズ」の道具。運用開始後の日々の修正は本体側で行う。
- **期の切り替えは反映ダイアログの「期切替として反映する」で行う**
  (`regular-builder/reflect.js` の `buildSwitchPlan`)。本体の表示は
  **2 経路**あり、どちらも面倒を見ないと切り替わったように見えない:
  - **日付ベース** (ダッシュボード・タイムテーブル・代行など) =
    時間割の `startDate`/`endDate`。**前の期に終了日を入れないと
    切替日以降どちらも有効**になり、コマが二重に出る。期切替は
    「新: 開始日 = 切替日」「旧: 終了日 = 切替日の前日」を対で設定する
  - **集計ベース** (週表示・月間・一覧などの「現在の時間割」) =
    ヘッダの時間割セレクタ (`activeTimetableId`、localStorage)。
    反映ダイアログの「反映後、表示中の時間割をこれに切り替える」で追従する
  - **期切替で前の期のコマは消さない** (終了日を入れるだけ)。旧期の代行・
    調整・回数補正・授業セットの紐付けを切らないため。過去の日付を開けば
    従来どおり見える
  - ダッシュボードは**日付ベースだけで判定する** (セレクタで二重に絞らない)。
    絞ると切替日より前の日のコマが消える (2026-08-16 修正)
  - 時間割の有効期間とは**別のフィルタ**である**表示期間設定**
    (`displayCutoff` の学年グループ / コース別終講日) が旧期で終わっていると、
    切り替えても新しい期のコマが画面に出ない。`buildSwitchPlan` が警告し、
    反映ダイアログの**チェックボックス「表示期間設定も新しい期に合わせる」**
    (`reflect.buildCutoffFollowUp`) で 1 手で直せる。**既定はオフ = 自動では
    触らない** (学年グループは期をまたいで共通なので、どこまで面倒を見るかは
    人が選ぶ)。同じ場所の「新しい期はオリエンなしにする」も同様にオプトイン
  - **既に切替日より前で終わっている時間割の終了日は伸ばさない**
    (`changesEndDate`)。夏期講習で止めてある期を期切替が黙って延長すると、
    休講中に旧期のコマが出る
  - **授業セット (ClassSet) は (学年, 曜日) 参照なので新しい期にもそのまま
    効く** (2026-08-18 に slot.id 参照から変更)。詳細は下の
    「授業セットと第N回の束ね」節。旧形式 (slotIds) のセットだけが期切替で
    切れるので、`buildSwitchPlan` はそれだけを警告する
- **部分反映 (学年・曜日を絞った反映)** は `regular-builder/reflectScope.js`。
  **下書きを絞るだけでは足りない** — 反映の突き合わせ (`matchReflection`) は
  「下書きに無い既存コマは削除」なので、**既存コマ側も同じ範囲で絞らないと
  範囲外のコマが丸ごと消える**。`applyReflection` / `diffReflection` は同じ
  述語 (`scopeMatcher`) を共有すること。**片方だけ直さない**
  (プレビューの削除件数と実際の削除件数がずれる)。
  - 範囲の判定は **slot / draft の `{day, grade}` だけ**を見る。下書きの
    セル参照 (tabId 等) に依存させないのは、本体のコマが下書きのタブを
    知らないため
  - 学年 × 曜日は **AND**。各軸の空 = その軸では絞らない
  - 学年で絞ったら `timetable.grades` もその学年に揃える (コマの無い学年を
    担当する時間割を作らない)

- 時刻だけ変わる軽微な期切替は時間割管理の**「時刻一括変換」**
  (`utils/timeBulkEdit` + `TimeBulkEditPanel`) でも組める。
- **「第N回」は期ごとにリセット** (通算しない仕様で確定)。カウント起点は
  `sessionCount.getSlotCountStartDate` = 学年グループ開始日と所属時間割
  startDate の遅い方。所属時間割の有効期間外の日はカウントしない。
- **開講日 1 限のオリエン扱いは学年グループの設定** (2026-08-18 確定)。
  `displayCutoff.groups[].orientationFirstDay` (真偽値) を時間割管理 →
  表示期間設定のチェックボックスで切り替える。判定は
  `sessionCount.isOrientationEnabledForGrade` に集約。
  - **未設定 (従来データ) は中学部のみ有効**の既定値にフォールバックする。
    既存の回数を変えないための互換なので、**この既定を消さないこと**
  - 2 学期以降のようにオリエンが入らない期はチェックを外す運用。
    **期切替は自動では触らない** — 学年グループは期をまたいで共通のため。
    反映ダイアログの「新しい期はオリエンなしにする」を**明示的に選んだ
    ときだけ** `buildCutoffFollowUp` が false を書き込む
  - オリエン日の基準は**学年グループ開始日**であって
    `getSlotCountStartDate` (時間割 startDate と遅い方) ではない。
    期切替で新しい時間割を足しても新期の初日がオリエンにならないように、
    **この基準を「カウント起点」に寄せ替えないこと**
- 時間割の複製では、旧形式 (slotIds) の授業セットだけを新スロット id に
  読み替えて引き継ぐ。units 形式は複製先のコマにもそのまま効くので複製しない
  (下の「授業セットと第N回の束ね」節)。

## 授業セットと第N回の束ね (2026-08-18 確定)

「第N回」は**コマ単体ではなくコースごとに通算する**。どのコマを 1 本の
カウンタで数えるかの決め方は 3 段で、上から順に当たったものを使う
(`sessionCount.resolveSetSlotIds`)。

1. **授業セット (ClassSet)** — 人が明示的に登録した組
2. **コース (コホート、`utils/cohorts`)** — 未登録時の既定。
   高1・2 は学校の英数、高3 は科目別、**中学は学年の平日ぜんぶで 1 コース**
3. 自分ひとり (単体扱い)

**2 段目の「中学は平日ぜんぶで 1 コース」が効きすぎる学年がある。** 中3 のように
週内で **火木コース / 水金コース**へ分かれる学年は、セットを登録しないと
火曜と水曜が同じカウンタになり、水曜の第1回が第2回として出る
(2026-08-18 の症状)。**この既定を「曜日ごとに別コース」へ倒してはいけない** —
中1 火金・中2 月木のように週 2 日ぜんぶ同じ生徒が通う学年が多数派なので、
そちらが壊れる。分かれている学年だけセットで上書きするのが正。

- **授業セットは (学年, 曜日) の組 (`units`) で保存する**
  (`utils/classSets.js`)。コマ id を参照しないので**期切替でコマを作り直しても
  紐付けが切れない**。旧 `slotIds` 形式も読めるが、画面で「曜日ベースに変換」
  できる。**新しくセットを作る導線を足すときは必ず units 形式で書くこと**
- 期をまたいで同じ (学年, 曜日) のコマが並存しても二重には数えない。
  カウントはその日に有効な時間割のコマだけを見て、起点も所属時間割の
  開始日で決まる (旧期のコマはセットに入っていても新期の回数に寄与しない)
- **時間割の複製では units 形式のセットを複製しない** (複製先のコマにも
  そのまま効くので、複製すると二重定義になる)
- **旧 `slotIds` → units の変換は対象が広がりうる**。旧いセットが「その曜日の
  一部のクラスだけ」を指していると、曜日ベースにした時点で同じ学年・曜日の
  他クラスも同じセットに入る。画面で「N → M コマに増えます」を出し、増える
  ときだけ確認ダイアログを挟む (黙って数え方を変えない)
- 「🔀 コース分けの候補」は**曜日の間隔が 2 日または 3 日の組に分けきれるか**
  だけで作る (火水木金 → 火木 / 水金、月火木金 → 月木 / 火金)。**候補であって
  自動適用はしない** — 週 4 日ぜんぶ同じ生徒が通う学年もあり、どちらが実態かは
  人しか知らない。授業の中身や cls 名から推測する案は採らない
  (火木と水金で同じ cls 名 S/A/B を使うため区別できない)

## 表示期間設定 / 終講日の扱い (2026-08-18 点検)

表示期間設定 (`displayCutoff.groups` = 学年グループの開始日・終了日) と
コース別終講日 (`displayCutoff.cohorts`) について、点検で確定した決まり。

- **学年グループの照合は必ず `timetable.findGroupForGrade`**。学年は自由
  入力なので「中1-3」(土曜プレップ) のような複合学年が実在し、完全一致で
  引くと表示は効くのに回数だけ出ない、という片肺になる
  (`sessionCount.getGradeStartDate` がこれで壊れていた)
- **どのグループにも属さない学年は何のフィルタも効かない**
  (「附中」「高1高2」等の表記ゆれ)。`findUngroupedGrades` で検出して
  表示期間設定の画面に出す。**黙って素通しにしないこと**
- **全日判定は「開講前」と「終講後」を必ず出し分ける**
  (`timetable.getDayCutoffKind` → `constants/cutoffMessages`)。
  開始日前も期間外なので、一括で「未確定」と出すと 2 学期の開始日を先に
  入れた瞬間に夏休みが全部「未確定」になる
- 全日判定は **コマを持つ学年グループだけ**で行う
  (`getCutoffGroupLabelsWithSlots`)。ただし**どのグループにもコマが無い
  ときは絞り込みを無効化**する (grades を空にした「全学年」1 グループ運用が
  あるため)。判定材料を失って永久に false になるのを防ぐ
- **コマの一覧から日付を数える集計は、曜日と休講だけでは足りない。**
  時間割の有効期間 (`isTimetableActiveForDate`) と表示期間
  (`isSlotBeyondCutoff`) を必ず併せて見る (月次の「通常出勤日」がこれを
  見ておらず、終講後の曜日を数えていた)
- **時間割管理の 3 枚は「全体像 → 学年グループ → コース」の順で読む**。
  期間の全体像 (`components/CutoffTimeline.jsx` + `utils/cutoffTimeline.js`) は
  **読む専用**で、編集は下の 2 枚のカードに集約する (設定を 2 か所から書ける
  ようにしない)。帯に出すのは「時間割の有効期間 / 表示期間 / コース終講日」と
  **その食い違い (斜線 = 時間割は有効なのにコマが出ない区間)** まで。
  半年〜1 年の物差しに数日の差 (終了日 vs 実際の最終授業日) を描いても
  バーの端に重なって読めないので、**日付は数字で出す**
- **「終了日」と「実際の最終授業日」は別物**。設定画面では
  `utils/lastSessionDate.findLastSessionOnOrBefore` で逆算して見せる。
  実施判定は `sessionCount.isSlotHeldOnDate` に委ね、**休講・テスト期間・
  特別時程・隔週・時間割の有効期間の解釈を回数計算と一本化する**
  (画面ごとに独自ルールを書き起こさない)

## 日まるごと振替 (2026-08-19 実装)

「12/7 (月) の授業を 12/4 (金) にまとめて移す」操作
(`utils/dayReschedule.js` + `components/DayRescheduleDialog.jsx`、
授業管理 → 時間割調整一覧の 📅 ボタン / Cmd+K)。決めごと:

- **モデルは足さない**。作るのは既存の別日振替 (`adjustments` の
  `reschedule`) をコマの数だけ並べたものなので、表示・印刷・一覧・個別解除は
  既存の振替の経路に乗る。**日単位の新レコードを導入しないこと**
- **対象は「振替元日に実施されるコマ」だけ**。判定は
  `sessionCount.isSlotHeldOnDate` + `timetable.isSlotBeyondCutoff` に委ねる
  (休講・テスト期間・時間割の有効期間・表示期間・特別時程の部分休講・隔週
  B 週)。**画面側で独自の実施判定を書き起こさない**。外したコマは理由を
  出す
- **時刻は変えない**。まるごと移す操作なので `targetTime` は入れない
  (1 コマだけずらすのは欠勤組み換え画面の振替)
- **第N回は振替元の日付で数えたまま** (`sessionCount` は振替を見ない)。
  総回数は変わらないので日付の付け替えはしない。**回数側に振替を配線する
  案は、要件が出てから**
- 講師・教室の重なりは**警告であって禁止ではない**。振替先が同じ曜日の
  別の週 (12/7 月 → 12/14 月) は通常授業と二重になるので必ず警告に出る
- **代行・合同授業・コマ移動は (コマ, 日付) 参照なので振替先に付いてこない**。
  日をまるごと動かすと振替元の日に取り残されるので、件数を注意書きに出す。
  **これらを自動で振替先へ付け替えないこと** (どれも「その日にそうする」と
  決めたものなので、移した先で同じにしたいかは人しか知らない)

**振替の表示は「全日休講の日」で消えやすい** (2026-08-20 修正)。他日から入る
コマは**休講日でも消さないこと** — 「休みの日へ寄せる」のが日まるごと振替の
典型なので、休講で隠すと画面のどこにも出なくなる (追加授業
`ExtraLessonBanner` と同じ扱い)。実際に 3 経路とも塞いである:

- **日別ダッシュボード** (`DashDayRow`) — 全日休講の分岐より前に
  `RescheduleInBanner` を出す。出ていくコマには「振」バッジ
- **タイムテーブル / ダッシュボードの時間割モード** (`ExcelGridView`) —
  全日休講とコマ 0 件の日は**セクションを描かない**ので、セクション内の
  振替バナー (`ExcelSection`) ごと消える。グリッド上部にまとめて出す
- **月次カレンダー** (`MonthView`) — 全日休講の日のカードを消していた。
  カットオフ (未確定日) では消すが、休講では消さない

日付 → 入ってくる振替の一覧は `adjustmentDisplay.collectIncomingReschedules`
に集約。**新しく日付ベースのビューを足すときはここを呼ぶこと。**

導線は**サイドバーの「📅 日まるごと振替」** (`Sidebar` の `action: "modal"`
項目。管理者のみ表示) が正面。ダイアログ本体は App が持ち、時間割調整一覧の
ボタンと Cmd+K も同じものを開く。**タブの中だけに置かない** — 機能があること
に気付けない (2026-08-20 の指摘)。

## 特別時程 (daySchedules) の設計要点 (2026-08-05 実装)

附属コース等の「行事都合で特定日だけ時程が変わる」対応。要件は
① 全体を 50 分授業にして 17:00 開始 (テ 21:00-21:30 は据え置き)、
② 1 限カット。実装ルール:

- モデルは **日付 × 対象学年 × 時刻読み替え表 (timeMap) + 部分休講
  (cancelTimes)** (`utils/daySchedules.js`、schema v16)。**slot.id を
  参照しない** (slot.time との完全一致で照合) — コマ入替・期切替に強く、
  孤立データ掃除の対象にもならない。
- 時刻読み替えの表示は `buildAdjustmentIndex` の opts (slots +
  daySchedules) で既存の**コマ移動表示網に合流**させる。**個別の move
  調整が同じコマにあればそちらが優先**。休講側は各ビューの既存の
  休講経路 (isOffForGrade と並置) で処理する。
- cancelTimes のコマは sessionCount で「実施なし」(第N回を進めない)。
  **隔週 A/B 週送りには関与しない仕様** (附属に隔週コマが無いため。
  必要になったら biweekly 側へ配線する)。
- プリセット (50 分授業 / 1 限カット) は**その日のコマから時間帯を導出**
  して生成する — 時程をコードに固定しない。
- 削除は cascade 無しなので removeWithUndo (上の削除 UX ルール準拠)。

## 参考: 今後の候補として残っている未実装アイデア

ブラッシュアップ作業のメモ。優先度は都度相談。

- **通常時間割作成**: 主な候補は実装済み (自動生成 RB26 のみ却下)。
  Excel 出力は「画面の曜日ビュー再現」を紙面要件として確定 (B4 横 +
  全曜日まとめ)。掲示用の専用レイアウトが必要になったら要件定義から。
  (2026-08-11 時点の実装済み: 手動入力・重複 (講師・教室・クラス)/NG/
  校舎間移動チェック + 承認 + 無効承認の掃除・反映 (コマ id 引き継ぎ / 差分
  プレビュー / 未承認警告 / 対象学年の限定 / 学年・曜日の部分反映)・本体から取込 + 平日/土曜タブ
  分割・複数プロジェクト + プロジェクト間の差分比較・Undo/Redo + 内容 toast +
  ジャンプ・セル D&D 入替 + Ctrl+ドラッグコピー・右クリック/長押しメニュー・
  Ctrl+C/V/Delete・複数選択 + 一括クリア/ロック/一括変更/選択範囲へ貼り付け・
  ⧉ 曜日まるごとコピー・合同セル結合表示・講師リネーム追従・講師NG (複数
  曜日一括・時限から選択)・コマ数上限・時限の一括時刻シフト・科目/教室
  マスタ (並べ替え・マスタ外の教室の検出)・📊 集計 (講師×曜日 + 稼働/拘束/
  空き + クラス×科目)・👁 講師/教室の強調 + 週間ミニビュー (講師は NG 表示・
  教室は空き状況) + 講師別週間印刷・📅 週表示・📌 スナップショット + 差分 +
  反映時の自動保存 + 保存サイズ表示・印刷 (単曜日/全曜日)・空行・空列
  非表示・本校/亀井町の分割表示・リロード時の曜日復元・教室入力の候補補完・
  時限/クラス/講師削除の確認 (セルまで後始末)・JSON 書き出し/読み込み
  (RB20)・セルのロック 🔒・⚙ 全体設定のモーダル + タブ化 (RB19)・
  はじめかたガイド (RB23)・Excel 出力 (曜日別 B4 横 + 全曜日まとめ +
  集計/講師別)・隔週コマの表示/集計/チェック (0.5 重み + パートナー)・
  合同コマの作成・変更 (`jointEdit.js`)・◫ 曜日を並べる + コースセット
  (`courseSets.js`)・グリッド横断 D&D・講師マスタのよみ/担当科目 +
  講師プルダウンの科目別グループ + アイウエオ順 (`teacherOrder.js`))
  - 未実装で残っているもの: **クラス×科目の目標コマ数と過不足表示**。要相談。

- **A7**: `Shift+?` を US / JP キーレイアウトで確実に拾えるか実機検証（コード変更ではなく実機テスト案件）
- **A8**: ショートカット設定のユーザーカスタム化（要件定義から）
- **A5b**: cascade ありの削除を Undo 対応にする検討（連鎖更新の復元設計が必要）
