# Changelog

## [Unreleased]

### Changed (コマカードと時間割セルをキーボードで操作できるようにした)

欠勤組み換えのカード (`AbsenceSlotCard`) とタイムテーブルのセル
(`ExcelCell`) は、右クリック・ダブルクリック・D&D にしか操作が無く、Tab で
到達できず読み上げもされなかった。

- どちらも操作があるときだけ `tabIndex=0` / `role="button"` になり、
  「19:50-20:35 中3 S 数学 堀上（欠勤、代行未定）」のように内容と状態を
  読み上げ名に持つ
- カード: Enter / Space でクリック相当、**ContextMenu キー / Shift+F10 で
  右クリック相当** (メニューはカードの左下に出る)
- セル: 代行モードでは Enter / Space でクリック相当、通常モード (管理者)
  では編集を開く (ダブルクリック相当)
- キーボードフォーカスのときだけ枠を出す (`:focus-visible`)

### Changed (CI と開発環境の整備)

- **E2E (Playwright) を CI で回す** (`ci.yml` の `e2e` ジョブ。build の後、
  失敗時はレポートを artifact に残す)。印刷 2 系統と Worker 経路は jsdom で
  検証できないので、これまで壊れても気付けなかった
- `npm run lint` を `--max-warnings 0` に (警告も CI で止める)。CI に
  `concurrency` (同じブランチへの連続 push で古い run を止める)
- `.editorconfig`、`.vscode/settings.json` (ESLint 拡張にサブディレクトリを
  教える)、PR テンプレート (`.github/PULL_REQUEST_TEMPLATE.md`) を追加
- GitHub Skills の教材の残骸 (`.github/steps/`) を削除
- README のディレクトリ構成を実態に合わせ、CONTRIBUTING に E2E・
  `CLAUDE.md`・CHANGELOG 追記の慣習・`.env.local` を追記
- 代行確定一覧の「確定 かつ 代行者あり」の判定を `substituteState.subState`
  に寄せた (画面に条件を書き起こさない。取り違えの再発防止)

### Fixed (存在しない日付が休講日・テスト期間に保存できていた)

`dateHelpers.isValidDateStr` が `Date.parse` に頼っていたため、V8 では
`2026-02-31` (→ 3/3 に丸められる) を通していた。保存された文字列は休講日・
テスト期間の文字列一致に永久にヒットしない。年月日を組み立てて往復一致で
確かめるようにした。

### Changed (性能・キーボード・閲覧者まわりの小さな改善)

- **講師検索の打鍵で App 全体が再描画されなくなった**。検索文字列を
  `Sidebar` のローカル state に降ろし、グループ分けは全量を 1 度だけ作って
  `filterTeacherGroups` で絞る (`useTeacherGroups` を 2 回呼んでいたのも 1 回に)
- **月間カレンダーの第N回計算で id → コマの Map を日ごとに作り直さない**
  (`useSessionCtx` が `_slotById` を 1 度作って渡す。`sessionCount.buildSlotById`)
- **閲覧者の書込が権限で拒否されたら、サーバの値に巻き戻す**
  (`useSyncedStorage.rollbackToServer`)。以前はこの端末だけが別のデータを
  表示し続けていた。ネットワーク断は SDK が再送するので巻き戻さない
- `prefers-reduced-motion` でアニメーションと transition を止める。
  「本文へ移動」のスキップリンク (Tab で最初に現れる)。月送りの ◀ ▶ に
  `aria-label`、年月に `aria-live`

### Changed (UX / アクセシビリティの点検で見つかった小さな穴をまとめて塞いだ)

- **クラウド書込・端末保存の失敗を toast で知らせる** (`App.onStorageError`)。
  権限エラー以外の失敗はフッタの同期ドットが赤くなるだけで気付けなかった。
  30 秒に 1 回に抑える。エラー toast は 4.5 秒 → 8 秒
- **「未定」と「依頼」の色を分けた** (`substituteState`)。代行者を探す必要が
  ある「未定」は橙、頼んで未返事の「依頼」は赤のまま。同じ赤だと遠目・
  白黒印刷で区別できなかった
- **追加授業の担当講師に既存の講師名の候補** (`<datalist>`) を出す。
  自由入力の表記ゆれ (末尾空白・別表記) で講師別カレンダーから外れるのを防ぐ
- **スマホでは閉じた状態から始める** (`App` の `sidebarOpen` 初期値を
  `matchMedia` で決める)。初回表示でサイドバーがダッシュボードを隠していた
- **データ管理のインポート・孤立データ掃除・初期化を管理者だけに**
  (`DataManager` の `isAdmin`)。閲覧者が押すとクラウド書込が拒否され、
  その端末の表示だけがずれていた。エクスポート・CSV は従来どおり誰でも
- **キーボード / スクリーンリーダー**:
  - サイドバーの `<button>` の中にあった `role="button"` (依頼中バッジ・
    展開トグル) を兄弟の `<button>` に分けた (入れ子は不正な DOM)。現在の
    ビューに `aria-current="page"`、トグルに `aria-expanded`
  - 休講・テスト期間・特別イベント・特別時程の学年チェックボックス 8 か所を
    `display:none` から `VISUALLY_HIDDEN` に (Tab で到達でき、読み上げも
    される)。`label:has(> input:focus-visible)` でフォーカスリングを出す
  - 本体の `ContextMenu` に `role="menu"` / `menuitem`、開いたら先頭項目へ
    フォーカス、↑↓ / Home / End で移動、Esc で閉じる、画面端でクランプ
  - ログインフォームに `aria-label` / `autoComplete`、エラーに `role="alert"`
- `useToasts` の context value を `useMemo` で安定させた (toast が出る /
  消えるたびに全消費者が再レンダーされていた)

### Fixed (孤立データを含むバックアップが復元できなかった)

通常時間割作成の「置き換え」反映は slots / timetables しか保存せず、消えた
コマを参照する代行・調整・回数補正・旧形式 (slotIds) の授業セットが残って
いた。一方でインポートの参照整合性チェックはそれを**丸ごと拒否**するので、
反映のあとに取ったバックアップが二度と復元できなかった。

- **インポートの参照整合性は「拒否」から「警告」に変更**
  (`schema.validateExportBundle` が `warnings` を返す)。参照先の無いレコードは
  取り込んだうえで件数を toast に出し、同じ画面の「孤立データ掃除」へ案内する
- **反映 (置き換え) でコマが減ったときは、消えたコマに紐づくデータを
  その場で掃除する** (`orphanCleanup.cascadeOrphansForSlots`、App の
  `handleSlotsReflected`)。コマ削除の cascade と同じ対象。確認文を
  「無効になります」から「一緒に削除されます」に改め、掃除した件数を
  完了 toast に出す
- **旧形式 (slotIds) の授業セットも孤立データ掃除の対象に加えた**
  (`analyzeOrphanClassSets`)。参照先が全部消えたセットは削除、一部なら
  消えた id を抜く。units 形式は対象外 (コマ id を参照しない)

### Fixed (デプロイ直後の古いタブが「保存データを初期化」に誘導していた)

GitHub Pages に新しい版を配信するとハッシュ付きチャンク名が変わり、開き
っぱなしのタブで別のビューへ移った瞬間に lazy import が失敗する。それを
ルートの ErrorBoundary が「画面の描画中にエラー」として受け、第 2 ボタンの
**localStorage 全消去**を見せていた (単なるキャッシュ切れでデータを消す導線)。

- **チャンク読込失敗を判別** (`ErrorBoundary.isChunkLoadError`) して
  「アプリが更新されました。再読込してください。保存データは消えません」
  だけを出す (初期化ボタンは出さない)
- **ビュー単位の ErrorBoundary** (`scope="view"`) を `#main-content` の
  Suspense の内側に置いた。1 つのビューの描画バグでサイドバーごと落ちず、
  「再試行」か別のビューへの移動 (`resetKey`) で復帰する。初期化ボタンは
  ルート (scope="app") にしか出さない

### Changed (依存関係の更新を回るようにした)

- **Node 22** に統一 (`.nvmrc` / `package.json` の `engines` / CI と deploy の
  `node-version-file`)。Node 20 は 2026-04-30 で EOL
- **Dependabot**: npm の minor / patch を 1 PR にまとめ、major (React 19 /
  Vite 8 / ESLint 10 / Tailwind 4 など) は `ignore` で出さない (個別に判断
  して手で上げる)。GitHub Actions も 1 PR にまとめる。上限を 10 に
- `npm audit fix` で修正できる脆弱性 (@grpc/grpc-js / brace-expansion /
  protobufjs) を lockfile で更新。残るのは exceljs が依存する uuid の
  moderate 2 件で、直すには exceljs のダウングレードが要るため見送り
  (使っている v3/v5/v6 の buf 引数の経路は exceljs の用途に無い)

### Fixed (最後の 1 件を消すと他端末から復活していた同期の穴を塞いだ)

Firebase RTDB は `[]` / `{}` を書くとノードごと消し、他端末には `null` が
届く。`useSyncedStorage` はその `null` を「Firebase 側が未初期化 = 自分の
localStorage で seed する」と読んでいたので、**代行記録や回数補正を最後の
1 件まで消す / データ管理の「初期化」をする**と、開いている他端末が古い
配列を書き戻して削除が復活していた。

- 空の値は **`{__empty: "<JSON>"}` のマーカー**として書き、`null` が届くのは
  本当に一度も書かれていないときだけにした (`useSyncedStorage.encodeForServer`
  / `decodeFromServer`)。`[]` と `{groups: [], cohorts: []}` のような形の
  違いもそのまま戻る
- `null` からの seed は**セッション中 1 回だけ**。値を受け取った後の `null`
  (旧版のタブが `[]` を書いた等) は無視して手元の state を保つ
- 併せて、配列のキーに RTDB がオブジェクト (`{0: …, 2: …}`。疎な配列や
  コンソールで 1 件消したとき) を返した場合は配列に直すようにした
  (そのまま入れると `slots.filter is not a function` で全画面が落ち、
  localStorage にも書かれてリロードで再現していた)

### Fixed (リロードのたびに管理者ログインが外れていた)

`firebase/config.js` がモジュール読込時に無条件で `signInAnonymously` を
呼んでいた。SDK は「今のユーザが匿名でなければ新しい匿名ユーザで置き換える」
ので、永続化された password セッションが毎回捨てられ、匿名ユーザも毎回
1 つ増えていた。`onAuthStateChanged` の初回でユーザが居ないときだけ匿名
サインインするようにした。

### Security (RTDB の書込権限を「登録済み管理者」に絞った)

`database.rules.json` の `.write` が「password プロバイダなら誰でも」だった。
Firebase Auth の Email/Password はクライアントからのアカウント作成が既定で
有効なので、公開ビルドの API キーで誰でも管理者になれる余地があった。

- 書込は **password ログイン かつ `/admins/<uid>: true`** に登録された
  uid だけ。**先に `/admins` へ uid を登録してからルールをデプロイする**
  (手順は `.env.example` / README)
- `appData` 直下は実際に書くキーだけを列挙し、それ以外は `$other` で拒否。
  各キーに最低限の型検証 (`hasChildren()` / 文字列) を付けた
- `.gitignore` に `.env` / `.env.*` を追加 (`.env.example` だけ追跡)

### Changed (GitHub Pages への配信を CI 成功後に限定した)

`deploy.yml` が `push: main` で単独起動していたため、テストが落ちたコミット
もそのまま配信されていた。`workflow_run` で CI (lint / typecheck / test /
build) の成功後にだけ走るようにした。手動実行 (`workflow_dispatch`) は
従来どおり。

### Added (テスト期間中でも例外的に授業を行う日を設定できるようにした)

イレギュラーで**特訓は始まっているのに授業は休みにならない日**があった
(2026-09-19 土の中3)。テスト期間に「例外的に授業を行う日」を持たせて、
その日だけ休止扱いにしないようにした。

- **⚙ 休講・テスト期間・イベント → テスト期間管理**の編集フォームに
  **「例外的に授業を行う日」**を追加。日付 + 授業を行う学年 (未選択 =
  対象学年すべて) + メモで登録する。「授業を休止する」が ON のテスト期間
  でのみ出す (OFF は元々授業を止めないため)
- 判定は `utils/scheduleHelpers` の **`examPeriodStopsClassesOn`** に集約。
  期間・対象学年・`stopsClasses`・例外日をここ 1 か所で見るので、
  **ダッシュボード / タイムテーブル / 月間・週間 / 第N回 / 欠勤登録 /
  日まるごと振替 / バイトの出勤日**がまとめて追従する
- **例外日は隔週ローテーションを送らない** (授業を行う週なので、
  休講・テスト期間で週送りする補正の対象外)
- **休講 (Holiday) の方が強い**。例外日でも休講が入っていれば休講のまま
  (テスト期間の例外は「テスト期間による休止」だけを外す)
- テスト期間そのものの表示は消さない。日別ダッシュボードの日付帯と
  イベントカレンダーに**「📖 中3 授業あり」**バッジを併記する
- 特訓シフト (`examPrepSchedules`) は無関係に動く。例外日も特訓の出勤は
  そのまま出る (今回の「特訓はあるが授業も行う日」がまさにこれ)

### Added (コースマスター管理のタブをサイドバーに出した)

「隔週管理」が**コースマスター管理のタブの中**にしか無く、画面名からは
辿り着けなかった。休講・テスト期間・イベントと同じ扱いにして、その画面を
開いている間だけタブ名をサイドバーに並べるようにした。

- サイドバーの**⚙ コースマスター管理**を開くと、下に**コマ一覧 / 隔週管理 /
  時間割表**が並ぶ (クリックでタブ切替、現在のタブを強調)。常時展開しないのは
  従来どおり (ビュー一覧が読めなくなるため)
- **Cmd+K でも「隔週管理」「時間割表」で直接開ける** (授業管理のサブタブと
  同じ扱い)
- タブの定義を `constants/masterTabs.js` に集約し、MasterView のタブバーも
  この配列から作るようにした (ラベルを 2 か所に書き起こさない)


### Fixed (振替の「休み」表示が消えていたのを直した)

「代行に出した日も休みと分かるように」した際に、**振替元の日の
「↻ 振替で休み」が出なくなっていた**。振替ピッカーの「振替先の担当 (任意)」
が**元担当を既定で選んでいた**ため、ほぼ全部の振替が「担当は自分のまま =
手を離れていない」と判定され、講師別カレンダーから日単位の休み表示も
コマの取消線も行き先も消えていた。

- **振替は振替先の担当を問わず「その日は担当なし」**にした。振替はコマが
  その日から出ていく操作なので、振替先で教えるのが自分でも振替元の日は空く
- **担当が変わらない振替では担当の表記を出さない**ようにした。振替先が
  元担当と同じときに「→ 堀上」「(堀上)」と出ていたのを、月間カレンダー・
  日別ダッシュボード・タイムテーブル・時間割調整一覧でそろえて省く
- 振替ピッカーの「振替先の担当」の既定を**空 (「(元担当 ◯◯ のまま)」)** に
  戻した。担当を変えるときだけ選ぶ

### Changed (講師別カレンダーで「代行に出した日」も休みと分かるようにした)

振替と同じ考え方を**代行・合同**にも広げた。講師別の月間カレンダー
(週間/月間の「月間」) が対象。

- **コマ側**: 自分の手を離れたコマは取消線 + **相手をカードに出す**
  (「→ 河野」= 代行に出した / 「→ 香川 に合同」/ 「→12/4」= 別日へ振替)。
  これまで相手の名前は tooltip にしか無く、一覧では分からなかった
- **他人のコマを代行する日**にも「(河野 の代行)」を出す
- **日単位**: その日のコマが全部手を離れたら「代 代行で休み」
  「合 合同で休み」「↻ 振替で休み」、理由が混ざったら「この日は担当なし」
  をセルの色つきで出す
- **その日に残る仕事があれば休みとは言わない** — 他人のコマの代行・
  他日から入ってくる振替・追加授業・講習コマを数える
- **振替先の担当が自分なら手を離れていない**扱い (日が変わるだけ)
- 日別ダッシュボードとタイムテーブルは変更なし。**代行が付いても授業自体は
  行われる**ので、休みになるのは担当を外れた本人だけ

### Changed (振替元の日が「休みになった」と分かるようにした)

授業を別日へ振り替えたあと、**振替元の日**が休みになったように見えず、
まだ授業があるように読めていた。日付ベースの 3 画面 (月間カレンダー /
日別ダッシュボード / タイムテーブル) をそろえて直した。

- **コマ側**: 教科と時刻に**取消線**を引き、**行き先をコマの中に出す**
  (「→ 12/4 (金) 19:40 (福江)」。月間カレンダーは幅がないので「→12/4」)。
  これまで行き先はバッジの tooltip にしか無かった
- **日単位**: その日のコマが**全部**出ていったら「振替で休み」を出す。
  日別ダッシュボードとタイムテーブルは「↻ この日の授業は N コマとも
  12/4 (金) へ振替済み」のバナー、月間カレンダーはセルの色 + 日付の横に
  「↻ 振替で休み」。休講日と同じ強さで、月の一覧でも空いた日が分かる
- **1 コマでも残っていれば日単位の表示は出さない** (入ってくる振替・追加
  授業・講習コマ・他人の代行も「その日にやる」ものとして数える)
- **コマは消さない**。どこへ移したかを調べる手がかりなので、取消線で残す

### Fixed (欠勤登録に旧期のコマが重なって出ていた)

欠勤登録の時間割グリッドが、同じクラスを 2 重・3 重に並べていた。

- 原因は**曜日だけでコマを絞っていた**こと。期切替では前の期のコマを消さず
  終了日を入れるだけなので、旧期の時間割のコマも同じ曜日に残る。日付ベースの
  他のビュー (ダッシュボード / タイムテーブル / 日まるごと振替) が通している
  **時間割の有効期間 (`filterSlotsForDate`)** と**表示期間・コース別終講日
  (`isSlotBeyondCutoff`)** を、この画面だけ通していなかった
- 絞り込みを `absenceHelpers.getAbsenceDaySlots` に切り出して両方を通した。
  セクション見出しのコマ数と、欠勤先生の赤枠も画面に出ているコマだけを見る
- **休講・テスト期間はここでは落とさない**。その日の状態を確かめる画面なので、
  従来どおり灰色の「休講」カードとして出す
- 表示期間の外の日は、コマを消すだけでなく**「開講前」「未確定」を出し分けて**
  伝えるようにした (他の画面と同じ文言)

### Added (日まるごと振替 — ある日の授業をまとめて別の日へ)

「12/7 (月) の授業を 12/4 (金) にまとめて振り替える」のように、**日単位で
授業を移す操作**を追加した。授業管理 → 時間割調整一覧の **「📅 日まるごと
振替」** から (Cmd+K でも「日まるごと振替」で開ける)。

- 作られるのは既存の**別日振替 (`adjustments` の `reschedule`)** をコマの
  数だけ並べたもの。**新しいモデルは足していない**ので、表示・印刷・一覧・
  個別解除はすべて既存の振替の経路にそのまま乗る
- 対象は**振替元日に実施されるコマだけ**。休講・テスト期間・時間割の有効
  期間外・表示期間外・特別時程の部分休講・隔週の B 週は最初から外し、
  **外した理由を画面に出す** (黙って減らさない)。実施判定は
  `sessionCount.isSlotHeldOnDate` + `timetable.isSlotBeyondCutoff` に委ね、
  画面独自のルールは書いていない
- 振替先の**講師・教室の重なりをコマごとに出す** (その日の通常授業に加え、
  他日からその日へ振り替えられてくるコマ・その日の追加授業・コマ移動・特別
  時程の時刻読み替えまで見る)。**警告であって禁止ではない** — 重なるかどうかを
  見た上で人が決める。振替先が同じ曜日の別の週 (12/7 月 → 12/14 月) は通常
  授業と二重になるので必ず重なりに出る
- **日付に紐づいていて振替先に付いてこないもの**を注意書きに出す。代行・
  合同授業・コマ移動はどれも (コマ, 日付) 参照なので、日をまるごと動かすと
  振替元の日に取り残される
- **既定は全選択**。外したいコマだけチェックを外せる
- 同じコマに既に振替が登録されていれば**上書き** (二重登録しない)。
  振替元日の振替を**まとめて解除**するボタンもある (cascade 無しなので
  Undo つき削除)
- **時刻は変えない**。1 コマだけ時刻をずらす場合は従来どおり欠勤組み換え
  画面から。**第N回は振替元の日付で数えたまま** (回数の総数は変わらない)

### Changed (「休講・テスト期間・イベント」の中身をサイドバーに出した)

この画面には**休講日 / テスト期間・特訓 / 特別イベント / 追加授業 / 特別時程**
の 5 つが縦に並んでいるが、画面名からは「追加授業」「特別時程」に辿り着けず、
スクロールしないと存在に気付けなかった。

- **その画面を開いている間だけ、サイドバーに 5 つのセクション名を出す**。
  クリックでそのセクションまでスクロールする
- 常時展開はしない (サイドバーが長くなって元のビュー一覧が読めなくなる)
- セクションの定義は `constants/eventKinds.js` の `EVENT_SECTIONS`。
  アンカー id (`eventSectionAnchorId`) を App と Sidebar で共有する

### Fixed (全日休講の日に振替のコマが消えていた)

12/7 の授業を 12/4 (全日休講) へまとめて振り替えたのに、画面のどこにも出て
こなかった件。**全日休講の日は「本日休講」だけを出して中身を描かない**画面が
あり、振替で入ってくるコマまで巻き添えにしていた。

- **ダッシュボードの時間割モード / タイムテーブル** (`ExcelGridView`):
  全日休講とコマ 0 件の日はセクションを描かないため、セクション内の振替
  バナーごと消えていた。グリッド上部にまとめて出すようにした
- **月次カレンダー** (`MonthView`): 全日休講の日は振替カードを出さない条件に
  なっていた。追加授業と同じく「その日にやると明示登録したコマ」なので、
  休講では消さない (表示期間外の未確定日では従来どおり消す)
- 日付 → 入ってくる振替の一覧は
  `adjustmentDisplay.collectIncomingReschedules` に集約し、日別・時間割の
  両方で共有した

### Changed (日まるごと振替をサイドバーから開けるようにした)

授業管理 → 時間割調整一覧のタブの中にボタンがあるだけで、機能があること自体
気付きにくかった。**サイドバーに「📅 日まるごと振替」を追加**（欠勤組み換えの
下、管理者のみ表示）。ダイアログ本体は App が持ち、サイドバー・時間割調整
一覧のボタン・Cmd+K のどこから開いても同じものが出る。

### Fixed (ダッシュボードに振替が出ていなかった)

日別ダッシュボードだけが振替 (reschedule) を見ていなかった。全日休講の日へ
振り替えると、**画面のどこにも出ない**まま消えていた (日まるごと振替は
「休みの日へ寄せる」のが典型なので、そのままでは使えない)。

- **他日から入ってくるコマのバナー**を日別行に追加した。追加授業
  (`ExtraLessonBanner`) と同じく「その日にやると明示登録されたコマ」なので、
  **全日休講の日でも消さない**
- 他日へ出ていくコマには**「振」バッジ**を出し、カードを薄くした
  (月次カレンダー・タイムテーブルと同じ扱い)

### Fixed (授業セットを「学年 × 曜日」で書けるようにした — 中3 火木 / 水金 の第N回)

期切替（1学期 → 2学期）のあと、水曜の中3 の第1回が火曜の続き（第2回）として
数えられていた件。原因は 2 つ重なっていた。

- **既定のコース束ね（`utils/cohorts`）は「中学は学年の平日ぜんぶで 1 コース」**。
  中3 のように週内で**火木コース / 水金コース**へ分かれる学年は、授業セットを
  登録しない限り 4 曜日が 1 本のカウンタになる
- **授業セットはコマ id を直に指していた**ので、期切替で新しい期のコマを作ると
  紐付けが切れる。切り替えるたびに作り直さないと、上の既定の束ねへ落ちる

そこで**授業セットの定義を「学年 × 曜日」の組（`units`）に変えた**。コマ id を
参照しないので、期切替で新しいコマを作っても紐付けが切れない（画面の選択単位は
もともと「学年 × 曜日」だったので、保存する形をそれに合わせただけ）。

- **🔀 コース分けの候補**を授業セット画面に追加。週 4 日ある学年に
  「中3 を 火木 / 水金 に分ける」を出し、**1 クリックで 2 セット登録**する。
  候補の作り方は「曜日の間隔が 2 日または 3 日の組に分けきれるか」だけ
  （火水木金 → 火木 / 水金、月火木金 → 月木 / 火金）。**あくまで候補**で
  自動適用はしない（週 4 日ぜんぶ同じ生徒が通う学年もあるため）
- 旧形式（コマ id 直参照）のセットには**「旧形式（コマ id 固定）」バッジ**と
  **「曜日ベースに変換」ボタン**を出す。読み取りは従来どおり動く
- **旧形式の変換は件数を出してから**行う。旧いセットが「その曜日の一部の
  クラスだけ」を指していると、曜日ベースにした時点で同じ学年・曜日の他クラスも
  同じセットに入るため。一覧に「変換すると対象が N → M コマに増えます」と出し、
  増える場合は確認ダイアログを挟む
- **期切替の警告は旧形式のセットだけ**に出るようにした。units 形式のセットは
  作り直し不要なので警告しない
- 時間割の複製でも units 形式のセットは複製しない（複製先のコマにもそのまま
  効くので、複製すると二重定義になる）
- 回数の索引（コース束ね・授業セット）は `useSessionCtx` で 1 度だけ作るように
  した。`buildSessionCountMap` は月次カレンダー（日ごと）や最終授業日の逆算
  （最大 29 日ぶん遡る）のようにループから呼ばれるため、呼び出しごとに索引を
  作り直すと全コマの走査がその回数だけ走っていた
- schema v17（`ClassSet.units` を追加。旧 `slotIds` 形式はそのまま読める）

### Fixed (校正: 表示期間設定・終講日まわりの手直し)

一通り実装したあとの読み直しで見つかった穴と、体裁の統一。

- **同じ学年が複数の学年グループに入っていると、後ろの行の設定は黙って
  効かなくなる**（`findGroupForGrade` は先頭一致）。とくに複合学年
  （「中1-3」は中1 として中1・2 グループにも一致する）で起きるので、
  行ごとに「⚠ 中1-3 は「中1・2」が先に一致するため、この行の設定は効きません」と
  警告するようにした
- **コース別終講日の一括設定に確認を入れた**（10 コース以上のとき）。絞り込まずに
  押すと全コースの終講日を上書きしてしまい、元の値には戻せないため
- **月次の「通常出勤日」に特別時程（部分休講）が効いていなかった**
  （`staffMonthlyRegularDates` は対応済みだったが `daySchedules` が
  講師管理・代行タブから渡っていなかった）
- 学年グループの行の**「最終授業日」は終了日とズレているときだけ**出す
  （同じ日なら情報が重複するだけ）。開始日 > 終了日 の行では逆算しない
  （矛盾した日付が警告の隣に出ていた）
- 学年グループの行では**第N回を出さない**ようにした。グループには複数コースが
  混ざっていて進度カウンタが別々なので「何回目で終わる」が一意に決まらないため
  （コース単位の第N回はコース別終講日カードに出ている）。あわせて設定画面の
  再計算が **約 140ms → 1.4ms** に落ちた（コース側も画面に出ている行だけ計算する）
- 用字の統一と言い回しの手直し（期間は「伸ばす」→「延ばす」、
  「表示中の N コース」→ 折りたたみ分も含むので「該当 N コース」、
  「この日はまだ開講前です（通常授業の開始前）」→
  「この日は開講前です（通常授業はまだ始まっていません）」ほか）
- 死んだコード・引数の掃除（`getDayCutoffKind` の到達しない分岐、
  `findLastSessionOnOrBefore` の未使用オプション、`cutoffTimeline` の未使用引数）

### Added (期間の全体像: 時間割 / 学年グループ / コースを 1 本の物差しで)

- 時間割管理に**「期間の全体像」カード**を追加した（R9）。時間割の有効期間・
  学年グループの表示期間・コース別終講日を同じ物差しの帯で並べる。
  設定は下の 2 枚のカードで、ここは「読む」ための面。
  - **「コマが出ない期間」を斜線で可視化**した。時間割はまだ有効なのに
    表示期間が先に終わっている区間（＝期切替で表示期間を伸ばし忘れた状態）が
    一目で分かる。件数入りの警告も出す
  - 開始日 > 終了日、グループ終了日より後のコース終講日は**赤い帯**で示す
  - 今日の位置に線、月の目盛り、コース行の表示トグル
  - 帯の組み立ては `utils/cutoffTimeline.js`（純粋関数・位置は 0..1 の割合）
  - **「実際の最終授業日」は帯に出さない** — 半年〜1年の物差しでは終了日との
    差が数日しかなく必ずバーの端に重なるため。日付は表示期間設定カードに
    数字で出している

### Added (期切替に表示期間設定を追従させるオプション)

期切替は時間割の有効期間しか動かさないので、表示期間設定が旧期で終わって
いると新しい期のコマが画面に出ない（警告は出るが直すのは別画面だった）。
反映ダイアログの期切替セクションに、**オプトインのチェックボックス**を追加。

- **「表示期間設定も新しい期に合わせる」** — 切替日より前で終わっている
  学年グループの終了日を新しい期の終了日（未入力なら解除）に変更し、
  旧期のコース別終講日を削除する。対象の件数を添えて予告する
- **「新しい期はオリエンなしにする（初回1限も回数に数える）」** — 2学期以降
  のようにオリエンが入らない期向け。いま有効なグループだけを対象にする
- **どちらも既定はオフ**。学年グループは期をまたいで共通なので、どこまで
  面倒を見るかは人が選ぶ（自動では触らない）。判定は
  `regular-builder/reflect.buildCutoffFollowUp`（純粋関数）に集約

### Added (コース別 終講日: 探せる・まとめて入れる・そろえる)

高3 だけで 20 行を超えるなど一覧が長く、1 件ずつしか触れなかったのを改善。

- **検索と「未設定のみ」フィルタ**を付けた（R6）。1 学年に 8 コースを超える
  ときは折りたたみ、「他 N コースを表示」で開く
- **表示中のコースへの一括設定 / 一括解除**（R5）。絞り込みと組み合わせて
  「高3 の未設定だけまとめて 1/17」のような入れ方ができる
- **未使用エントリ（対象の授業が見つからない設定）のまとめて削除**（R7）。
  これまでは 1 件ずつ日付をクリアするしかなかった
- **終講日が実際の最終授業日とズレているとき**（火金コースに木曜を入れた等）、
  「終講日をこの日にそろえる」ボタンを出す（R8）

### Added (表示期間設定: 対象学年の編集と一括設定)

- **学年グループの対象学年をその場で編集できるようにした**（R2）。学年欄は
  自由入力なので「附中」「高1高2」のような表記が生まれ、どのグループにも
  属さないコマは表示期間も終講日も効かず第N回も出ない。
  - 未所属の学年の警告に**「グループに追加…」プルダウン**を付けた
  - 各グループの対象学年をチップで出し、× で外せる
  - **学年は 1 つのグループにしか属さない**（追加時に他のグループから外す）。
    `findGroupForGrade` は先頭一致なので、二重登録すると後ろのグループの
    設定が黙って効かなくなるため
  - 学年グループ自体の**追加・削除**も可能にした（削除は波及があるので
    確認ダイアログ。同名グループは作れない）
- **開始日・終了日の一括設定**を追加した（R5）。対象（全グループ / 中学部 /
  高校部）を選んで日付を入れると、まとめて適用する。**空欄の項目は
  触らない**ので「終了日だけ揃える」もできる

### Fixed / Changed (表示期間設定・終講日まわりの点検)

設定の効き目が見えず、静かに食い違っていた箇所をまとめて直した。

- **複合学年 (「中1-3」の土曜プレップ等) で「第N回」が出なかった。**
  回数の起点だけが学年の完全一致でグループを引いていたため、表示は
  中1・2 の期間で効いているのに回数は 0、という片肺状態だった。
  表示フィルタと同じ `findGroupForGrade` に統一（複合学年を展開して照合）
- **どの学年グループにも属さない学年を警告するようにした。** 学年欄は
  自由入力なので「附中」「高1高2」のような表記が生まれ、そのコマは
  表示期間・終講日が一切効かず（常に表示され）第N回も出ない。
  表示期間設定の末尾に学年とコマ数を出す
- **「この日以降の予定は未確定です」を開講前の日に出さないようにした。**
  全日判定が開始日前でも真になるため、2学期の開始日を先に入れておくと
  夏休み中ずっと「未確定」と出ていた（意味が逆）。
  `getDayCutoffKind` で 開講前 / 未確定（終講後）/ 期間外（混在）を出し分け、
  ダッシュボード・タイムテーブル・月次カレンダーの 3 経路で文言を共有する
- **月次の「通常出勤日」が表示期間も時間割の有効期間も見ていなかった。**
  7 月で終わった通常授業が 8 月の出勤日として数えられていた。
  `staffMonthlyRegularDates` に時間割・表示期間・特別時程の部分休講を
  効かせた。あわせて、代行タブが `examPeriods` を渡しておらず講師管理と
  日数が食い違っていたのも揃えた
- **全日判定から「コマが 1 つも無い学年グループ」を外した。** 運用して
  いない学年グループの終了日が空のままだと、他が全部終わってもバナーが
  出なかった。どのグループにもコマが無いときは従来どおり全グループで判定する
- **矛盾した設定を警告するようにした。** 開始日 > 終了日（そのコマは
  どの日にも出ない）、コース別終講日がグループ開始日より前

### Added (表示期間設定・終講日の「効き目」表示)

- 学年グループの行に **対象コマ数 / 授業曜日 / 開講日 / 実際の最終授業日
  （第N回）** を出すようにした。終了日を土曜に入れても実際の最後の授業は
  金曜、といったズレが目で分かる。最終授業日は休講・テスト期間・特別時程・
  隔週・時間割の有効期間まで**回数計算と同じルール**で逆算する
  (`utils/lastSessionDate.js`)
- コース別 終講日の各行にも同じ **最終授業日（第N回）** を出す
  （終講日を入れたコースのみ計算する）
- 「開始日は『第N回』のカウント起点でもある」旨を設定画面に明記した
- 表示期間設定のカードを `components/DisplayCutoffEditor.jsx` に切り出した

### Changed (開講日 1 限のオリエン扱いを設定できるようにした)

- 授業回数（第N回）の「開講日の 1 限はオリエンテーションなのでカウントしない」
  挙動を、**時間割管理 → 表示期間設定の学年グループ単位のチェックボックス**
  「初回1限はオリエン（回数に数えない）」で切り替えられるようにした。
  従来はコード側で中学部固定だった。
  - **2 学期以降のようにオリエンが入らない期はチェックを外す**と、開始日
    以降の初回授業日の 1 限も ① から数える
  - 保存先は `displayCutoff.groups[].orientationFirstDay`（真偽値）。
    **未設定（従来データ）は従来どおり中学部のみ有効**なので、既存の回数は
    変わらない
  - 学年グループ単位なので、中1・2 だけオリエンあり / 高校部でもオリエンあり、
    といった設定もできる
  - オリエン日の判定基準（学年グループの開始日以降の初回授業日、セット内の
    最早時刻 = 1 限）は従来のまま

### Added (通常時間割作成: 学年・曜日を選んだ部分反映)

- 「⤴ 本体へ反映」に**「反映する範囲を絞る（学年・曜日）」**を追加した。
  「土曜だけ組み直した」「中3 だけ差し替えたい」ときに、プロジェクト全体を
  反映せずに済む。新規作成・置き換えのどちらでも使える。
  - 学年と曜日は **AND**。どちらの軸も「何も選ばない = その軸では絞らない」
    なので、「中3 だけ（曜日は全部）」「土曜だけ（学年は全部）」も書ける
  - **既定はオフ = 従来どおりプロジェクト全体**。絞るのは明示的な選択にした
- **置き換えでは範囲外の既存コマを触らない。** 反映の突き合わせは
  「下書きに無い既存コマは削除」なので、**下書きだけ絞ると範囲外のコマが
  丸ごと削除に分類されて消える**。既存コマ側も同じ範囲で絞り、範囲外は
  突き合わせに掛けずそのまま持ち越す。
  - **差分プレビューも同じ範囲で絞る**。絞らないと、実際には触らないコマが
    「削除」として出てしまう（プレビューの削除件数と実際の削除件数が
    一致することをテストで固定した）
  - 確認ダイアログとトーストに範囲を書き、**範囲外の N コマは触りません
    （削除もされません）**と件数で見せる。削除件数だけ出すと範囲外まで
    消えるように読めるため
  - 📌 自動スナップショットの名前にも範囲を残す（`⤴ 反映: 2学期 (中3 の 土曜)`）
- **範囲外だけで起きている未承認の重なりは警告しない。** その重なりはこの
  反映では本体に持ち込まれないため。片方でも範囲に入っていれば従来どおり
  警告する（範囲内のコマが関わる重なりは実際に持ち込まれる）。
- 範囲に該当するコマが 1 件も無いときは、**範囲を書いたエラー**で止める
  （「『水曜』に反映できるコマがありません」）。何を直せばよいか分かるように。
- 絞り込みの状態が実態と食い違わないようにした:
  - **全曜日を選んだ状態は「絞らない」にたたむ**（「月・火・土曜のみ」と
    絞っているように嘘をつかない）
  - チェックを入れただけで何も選んでいないうちは**「まだ絞られていません」**と
    知らせる（黙って全体を反映すると「絞ったつもり」との食い違いになる）
  - タブを消した後に残った古い選択は落とす（何も反映されない理由が分からなく
    なるため）
  - **学年で絞ると「対象学年を下書きの学年に限定する」もその学年だけ**になる。
    中2 だけ反映したのに時間割の対象学年に中3 が入ると、コマの無い学年を
    担当する時間割になり日付ベースの表示・期切替の点検が狂う

### Added (講師名の並びを「よみ」順にした — 本体アプリ全体)

- **本体アプリの講師一覧・プルダウンを、よみ (かな) のアイウエオ順**で出す
  ようにした。代行候補ポップオーバー・講師フィルタ・全講師一覧・講師比較・
  欠勤組み換え・特訓シフト・サイドバーの講師グループなど、**講師名を並べて
  いる 20 箇所すべて**が対象。
  - これまでは全て `localeCompare("ja")` で並べていた。**漢字はこれで読み順に
    ならない** — `Intl.Collator("ja")` / `localeCompare("ja")` は漢字を部首・
    画数順に照合するため、「堀上・河野・香川」が "ほりかみ / こうの / かがわ"
    の順にはならず、人が名前を探すときの手掛かりが無かった
  - 通常時間割作成 (2026-08-14) で入れた「よみ順」の考え方を本体へ広げたもの。
    正規化 (半角カナ → 全角、カタカナ → ひらがな、空白除去) と照合は
    `utils/teacherKana.js` に集約し、通常時間割作成の `teacherOrder.js` も
    そこを参照する (**同じよみで両画面の並びがずれないように**)
- 講師の**よみは「バイト管理」(⚙ → 👤) で登録する**。バイト一覧タブと
  常勤講師タブの両方に入力欄があり、打鍵ごとではなく **blur / Enter で保存**
  する (Firebase への書き込みを確定時の 1 回にまとめるため)。Escape で編集を
  破棄できる。
  - 保存形は `partTimeStaff` の項目ではなく**名前 → よみの独立したマップ**
    (`teacherSubjects` と同型)。**常勤講師 (時間割のコマにしか出てこない人)
    にもよみを付ける**ためで、バックアップの書き出し / 読み込み・初期化にも
    対応している (旧バックアップに `teacherKana` が無いときは現状維持)
  - タブの上に**よみの設定状況の帯**を出す。未設定の人数と**誰が未設定か**を
    名前で並べるので、そのまま埋めに行ける
  - **「📥 通常時間割作成から取り込む」**で、通常時間割作成の講師マスタに
    入れてあるよみを一括で持ってこられる (全プロジェクトを見る)。**一度きりの
    入力補助**であって常時同期ではなく、**既に入っているよみは上書きしない**
    (手入力を黙って書き換えないため)
- **よみが未設定の講師は、勝手に別の順序を捏造せず一覧の末尾**に置き、そこは
  従来どおりの文字列順にする。誰にもよみが無いうちは**並びが従来と一切変わら
  ない**ので、この機能を使わない場合の見た目は変わらない。
  - 関連度で並べているところ (代行候補の「担当教科が一致」順、月次集計の
    件数降順、特訓シフトの教科順) は**主たる並びを変えず、同点のときだけ**
    よみ順で割る。月次集計は 0 件の講師が大半なので、ここが効く

### Added (通常時間割作成: 「⤴ 本体へ反映」に期切替 (1学期 → 2学期) を追加)

- 反映ダイアログ (新規作成) に**「期切替として反映する」**を追加した。
  切替日 (= 新しい時間割の開始日) を入れると、**前の時間割の終了日を
  切替日の前日に設定**して 2 つの期を隙間なくつなぐ。
  - 入れないと切替日以降どちらの時間割も有効なままで、**ダッシュボードや
    タイムテーブルに新旧のコマが二重に出る** (時間割の有効期間は
    startDate/endDate で決まるため)。手作業だと時間割管理で前の時間割に
    終了日を入れ直す必要があった
  - **前の期のコマは消さない**。終了日を入れるだけなので、旧期の代行・
    時間割調整・回数補正・授業セットの紐付けはそのまま残り、過去の日付を
    開けば従来どおり見える
  - 実行前に「新しい時間割 / 前の時間割の終了日 / 表示の切替」を確認
    ダイアログで見せる。前の時間割の開始日より前の切替日はエラー、
    以下は警告で知らせる:
    - 前の時間割の**終了日を前倒し**するとき
    - 前の時間割が**既に切替日より前で終わっている**とき (夏期講習で止めて
      あるなど)。この場合は重ならないので**終了日を伸ばさず据え置く**
    - **切替日以降も有効な他の時間割**があるとき (対象学年が交わるものだけ。
      附属コースのように学年で分かれている時間割は重ならないので挙げない)
    - **表示期間設定 (学年グループ・コース別終講日) の終了日が切替日より前**の
      とき。時間割を切り替えても**新しい期のコマがダッシュボード等に出ない**
      ため、伸ばす場所 (時間割管理) まで案内する
    - **授業セット**が前の時間割のコマに紐づいているとき。新しい期のコマは
      別 id なので、第N回をまとめて数えるには作り直しが要る
- 同じダイアログに**「反映後、表示中の時間割を新しい時間割に切り替える」**を
  追加した (期切替をオンにすると既定でオン。置き換えモードでも使える)。
  週表示・月間・一覧など「現在の時間割」を見るビューはヘッダの時間割
  セレクタで絞っているため、反映しただけでは切り替わらなかった。

### Fixed (ダッシュボードの時間割表示が、表示中の時間割で二重に絞られていた)

- ダッシュボードの**タイムテーブル表示で、表示日に有効なコマが出ない**
  ことがあったのを直した。日付で有効な時間割を選んだ上に、さらにヘッダの
  時間割セレクタでも絞っていたため、**期切替の直後に切替日より前の日を
  開くとその日のコマがまるごと消えていた** (日別リスト表示は日付だけで
  判定しているので出ていた)。ダッシュボードは日付側の判定に統一し、
  セレクタは日付を持たない単体のタイムテーブルビュー専用にした。

### Changed (通常時間割作成: Excel の「全曜日」を 3 曜日ずつ横並べの 2 段組に)

- Excel 出力の**「全曜日」シートを、曜日を横に 3 つ並べた帯 × 2 段**
  (月火水 / 木金土) にした。**A3 横 6 枚 → 2 枚**で週ぶんが刷れる。
  - 1 日 = 1 ページだった頃は倍率が**高さ**で決まっていて、A3 の幅を
    1/3 以上余らせていた (一番広い曜日でも 10.1in / 使える幅 15.7in)。
    その余りに次の曜日を入れる形。3 曜日並べると縛りは幅に移るので、
    倍率は 69% → 51% (科目の字が 6.9pt → 5.1pt) になる
  - 並べ方は画面の**◫ 曜日を並べるビューと同じ** — 曜日ごとにセクションを
    縦に積み、セクションの開始行を曜日間でそろえる。並び順は曜日に依存
    しないキー (中学部 → 高校部 / タブ定義順 / 本校 → 亀井町) で決めるので、
    「月曜は亀井町が先・木曜は本校が先」で行がずれることがない。この順序は
    画面と `regular-builder/sectionOrder.js` で共有する
  - **行の高さは横に並んだ曜日の最大**を採る (Excel の行高はシート共有。
    後から書いた曜日で上書きすると、先の曜日の折り返した行が消える)
  - 曜日別シート (B4 横・1 ページ収め) は従来どおり

### Added (通常時間割作成: 講師プルダウンを科目別 + アイウエオ順に)

- **セルの講師プルダウンを、担当科目ごとの見出し (optgroup) + 各グループ内は
  よみのアイウエオ順**で出すようにした。人数が増えても目当ての講師を探せる。
  - 見出しの並びは**科目マスタ順** (⚙ → 📚 科目 で並べ替えた順がそのまま出る)。
    複数科目を担当する講師は**それぞれのグループに出る** (どちらから探しても
    見つかる)。担当科目が未設定の講師は末尾の「その他」グループへ
    (科目マスタに「その他」という科目がある時だけ見出しは「未設定」)
  - 「(NG)」「(重複)」の予告表示・「✎ 直接入力…」・マスタ外の講師名の扱いは
    従来どおり
- 講師マスタ (⚙ → 👤 講師) に**「よみ」と「担当科目」**を追加した。どちらも
  任意で、**担当科目が誰も未設定のうちは従来どおり見出しの無い一覧**のまま。
  - **漢字の名前は読み順に並べられない** (`Intl.Collator("ja")` は漢字を
    部首・画数順に並べる) ため、アイウエオ順の唯一の手掛かりが「よみ」。
    未設定の講師は**勝手に別の順序にせずマスタ順のまま末尾**に置く
    (何が未設定かが画面で分かる方が直せる。未設定の人数もタブに出す)
  - **「📚 担当科目を推定」**でいまの割当から一括で埋められる。セルの科目名は
    「完全一致 → 科目マスタの名前を含む → 親アプリの教科マスタ (別名) で
    正規化」の順に読み替えるので、「英/数」「中3数学」も英語・数学として拾える。
    既に入っている担当科目は残す (手入力を上書きしない)
- 講師を選ぶ・並べるところは同じアイウエオ順に揃えた (🚫 NG・上限 タブの
  講師選択と一覧、👁 強調表示の候補、「✎ 直接入力」の入力候補)。datalist は
  optgroup を持てないので科目別にはならず、並びだけ揃う。

### Fixed (通常時間割作成: ブラウザ印刷の時間列の幅がセクション・曜日で変わっていた)

- **紙面の「時間」列の幅を実寸で固定した** (A4 縦で 28mm / コンパクト表示は
  23mm)。これまでは**セクションの列数で時間列の幅が変わっていた** — 紙面は
  `table-layout: fixed` + `width:100%` で「幅指定の無い列を等分する」ため、
  同じ日の紙の中でも実測で
  - 中学部 (3 列) → 171px
  - 高校部・本校 (8 列) → 76px
  - 附中（本校）(1 列) → 343px (紙の半分)

  とバラバラになり、セクションごとに表の左端が揃わなかった。曜日によって
  クラス列の数が変わる (その日いない学年・「▤ 空行・空列を隠す」) と、
  **曜日ごとに刷った紙の間でも時間列の幅が変わって**いた。
- **時刻が隣のクラス列へはみ出す (貫通する) のも直った**。時間列は
  `whitespace-nowrap` なので、狭く潰れた高校部 (76px) では「18:30-19:30」
  (約 86px 必要) が収まらず、そのまま隣のコマの上に重なって刷られていた。
  固定幅は実測 + 1 割の余裕で決めてあり、それでも入らない環境 (幅の広い
  フォント・長い時限ラベル) では**隣を侵さずに折り返す**ようにした。
- 余った幅はクラス列が等分するので、**列数の少ないセクションほどセルが
  広くなる** (時間列が痩せる分だけコマ側に回る)。
- **1 ページに収まらない背の高いセクション (11 時限以上) は素直に分断する
  ようにした**。従来は「1 セクションはページをまたがない」(break-inside:
  avoid) を全セクションに効かせていたため、収まらない表があると Chromium は
  その表の前に空きページを作った末に結局分断していた (実測 A4 縦・20 時限:
  「タイトルだけの紙」+「セクション見出しだけの紙」+ 表 2 枚 = **4 ページ** →
  **2 ページ**)。分断しても列見出しはページごとに繰り返される。10 時限までの
  セクションは従来どおりページをまたがない (折り返しの多いセルでも 1 行
  90px として 10 行は必ず A4 縦に収まるため)。
- あわせて**セクション見出し (色帯) だけがページ末尾に取り残されない**ように
  した (`break-after: avoid`)。
- 回帰ガード: CSS 文字列の単体テスト (`printStyle.test.js`) に加えて、
  e2e `print.spec.js` で**実際の紙面レイアウトを測る**テストを足した
  (3 列と 8 列のセクションを同居させ、時間列の幅が一致すること・時刻が
  折り返しもはみ出しもしないことを通常表示とコンパクト表示の両方で確認)。

### Changed (通常時間割作成: Excel 出力を画面の曜日ビューそのままの体裁に)

- **曜日グリッドの Excel (📊 Excel 出力) を、画面で見ている曜日ビューを
  そのまま紙に写した見た目に変えた**。これまでは同じ内容でも「全部 9pt
  の黒字・灰色の見出し」だったため、画面より読み取りづらかった。
  - **セクション見出しが部の色の帯**になり (中学部 = 緑 / 高校部 = 橙 /
    混在 = 青灰)、右肩に**コマ数**が入る。
  - **学年の見出しが学年色** (中1 = 緑 / 中3 = 紫 / 高1 = 橙 …)、
    **クラス見出しは「S 602」のようにラベル + 小さいグレーの教室**。
  - **セルは 3 段組み**: 科目 (太字・濃色) / 講師 (青) / 教室・備考
    (小さいグレー)。科目カラーの背景は従来どおり。
  - **合同コマ (「S〜B」「S/AB」等) が構成クラスの上にセル結合で載る**
    (これまでは合同用の列が 1 本増えていた)。並列の合同 (確認テストの
    複数監督など) はスパンを分け合うところまで画面と同じ。
  - **学年の境目に太い縦罫**が入り、学年のまとまりが目で追える。
  - **時限の見出しは「ラベル(太) + 時刻(小さいグレー)」の 2 段**。
  - **行の高さを中身から見積もる**ようにした (長い科目名で折り返す行は
    高く、短い行は詰める)。時限ラベルやクラス名が長くて折り返す場合も、
    その分だけ見出し行を高くするので**紙面で文字が切れない**。
    見積りは**日本語環境の既定フォント (游ゴシック) の行送り**に合わせて
    ある — Excel は行の高さが足りないと折り返した行を表示しない
    (省略記号も出ない) ため、行間の広いフォント側に倒している。
  - **既定の教室はセルに出さなくなった** (クラス見出しに出ているため)。
    セルに出るのはそのコマだけ教室を変えた場合と、列見出しを持たない
    合同セルの教室。
  - 行の配置 (合同の結合) は画面と同じ計算 (`computeRowCells`) を共有
    しているので、画面とずれない。

### Added (タイムテーブル: 全曜日まとめて印刷)

- **時間割グリッド (タイムテーブル表示) を月〜土まとめて 1 回で印刷できる
  ようにした**。曜日タブの右端に **「🖨 全曜日」** ボタンを追加
  (ダッシュボードの時間割モード / コースマスター管理の時間割表タブ /
  授業管理の時間割タブ)。
  - 従来のトップバー 🖨 は「表示中の曜日」だけを刷るので、週ぶんの紙が
    欲しいと曜日タブを切り替えて 6 回印刷ダイアログを出す必要があった。
  - **コマのある曜日だけ**を対象にし、曜日ごとに改ページする
    (曜日ブロック内は従来どおり 中学 → 改ページ → 高校)。
  - 各ページのヘッダはこれまでと同じ形式。日付が判るビュー
    (ダッシュボード) では**表示中の週のその曜日の日付**が入り
    (例「中学の時間割 — 2026年09月15日（火）」)、日付を持たないビューでは
    曜日を出す (例「中学の時間割 — 火曜日」)。第N回・休講・特別時程・
    追加授業も**その日付**で計算された状態で刷られる。
  - 代行モード中 (代行管理日付を入れている間) は表示が 1 日に固定される
    ため無効。
  - 実装は月次の「📋 まとめて印刷」と同じ popup 系統。曜日を差し替え
    ながら画面の DOM をスナップショットして連結するので、紙面は
    「画面に出ているものそのまま」になる。popup 生成
    (`utils/printWindow.js`) とヘッダ注入 (`injectTimetableHeaders`) は
    単日印刷と共通化した。

### Fixed (通常時間割作成: 隔週コマ同士の誤った「重複」警告)

- **主担当とパートナーが左右で入れ替わる隔週コマの組に「⚠ 重複」が
  出ていた**。例 (中2 19:50-20:35):
  - AB 列 `英/数 堀上 / 河野` 備考 `隔週(河野)`
  - C 列 `数/英 河野 / 堀上` 備考 `隔週(堀上)`

  A 週は 堀上→AB・河野→C、B 週は 河野→AB・堀上→C と交互になるので、
  どちらの週も同じ人が 2 か所に居ることにはならない。
  - 原因: 衝突検出が「そのコマに関わる講師」を **週を問わない名前の
    集合** (講師欄 + 備考のパートナー) として突き合わせていたため、
    担当する週が正反対でも名前が一致すれば重複としていた。
  - 修正: 講師欄の主担当 = **A 週** / 備考 `隔週(◯◯)` のパートナー =
    **B 週** (📊 集計・👁 週間ミニビュー・Excel の「隔週A/隔週B」と同じ
    定義) として持ち、**担当週が交わるときだけ**重複とする。
    **隔週と通常コマの重なりは、通常コマが毎週である以上どちらかの週で
    必ず当たるので従来どおり警告する** (ここが本来の狙い)。
  - ラベルに当たる週を明示するようにした
    (例:「…（隔週 A 週＝講師欄の主担当の週）」)。従来の
    「隔週コマを含む」は、備考が `隔週` だけでパートナーを読み取れず
    週を絞れない場合にのみ出る (この場合は従来どおり保守的に検出する)。
- **校舎間の移動 (本校 ↔ 亀井町) チェックも同じ基準に揃えた**。週ごとに
  「その週に居る人のコマ」だけを並べて隣接を見るので、担当週が違う隔週
  コマの並びは移動として警告しない (両方の週で当たる組み合わせは
  従来どおり 1 件にまとめる)。
- **講師プルダウンの「(重複)」予告も担当週で絞った**。プルダウンが決める
  のは主担当 (= 隔週コマなら A 週) なので、相手コマで B 週にしか居ない
  講師は予告しない。
- 影響: この変更で重複でなくなった組を過去に「承認」していた場合、その
  承認は対象が消えて **stale** になる (問題パネルの「🧹 掃除」で片付く)。

### Fixed (印刷が全面グレーに沈む — サイドバー backdrop の写り込み)

- **`window.print()` 系の印刷 (通常時間割作成 / 講習時間割作成 /
  ダッシュボード / 週間 / 代行確定一覧 / マスタ) で、紙面全体に
  `rgba(0,0,0,.4)` が乗って暗い出力になっていた**。犯人はサイドバーの
  backdrop (`.sidebar-backdrop`)。
  - **なぜ画面では見えないのに紙面に出るか**: backdrop はデスクトップ幅
    では `@media (min-width: 769px)` で消しているが、**印刷時のメディア
    クエリは画面幅ではなく紙面幅で評価される**。A4 縦 (210mm) から
    `@page` の余白 8mm×2 を引くと ≒ 733px で 769px を下回るため、
    デスクトップで印刷しても「モバイル扱い」になり backdrop が復活して
    いた。グローバル `@media print` の非表示リストは `.sidebar` /
    `.sidebar-spacer` / `.hamburger` だけで backdrop を含んでいなかった。
  - **なぜ普段から出るか**: `sidebarOpen` の初期値は `true`、一方 `view` は
    sessionStorage から復元されるので、**印刷したい画面を開いたまま
    リロードすると backdrop が DOM に残ったまま**になる (サイドバーから
    画面遷移した直後だけ `false` になる)。
  - 修正: backdrop に `no-print` を付け、グローバル `@media print` の
    非表示リストにも `.sidebar-backdrop` を追加した。
- ついでに**トースト (`ToastContainer`) が紙面右下に写り込む**のも塞いだ
  (削除直後の「元に戻す」など、印刷と同時に出ていることは普通にある)。
- 回帰ガード: e2e `print.spec.js` に「紙面幅 (733px) でメディアクエリを
  評価させたうえで、紙面を覆う固定要素が残っていないこと」の検証を追加
  (従来はデスクトップ幅のまま `emulateMedia('print')` していたため、この
  種の回帰を構造上取りこぼしていた)。

### Fixed (印刷まわりの総点検)

上の backdrop 不具合と同じ根 (画面用の CSS が紙面に漏れる) を潰した。

- **レスポンシブ規則をすべて `@media screen and (...)` に限定した**。
  印刷のメディアクエリは紙面幅で評価されるため、従来は
  **用紙サイズを変えると紙面レイアウトが黙って変わる**状態だった
  (A4 縦 ≒ 718〜733px はモバイル扱い / A3 縦 ≒ 1062px はデスクトップ
  扱い)。紙面の見た目は `@media print` だけで決まるようになった。
  - あわせて、モバイル規則に頼って偶然 1 カラムになっていた
    `.excel-grid-sections` (中学/高校) の縦積みを `@media print` に明示。
    popup 印刷 (`printStyles.js`) と同じ紙面になる。
- **通常時間割作成: 紙面でセルの文字が「…」で切れていた**。画面は行高を
  揃えるため科目・講師・備考を `truncate` しているが、紙はクリックして
  全文を読めない (例: 「高松西 文系数学」→「高松西 文…」)。紙面では
  省略を解除して折り返すようにした。
- **開いたまま印刷すると紙面に写り込む固定要素**に `no-print` を追加:
  Cmd+K パレット (`rgba(0,0,0,.5)` の全面オーバレイ — backdrop と同じ
  失敗)、右クリックメニュー (共通 / 通常時間割作成)、代行候補ポップオーバー。
- e2e: ダッシュボードの時間割グリッドを見るテストが**祝日に落ちていた**
  (実行日が休講日だとグリッドが描画されない)。時計を固定日に固定して
  実行日非依存にした。

### Changed (紙面の可読性: 科目カラーの統一・白黒印刷・用紙・文字サイズ)

- **科目カラーを「教科」単位に統一した** (`regular-builder/subjectColor.js`)。
  従来は `getSubjectColor` が**文字列そのもの**を見ていたため、
  「理科A」と「理科B」、「共テ数IA」と「高松西 文系数学」のように同じ教科
  でもクラス記号や冠がひとつ違うだけで別色 (ハッシュ fallback) になり、
  紙面で「色 = 教科」として読めなかった。教科マスタで正規化してから色を
  引く (正規化はアプリ共通の `pickSubjectId` = 完全一致 → 名前を含む →
  別名を含む。代行の教科判定・講師の担当教科推定と同じ規則)。マスタに
  当たらない科目 (確認テスト等) は従来どおり文字列ごとに安定した色。
  画面・ブラウザ印刷・Excel の 3 経路すべて同じ色になる。
- **🖨 白黒 (インク節約) を追加** (通常時間割作成のツールバー、状態は保存)。
  紙面から背景の塗りを全部落として罫線と文字だけで刷る。下書きの目視
  チェック用。**画面の色は変えない** — 紙面だけの切替なので、配布用に
  色付きで刷るときは OFF に戻す。
  - 塗りを落とすと升目が消えるため、白黒では罫線を自前で引く。この画面の
    升目は実は罫線ではなく**セルの塗り分け**で見せていた (Tailwind の
    preflight を読み込んでいないため `border-*` ユーティリティに
    `border-style` が付かず、幅だけ指定されて何も描かれない)。
- **Excel「全曜日」シートを A3 横にした** (曜日別シートは B4 横のまま)。
  全曜日シートは手動改ページを生かすため fitToPage が使えず固定倍率で
  収めており、紙が大きいほど字が大きく刷れる — 実測で **B4 横 77% →
  A3 横 90%**。曜日別は fitToPage で Excel が実測して収めるので B4 で十分。
- **ブラウザ印刷のセル文字を 1px 大きくした** (科目 13→14 / 講師 12→13 /
  教室・備考 10→11px ≒ 7.5pt→8.3pt)。字を大きくすると行の折り返しが減り、
  実測では紙が増えるどころか**週表示 13 → 12 ページに減った**。
  コンパクト表示は「詰めて見る」のが目的なので据え置き。
  - あわせて、科目名と ⚠バッジの行は紙面で flex を解除。狭い列 (7 列以上の
    セクション) でバッジが横幅を取り、科目名が 1 文字ずつ縦に折り返って
    いたのを直した。

### 通常時間割作成: 整合性の穴の修正・編集/検証の道具立て・Excel の刷新

**整合性の穴 (バグ)**
- **時限・クラス列の削除でコマが残骸化し、id 再利用で復活していた**:
  削除は定義から外すだけでセルを残していたため、次に追加した時限・
  クラスが同じ id (`nextNumericId` = max+1) を受け取ると「消したはずの
  コマ」が新しい行・列に現れていた。削除でセルまで落とし (件数を確認
  ダイアログで提示 — CLAUDE.md の cascade あり = confirmedRemove ルール
  準拠)、採番も schedule に現れる id を含めて行う
  (`removePeriodFromProject` / `removeClassFromTab` / `nextClassId` /
  `nextPeriodId`)。
- **未承認の重複・NG が残ったまま反映できていた**: 反映ダイアログの
  警告に件数と内訳を出す (反映自体はブロックしない — 意図した重なりも
  あるため)。
- **隔週パートナーが重複・NG チェックの対象外だった**: 📊 集計・👁 強調・
  週間ミニビューは note の「隔週(◯◯)」を本人の担当として扱うのに、
  チェックだけ見ていなかった。定義を揃え、隔週が絡む重なりはラベルに
  注記して承認で消せるようにした (ビルダーには週 A/B の基準日が無いため)。
- **プロジェクト切替が履歴に載っておらず、「切替 → Ctrl+Z」で前の
  プロジェクトへ黙って引き戻されていた** (見ていない側の編集が取り消され
  ていた)。切替も 1 つの取り消し単位にし、toast にも出す。
- **対象の消えた承認が保存に残り続けていた**: 承認したコマを動かすと
  承認キーは無効になるが、画面に出ないまま残り、同じ配置に戻すと承認が
  生き返っていた。問題パネルに件数と「🧹 掃除」を追加。

**反映**
- **置き換え反映がコマ id を引き継ぐようになった**: 従来は対象時間割を
  全削除して新 id で作り直していたため、代行 (sub.slotId) / 時間割調整 /
  回数補正 / 授業セット (classSet.slotIds) の紐付けが、内容の変わって
  いないコマの分まで切れていた。突き合わせを `matchReflection` に切り
  出して差分プレビューと共用し、位置 (曜日 × 時刻 × 学年 × クラス) の
  一致したコマは slot.id を保って中身だけ更新する。参照が切れるのは
  実際に消えるコマだけ。確認・結果表示もその実態に合わせた。
- 新規作成に**対象学年を下書きの学年に限定する**選択肢を追加
  (既定は従来どおり全学年)。

**編集**
- **⧉ 曜日コピー** (`dayCopy.js`): 曜日のコマをまるごと別の曜日へ複製
  (上書き / 空きマスだけ、コピー先の曜日を使っていない学年に曜日ごと
  足すオプション)。実行前に書き込み・上書き件数を dry-run で表示。
- **✎ 一括変更** (`bulkEdit.js`): 選択セルの教科・講師・教室・備考のうち
  チェックした項目を同じ値に揃える (空欄でクリア)。期切替で頻出の
  「この曜日のこの講師を差し替える」がセル単位でしかできなかった穴を塞ぐ。
- **📋 選択範囲へ貼り付け**: Ctrl+C したコマを選択中のセルすべてへ配る。
- **全時限の一括時刻シフト** (`model.shiftPeriodTimes`): 「全体を 15 分
  後ろへ」を 1 操作で。時限 id は変えないのでコマの中身は動かない。

**検証・集計**
- **校舎間 (本校 ↔ 亀井町) の移動**: 同じ講師の隣り合うコマで校舎が違い、
  間隔が足りなければ警告する (問題の種類 `travel`)。必要分数は施設ごとの
  事情なので ⚙ 全体設定に入れたときだけチェックする (既定 = しない)。
- **⏱ 拘束時間・空きコマ**: 📊 集計の下段を稼働 ⇄ 拘束 (空き) で切替。
  拘束は実際に居る時間なので隔週の 0.5 重み付けはしない。
- **👁 教室の週間ミニビュー**: 教室を選ぶと週のいつ塞がっているか
  (= どこが空いているか) を一覧。教室重複には ⚠️ を付ける。
- **🏫 教室マスタ**: 教室の重複チェックも亀井町判定も文字列一致なので、
  表記ゆれ (「501」と「５０１」) があると黙ってすり抜けていた。マスタと
  「マスタに無い教室（使用中）」の一覧でゆれに気付けるようにした。

**Excel**
- 曜日別シートを **B4 横・1 ページ丸ごと収め** (fitToPage 1×1) に変更。
- 曜日別の後ろに **「全曜日」シート**を追加。全曜日を 1 枚に積み、曜日の
  切れ目に手動改ページを入れて「1 曜日 = 1 ページ」で刷れる。Excel は
  fitToPage を有効にすると手動改ページを無視するため、このシートだけは
  見積り倍率 (`estimatePrintScale`) を入れる。
- 集計 Excel に**クラス別科目シート**を追加 (📊 集計パネル下段の紙面版)。

**設定・その他**
- NG (不在) の入力: 曜日を複数選択でき、時間帯は時限プールから選ぶ形に。
- 科目マスタ・教室マスタの並べ替え (◂ ▸)。
- **他のプロジェクトと比較**: 📌 パネルの差分ビューを一般化し、「1学期と
  2学期で何が違うか」をセル単位で見られるようにした。
- **保存サイズの表示** (`storageSize.js`): スナップショットを溜めて quota に
  当たる前に気付けるように、📌 パネルに概算サイズを出す。

**保守**
- RegularBuilderApp.jsx から Undo/Redo・複数選択・プロジェクトの出し入れを
  フック (`hooks/useRegularHistory` / `useCellSelection` /
  `useRegularProjects`) へ分離 (挙動の変更なし)。
- テスト +116 (regular-builder 計 390)。

### 校正 (通常時間割作成ブランチの総点検)
- **新しい任意フィールドがスナップショット復元で消えなかった**:
  復元は spread のため、保存時に無かったフィールドは現在の値が残る。
  approvedConflicts だけ個別に消していたので、後から足した
  campusTravelMinutes が生き返っていた。任意フィールドを一覧化して
  まとめて処理する (増やしたらここに足す)。
- **教室マスタ・校舎間の移動時間の変更が Undo の toast で「変更なし」に
  なっていた**: diffProject が新しいフィールドを見ておらず、何が戻ったか
  分からなかった (プロジェクト切替で直したのと同じ失敗)。
- **校舎移動だけが問題のセルに「⚠️重複」バッジが出ていた**: 問題の種類と
  食い違うため、バッジ文言を種類から決める `badgeByRef` に一般化した
  (NG のみ → NG、校舎移動のみ → 移動、複数種類 → 重複)。
- **Excel の完了通知が、まとめシートを作っていないときも「全曜日まとめ」と
  言っていた** (1 曜日のプロジェクト)。`downloadRegularExcel` が内訳を返し、
  文面を実態に合わせる。
- **E2E が 2 件壊れていた** (NG の曜日入力がトグル群になった / 👁 セレクトの
  title 変更)。セレクタを追随させ、title 依存だった箇所には aria-label を
  付けて文言変更で壊れないようにした。教室の週間ミニビューの検証も追加。
- **E2E「👁 …の週間（計 2 コマ）」は本ブランチ以前から壊れていた**
  (稼働時間の併記を足した際に未更新)。前方一致に直した。
- 見出し・コメントの追随: 問題パネルの見出しに校舎間の移動、⚙ 全体設定の
  見出しコメントに教室、model.js のスキーマ説明に rooms /
  campusTravelMinutes / approvedConflicts / snapshots、取込が教室マスタを
  自動生成しない理由、プロジェクト間比較は id 基準なので系譜のつながった
  プロジェクト同士で使う旨の注記。

### 通常時間割作成: 教室の曜日別既定 (roomByDay)
- **クラス列の既定教室を曜日ごとに持てるようにした**: 「基本は亀21、
  木曜だけ亀63」のような曜日で違う教室を、コマごとの上書きでなく列の
  設定として表せる。実効教室の解決は**セル上書き → 曜日別既定
  (roomByDay) → 基本既定 (room)** の順 (`model.classRoomForDay` /
  `effectiveRoom`)。重複チェック・反映・講師週間・Excel 出力・建物判定
  (splitCampus)・合同の教室正規化も同じ解決順に統一。
- **列見出しの教室クリックは「その曜日だけ」の変更に**: 従来は全曜日の
  既定が変わり、「木曜の教室を直したら火曜までズレた」が起きていた
  (`setClassRoomForDay`)。上書きの無いコマは連動し、新実効既定と同じ
  上書きは既定追従へ正規化 (setClassRoom と同じ思想)。基本と同じ値・
  空欄にすると曜日別指定を解除。toast の **「全曜日に適用」** ボタンで
  基本の既定への昇格 (従来の全曜日変更) もワンクリックでできる。基本が
  未設定の列は従来どおり全曜日の既定として設定。
- **見出しの教室表示はその曜日の実効既定**: 曜日別が効いている列は
  「亀63＊」のように ＊ 印 + tooltip (基本の教室も表示) で分かる。
  セルの教室プレースホルダ・教室強調 (👁) も曜日別を反映。
- **クラス設定に「曜日別」の教室入力列を追加**: タブ設定のクラス行に
  曜日ごとの入力欄が並び、列 × 曜日の教室が一覧で点検・修正できる
  (空欄 = 基本どおり)。全曜日の既定変更 (基本欄) はここから。
- 基本の既定変更 (`setClassRoom`) は新既定と同じになった曜日別指定を
  冗長として自動で外す。sanitize は既知の曜日 × 空でない値のみ保持
  (スキーマ追加のみで既存データはそのまま有効)。

### 校正 (レイアウト調整ブランチの総点検)
- **◫ 曜日を並べる: 古いジャンプの残骸で初回トグルが弾かれる問題を修正**:
  通常の曜日表示で重複一覧・Undo toast の「表示」を押した後に ◫ を on に
  すると、未消化のジャンプ先の曜日で「並べていない曜日」判定が走り、
  トグルが 1 回目だけ即座に解除されていた。ジャンプの nonce は表示モード
  に関係なく消化する。
- **◫ 曜日を並べる: 「▤ 亀井町を分ける」で改名される手動グループの行対応
  を修正**: splitCampus は手動グループ名を「◯◯（亀井町）」に改名するため、
  縦積みの曜日非依存ソートがセクション名から元グループを引けず末尾送りに
  なっていた。メンバータブの id → 元タブの group 名で対応付け、本校 →
  亀井町のタイブレークも手動グループに効くようにした (メンバーの campus
  注釈から判定)。
- **右クリックメニューを Escape で閉じると複数選択まで消える問題を修正**:
  メニューと選択解除の両方が window の keydown リスナーで、
  stopPropagation は同一ターゲットの他リスナーには効かない。メニューが
  開いている間は選択解除しないようにした。
- **特別時程: 日付変更で前の曜日の行編集が持ち越される問題を修正**:
  プリセット適用後に日付を選び直すと前の曜日の時間帯が行に残り、新しい
  日付に存在しない時間帯の読み替え・休講を保存できてしまっていた。日付
  (曜日) 変更時に新しい日付のコマに無い行を落とす (編集読込で残す既存
  エントリの残存時間帯はこれまで通り)。
- **バイト調査票 Excel: シート名の重複で出力全体が失敗する問題を修正**:
  同姓同名・記号除去後の一致・先頭 31 文字の一致でシート名が重複すると
  exceljs の addWorksheet が例外を投げ、全員分の生成が失敗していた。
  「~2」「~3」… を付けて一意化する。
- **通常時間割作成: 終日 NG が時刻未設定の時限で検出されない不整合を修正**:
  講師プルダウンの「(NG)」予告は時刻未設定の時限にも出る (終日 NG は時刻
  に依存しない) のに、割当後の問題一覧には載らず承認フローにも入らな
  かった。computeConflicts の NG 判定を予告側と同じ判定に揃えた (時刻範囲
  付き NG は従来どおり時刻が判定可能な時限のみ)。

### 通常時間割作成: ◫ 曜日を並べる (複数曜日の同時表示・編集)
- **◫ 曜日を並べる**: 選んだ複数の曜日 (例: 火と木) の表を横に並べて
  一画面で同時に表示・編集できる表示モードを追加 (📅 週表示の隣の
  トグル。週表示とは排他)。モード中は曜日チップが複数選択トグルになり、
  クリックで並べる曜日を足し外しできる (最後の 1 曜日は外せない)。
  各曜日は通常の曜日ビューと同じ RegularGrid の**フル表示 (全学年・全
  クラス)** — 「火木セットのクラスを編集しつつ、同じ曜日の他学年・他
  コースとの講師・教室の兼ね合いも見ながら調整する」が画面切替なしで
  できる (当初はセットのクラス列に絞る「セット編集」として実装したが、
  同曜日の他の授業を見ながら組む必要があるため絞らない形に転換)。
  各曜日の中はセクションを縦 1 列に積み、中学部 → 高校部の順に揃える
  (RegularGrid の stackSections。通常の曜日ビューは従来どおり 2 カラム
  流し込み・タブ定義順)。曜日カラムはコンテンツ幅 (CSS グリッドの
  max-content 列) で詰めて並ぶ — 左の曜日の表が終わったすぐ隣から次の
  曜日が始まり、画面を半分ずつの等幅では分けない。セクションの行も
  全曜日で共有し (各曜日のグリッドは display:contents で親グリッドへ
  直接配置)、左右の曜日で同じ行のセクションは縦の始まりが揃う。同じ部の
  中の並びは曜日に依存しない基準 (タブ定義順 → 本校 → 亀井町。手動
  グループはその曜日に居ないタブも含めた全体の定義順) に揃え、「月曜は
  亀井町が先・木曜は本校が先」のような行の食い違いも防ぐ。合計幅が
  画面を超える分・セル編集でプルダウン分だけ表が広がった分はページ側の
  横スクロールで見る。クラス列の最小幅の下限も通常ビューより
  詰める (125→90px、コンパクト 80→56px。内容が長いセルは従来どおり
  自動で広がる)。セル編集・右
  クリックメニュー・複数選択・一括操作・ロック・合同の結合表示・
  重複/NG の赤枠・予告は曜日ビューと完全に共通。
- **セットチップ (曜日の組のショートカット)**: 「中3（火・木）」
  「中3（水・金）」「中1 のセット1/セット2」のような、同じクラスが
  決まった曜日の組で通う**コースセット**をスケジュールから自動検出し
  (`courseSets.computeCourseSets`。設定・保存は不要でコマを動かせば
  追従)、チップのクリックでその組の曜日の同時表示へ一発で切り替え
  られる。クラス列の「セルがある曜日の組」が同じクラス同士を 1 セットに
  束ね、合同列 (範囲・列挙ラベル) のセルの曜日は構成クラスの曜日にも
  数える。セルの無いクラス列は学年の全曜日扱い。表示中の曜日と一致する
  セットは押下状態になる。
- **グリッド横断 D&D**: 別の RegularGrid へのドラッグ入替・Ctrl+ドラッグ
  コピーができるようになった (dataTransfer のカスタム型
  `text/x-regb-cell-<projectId>` で受け渡し。型にプロジェクト id を含め、
  別プロジェクトを開いた別ウィンドウからの誤ドロップは弾く)。曜日並列の
  「火のコマを木へ移す / コピーする」に加え、📅 週表示でも曜日をまたぐ
  入替ができる。
- 重複一覧・Undo toast の「→ 表示」ジャンプは、並べている曜日内なら
  その場で点滅表示し、並べていない曜日を指す場合は通常の曜日表示へ
  戻してから表示する。🖨 印刷は並べている曜日を曜日ごとに改ページして
  刷る (改ページはフレックスアイテムに効かないため紙面はコンテナごと
  縦積みに戻す)。並べる曜日はリロード後も復元 (sessionStorage)。

### 通常時間割作成: 取込の教室列割当の修正 + 列見出しの教室編集 (連動)
- **取込 (⬇ 本体から取込) の列割当を教室基準に修正**: クラス名の無い
  コマ (高校の講座など) を「同ラベルで最初に空いている列」へ先着順で
  振り分けていたため、404 列に 407 のコマが入る・既定教室 405 の列が
  2 本できる、といった列見出しと中身の食い違いが起きていた。教室を列の
  同一性として教室ごとに列を作り、講座タブの列は教室番号順に整列する
  (`importTimetable.buildProjectFromSlots`)。クラス名のある列は従来
  どおりラベル単位 (教室変更はセル上書きのまま)。同時刻・同室の並列コマ
  は同室の列を増やして保持する。
- **列見出しの教室をクリックで変更 (列のコマと連動)**: 曜日ビューの
  クラス列見出しの教室が display-first の編集になった (クリックで入力、
  Enter/フォーカス外で確定、Escape で取消)。確定すると列の既定教室が
  変わり (`model.setClassRoom`)、教室上書きの無いコマは実効教室が自動で
  追従、新既定と同じ上書きは既定追従へ正規化される。別教室の個別指定と
  ロック中のセルは保持し、toast で変更内容と件数を通知する (Ctrl+Z で
  戻せる独立 Undo 単位)。

### 校正 (通常時間割作成ブランチの総点検)
- **👁 強調表示の講師候補を「マスタ + セルに現れる講師 + 隔週パートナー」
  に**: コメントは元からそう謳っていたが実装はマスタのみだった。マスタ
  未整備の取込直後の講師や、note にしか現れない隔週パートナーも強調・
  週間ミニビューの対象にできる。
- **週間ミニビュー・講師別週間印刷の「計 N コマ」を重み付きに統一**:
  隔週コマを 1 と数えて 📊 集計 (0.5 重み) と食い違っていた。
  `computeTeacherWeek` に weightedTotal を追加し formatCount で表示。
- 細部: 隔週A/B バッジの title 文言を明確化 (「A 週の担当 (講師欄の
  主担当)」/「B 週の担当 (note の「隔週(◯◯)」側)」)、JSON 書き出し
  toast が無名プロジェクトで「」になるのを「無題」に、CLAUDE.md の
  実装済み日付を 2026-08-06 に更新、コメントの ASCII "---" を "—" に。

### Added (通常時間割作成: Excel 出力 (RB22)・はじめかたガイド (RB23)・隔週コマの可視化)
- **📥 Excel 出力 (RB22)**: 曜日チップ横の「📥 Excel」で、全曜日を
  曜日 1 つ = 1 シート (A4 横・横 1 ページ収め) の Excel に書き出せる
  (`regular-builder/excelExport.js`、exceljs はボタン押下時に dynamic
  import)。紙面は画面の曜日ビューの再現: セクションごとに
  時間 × 学年・クラスの表を縦に並べ、セルは 教科 / 講師 / 教室・備考 の
  3 行 + 科目カラー背景。学年が使わない時限はグレー塞ぎ。v1 の割り切り
  として「🏫 亀井町を分ける」は画面のトグルに従い、空行・空列非表示は
  反映せず、合同列は結合せず独立列で出力する。
- **はじめかたガイド (RB23)**: 空状態の静的ガイドを、最初のコマが入る
  まで表示される状態連動チェックリストに置き換え (`RegularOnboarding`)。
  時限 → 講師 → 学年 → セル入力 の 4 ステップを実データから ✅ 判定し、
  各ステップのボタンで該当 UI (全体設定の該当タブ・学年追加・本体から
  取込) へ直接飛べる。全体設定モーダルに initialTab prop を追加。
- **隔週コマの可視化**: 本体の隔週モデル (note の「隔週(パートナー)」+
  アンカー) は取込・反映で既にそのまま往復するため配線は不要と確認。
  builder 側の見え方を本体に揃えた — ① セルの講師欄を「主担当 /
  パートナー」表示に (`formatBiweeklyTeacher`)、② 📊 集計は隔週コマを
  本体の slotWeight と同じ 0.5 で数え、パートナーにも 0.5 を計上
  (端数は formatCount 表示)、③ 👁 強調表示・週間ミニビュー・講師別週間
  印刷はパートナー側にも載る/光る (エントリに 隔週A/隔週B 印)。
  講師・教室の重複チェックは従来どおり主担当のみ (A/B 週で交代するため
  常時重複とは限らない)。`biweeklyPartner` を utils/biweekly に追加。

### Fixed (低い画面でサイドバーの講師一覧が潰れて選べない)
- デスクトップ幅でもビューポートが低い (~860px 以下) と、サイドバーの
  「メニュー固定・講師のみスクロール」分割で講師一覧の flex 高さが
  数 px まで潰れ、講師がログインフォームの裏に隠れて選べなかった。
  モバイル幅で既に入れていた「サイドバー全体を 1 つのスクロール領域に
  統合する」対処を `@media (max-height: 860px)` にも適用。
  (print.spec の講師クリックがこれで安定して通るようになった)

### Changed (通常時間割作成: ⚙ 全体設定のモーダル + タブ化 — RB19)
- ⚙ 全体設定をインラインパネルから、講習ビルダーの「⚙️ 設定メニュー」と
  同じ作法のタブ付きモーダルに変更 (`ProjectConfigModal`、旧
  ProjectConfigPanel)。🕐 時限 / 📚 科目 / 👤 講師 / 🚫 NG・上限 の
  4 タブで、縦に長くなっていた設定を分類。focus trap (Escape で閉じる・
  Tab 閉じ込め)・tablist の ←→/Home/End ローミングナビ付き。編集は
  従来どおり即時保存 (Ctrl+Z で戻せる) で「OK で確定」型ではない。
- 併せて 2 つのフォーカス事故を修正: ① モーダル内で設定を保存するたびに
  App 再レンダーで onClose (inline 関数) が変わり、useFocusTrap が再初期化
  されてフォーカスが ✕ ボタンへ飛ぶ (onClose を useCallback で安定化)。
  ② 講師リネームの Enter 確定で、①のフォーカス移動と Enter の既定動作が
  重なると ✕ ボタンが押されてモーダルごと閉じる (preventDefault を追加)。
- E2E: 講師リネーム・NG/上限登録の操作をモーダル + タブ経由に更新。

### Added (通常時間割作成: JSON 書き出し/読み込み・セルのロック・グリッドの軽量化)
- **JSON 書き出し / 読み込み (RB20)**: ツールバーの 💾 でアクティブな
  プロジェクトを JSON ファイルに書き出し (スナップショット込み・id 除外)、
  📂 でファイルを新しいプロジェクトとして読み込める (既存は上書きしない)。
  下書きが LocalStorage + 同期にしか無いことへのバックアップ・別環境への
  移行手段。封筒形式 `{kind, version, exportedAt, project}` で、読み込みは
  封筒付き・生プロジェクトの両方を受けて `sanitizeProject` で整えてから
  追加する (`projectJson.js`、テスト付き)。壊れた JSON・別物の JSON は
  toast でエラーを知らせる。
- **セルのロック (固定)**: 右クリックメニューの「🔒 ロック」(時限行・
  クラス列は一括、複数選択バーにも 🔒/🔓) で、確定済みコマの編集・
  クリア・D&D 入替・貼り付けを防げる (`cell.locked`)。ロック中のセルは
  🔒 マーク付きで、クリックしても編集モードに入らず、ドラッグで掴めず、
  ドロップ先にもならない (モデル側の swap/copy も no-op の二重ガード)。
  一括クリアはロック中のコマを対象外にして件数を知らせる。コピーは
  ロックを引き継がない (複製は編集できる状態で置く)。ロックの付け外しは
  Ctrl+Z で戻せて、undo toast・スナップショット差分にも 🔒 付きで出る。
  紙面にはロック印を載せない。
- **グリッドの軽量化**: RegularGrid のセクション分割・結合レイアウト・
  重複/NG 予告索引の計算を useMemo 化。ドラッグ中や編集セル切替などの
  表示だけの再レンダーで重い再計算を繰り返さない (週表示で効く)。

### Added (通常時間割作成: クラス×科目集計・反映時の自動スナップショット・講師削除の確認)
- **📊 集計にクラス × 科目の週コマ数**: 集計パネル下段に「📚 クラス別
  科目コマ数（週計）」を追加 (`classLoad.computeClassSubjectLoad`)。学年
  (タブ) ごとに クラス × 科目 の週コマ数と計行を表にし、「中3 の数学が
  週 3 コマあるか」というカリキュラム側の検算がツール内で完結する。
  教科の入ったセルだけが対象 (反映と同基準)。科目列はマスタ順 → マスタ外は
  名前順、合同 (結合) コマは合同クラスの行に数える。コマゼロのクラス行は
  減光して残す。
- **反映成功時の自動スナップショット**: 「⤴ 本体へ反映」が通ったとき、
  その時点の案を「⤴ 反映: 時間割名」として自動保存する (どの状態を本体に
  出したかへ後から必ず戻れる安全網)。他のスナップショット操作と同じく
  Ctrl+Z で取り消せる。成功 toast にも保存した旨を添える。
- **講師削除の確認**: 講師マスタの ✕ で、割当セルのある講師・NG (不在) や
  コマ数上限を設定した講師だけ、失うもの (割当は残る・設定は消える) を
  件数付きで知らせる確認を挟む (`model.countTeacherAssignments`)。
  割当も設定も無い講師は従来どおり即削除 (どちらも Ctrl+Z で戻せる)。

### Added (通常時間割作成: クラス重複チェック・NG の週間表示・教室の入力補完)
- **クラス (生徒) の時間重複チェック**: 講師・教室に加えて、同じ学年の
  同じクラス列に時間帯の重なるコマが 2 つある状態を「問題」として検出する
  (`conflicts.js` の type "class")。時限時刻のタイポ等で生徒が二重在籍に
  なっても従来は無警告だった。意図した分割授業などは他の重複と同じく
  承認フローで消せる。問題パネルの見出しは「講師・教室・クラスの重複と
  講師NG」に更新。
- **週間ミニビューに NG (不在) を表示**: 👁 で講師を選んだときの週間
  ミニビューに、その講師の NG 時間帯を曜日ごとの先頭に 🚫 で出す
  (`computeTeacherWeek` の ngByDay。終日 → 時刻順)。従来は割り当てた後の
  警告しかなかった NG が、割り当てる前に週間で見える。講師別週間印刷にも
  「🚫 NG: …」行を載せ、NG だけで担当コマゼロの講師もミニビューを出す。
  使わない曜日の NG は載せない。
- **教室入力の候補補完**: セルの教室入力とタブ設定のクラス既定教室に、
  プロジェクト内の教室 (クラス既定 + セル上書き) を候補に出す datalist
  (`regb-rooms`) を配線。教室重複チェックと亀井町判定 (「亀」前方一致) は
  文字列一致なので、表記ゆれ (全角/半角など) の混入を入力時に防ぐ。

### Added (バイト出勤可能時間調査の Excel 出力)
- **出勤可能時間調査 Excel** (`utils/staffSurveyExport.js`): バイト管理 →
  バイト一覧タブに「📥 出勤可能時間調査 (Excel)」ボタンを追加。バイト
  1 人 = 1 シート (A4 縦・1 ページ収まり) で、上段に現在の時間割
  (時間割スイッチャで選択中のもの) の担当コマを曜日 × 時間帯グリッドの
  網掛けで表示し、下段に「出勤できる時間帯」を曜日ごとに手書きできる
  黄色の記入欄 + 備考欄を置く。2学期に向けて「どの時間帯に入れるか」を
  紙で調査する用途。
- 担当コマの判定は一覧の担当コマ数と同じ `isSlotForTeacher`
  (直接担当 + "·" 区切りの複数講師 + note の隔週パートナー)。隔週ペアは
  同じ曜日 × 時間帯のセルに両方並べて注記ごと見せる。
- exceljs はボタン押下時に dynamic import してメインバンドルから外す
  (講習ビルダーの Excel 出力と同じ方式)。ビルダー非依存の純関数
  (`collectStaffGrid` / `buildStaffSurveyWorkbook`) は
  `staffSurveyExport.test.js` でテスト。

### Added (特別時程: 附属の 50 分授業切替・1 限カット)
- **特別時程 (DaySchedule, schema v16)**: 学校行事の都合で特定日だけ時程が
  変わるコース (主に附属) を「日付 × 対象学年 × 時刻読み替え表 +
  部分休講」で登録できる新リソース (`appData/genyakubu-day-schedules`、
  `utils/daySchedules.js`)。slot.id を参照しない (時間帯文字列で照合する)
  ため、期切替やコマ入替に強い。
- **登録 UI**: 「休講・テスト期間・イベント」画面の ⏰ 特別時程
  マネージャ。日付を選ぶとその曜日の対象学年の時間帯一覧が出て、
  プリセット「① 50分授業 (17:00開始)」(先頭 4 コマを 17:00-17:50 /
  18:00-18:50 / 19:00-19:50 / 20:00-20:50 へ圧縮、テストは据え置き) と
  「② 1限カット」(最初の時間帯を休講) で読み替え表を自動生成、保存前に
  自由に編集できる。時刻はコードに固定していないので後期の時程でも
  そのまま使える。削除は cascade 無しの removeWithUndo。
- **衝突プレビュー** (時間割を組むときの考慮用): 登録フォームで、読み替え
  後に「新たに生じる」講師・教室の重なりを警告表示する
  (`daySchedules.findNewConflicts`)。元の時程でも重なっていた組
  (並列監督・合同) は報告しない。警告のみで登録は妨げない。
- **可視化**: ダッシュボード日別 (日ヘッダに ⏰ チップ + コマは読み替え
  後の時間グループへ移動し「移」バッジ)・時間割モード (⏰ バナー +
  行の再配置)・講師の月間 (時刻読み替え + ツールチップ)・週間
  (直近 2 週間の特別時程バナー)・イベントカレンダー (常時表示バー +
  クリックで編集ジャンプ、＋特別時程ボタン) に反映。読み替えは既存の
  「コマ移動」表示網 (`buildAdjustmentIndex`) に合流させ、個別の
  コマ移動調整が同じコマにある場合はそちらが優先。
- **回数カウント連動**: 1 限カット等で休講にしたコマはその日
  「実施なし」扱いになり第N回を進めない (`sessionCount` +
  `useSessionCtx` に daySchedules を配線)。時刻読み替えのみの日は
  従来どおりカウントする。隔週の A/B 週送りには関与しない (附属に
  隔週コマが無いため。必要になったら biweekly 側へ配線)。

### Added (通常時間割作成: 複数選択 + 一括クリア・講師の週間印刷)
- **複数選択 + 一括操作** (講習 N2a 相当): Ctrl(⌘)+クリックでセルを
  追加/除外、Shift+クリックで同じ表 (セクション) 内の矩形選択。選択中は
  「☑️ N セル選択中」のバーが出て、🧹 一括クリア (中身のあるコマ数を
  明示した確認 + Undo 1 回で復帰) と ✕ 選択解除 (Esc) ができる。矩形の
  計算はセクション構造を知る RegularGrid 側が行い、アンカーが別セクション・
  別曜日ならトグルに落とす。曜日・プロジェクト・表示モードの切替で選択は
  持ち越さない (見えないセルへの一括操作を防ぐ)。
- **講師の週間印刷** (週間ミニビューの 🖨): 強調表示中の講師の週間時間割
  (曜日ごとの担当コマ表) を A4 縦で印刷できる。全曜日印刷と同じ
  「印刷中だけ print 専用シートを描画」方式で、画面側の DOM は汚さない。
- CLAUDE.md: **自動生成 (RB26) を却下として記録** (使わないとの判断。
  講習ソルバの移植・生成 UI は今後提案しない)。通常時間割作成の
  実装済み一覧・残り候補も最新化。
- E2E に第 5 テスト (Ctrl/Shift 選択 → 一括クリア → Undo → Esc 解除) を
  追加。
- 校正 (第二〜第五波レビュー): NG ラベルの曜日重複表記を解消
  (「火 … NG (火 終日)」→「火 … NG (終日)」)。複数選択の装飾 (緑リング +
  薄緑背景) を紙面に載せないように (印刷スタイルに regb-selected の
  打ち消しを追加)。セルの title ヒントに Ctrl+クリックの複数選択を追記。
  空セルだけを選択して 🧹 クリアしたときは無言 no-op にせず toast で
  理由を伝える (講習 H3 と同じ思想)。スナップショットのヒントに保存容量へ
  の言及を追加。

### Added (通常時間割作成: 📌 スナップショット・Ctrl+C/V/Delete・Ctrl+ドラッグコピー)
- **📌 スナップショット** (CLAUDE.md v2 候補): ツールバーの「📌 案 (N)」で
  プロジェクトの名前付き保存・復元・削除・リネームができる
  (`model.addSnapshot` / `restoreSnapshot`、テスト 3)。「🔍 差分」で
  「保存時 → 現在」を ＋追加 / －削除 / ≠変更 の件数 + 明細
  (最大 20 行、各行に該当セルへの「表示」ジャンプ) で確認できる
  (`historyFeedback.diffProjects` — Undo フィードバックの diff 基盤を流用)。
  保存・復元・削除はすべて Ctrl+Z で取り消せる。snapshot.data は
  snapshots 自身と id を含まないコピーで、sanitize は入れ子・不正要素を
  落とす。保存時に無かった任意フィールド (承認など) は復元後も持たない。
- **Ctrl+C / Ctrl+V / Delete のキーボード配線** (講習は L2d 未実装 —
  regular が先行): 表示セルにフォーカスがある状態で Ctrl+C = コピー
  (toast 付き)、Ctrl+V = 貼り付け、Delete/Backspace = クリア。矢印移動と
  組み合わせてキーボードだけで複製・整理ができる。実体は右クリック
  メニューのコピー/貼り付けと同じ (クリップボードも共有)。
- **Ctrl+ドラッグでコピー配置**: ドラッグ中に Ctrl (または Alt) を
  押しながらドロップすると、入替ではなくコピーになる (カーソルも
  copy に切り替わる)。同じコマを複数曜日・複数クラスへ繰り返し置く
  通常時間割向け (`model.copyCellAcrossTabs`、テスト 2)。
- E2E に第 4 テスト (キーボード操作 → スナップショット保存 → 差分 →
  復元) を追加。

### Added (通常時間割作成: 週間ミニビュー・教室ハイライト・📅 週表示)
- **👁 強調表示を講師 / 教室の 2 系統に**: ツールバーのセレクトを
  optgroup (講師・教室) に拡張。教室を選ぶと、その実効教室 (セル上書き →
  クラス既定) のコマだけが青枠で光り、他は減光する (中身のあるセルのみ —
  空マスまで光らせると教室のクラス列全体が光ってノイズになるため)。
  候補はクラス既定教室 + セル上書き教室から自動収集。選択中の講師・教室が
  消えたらハイライトは自動解除 (従来どおり)。
- **講師の週間ミニビュー**: 講師で強調表示している間、その講師の週間
  (曜日ごとの担当コマを開始時刻順) をパネルで一覧
  (`teacherLoad.computeTeacherWeek` + `RegularTeacherWeek`、テスト 2)。
  ハイライトは表示中の曜日しか光らないため、週全体のやりくりはここで見る。
  各エントリはクリックで該当セルへジャンプ (曜日切替 + 一時ハイライト)。
- **📅 週表示** (トグル・リロード後も保持): 全曜日のグリッドを縦に並べて
  俯瞰する。曜日チップは該当ブロックへのスクロールに変わり、編集・D&D・
  右クリックメニュー・ジャンプは全曜日で機能する。印刷は曜日ごとに
  改ページ (既存の regb-print-day を共用) で、週表示中は「🖨 全曜日」
  ボタンを隠す (画面 DOM がそのまま全曜日のため)。
- E2E に第 3 テストを追加 (強調 2 系統 → ミニビューからのジャンプ →
  週表示の通し確認)。

### Added (通常時間割作成: 講師NG・コマ数上限・📊 集計パネル)
- **講師NG（不在）**: 全体設定の講師欄に「NG（不在）」を追加。講師 ×
  曜日 (+ 任意の時刻範囲、空欄で終日) で登録し、NG の時間帯への割当は
  重複と同じく赤枠 + ⚠️NG バッジ + 一覧で警告される (refs 1 件の
  type "ng"、`computeConflicts`)。意図した割当は既存の承認フローで
  消せる。講師プルダウンには選択前に「(NG)」を予告
  (`computeNgTeachersForTabs` — 終日 NG は時刻未設定の時限にも当たる)。
  セルの右クリックメニューからも承認できる。テスト +9。
- **コマ数上限**: 講師ごとに 1日 / 週の上限を設定できる (空欄 = 無制限)。
  超過しても入力は妨げず、集計パネルで赤字警告する (講習の「上限到達で
  warning」と同じ思想)。sanitize は不正値 (0・負値) を未設定に倒す。
- **📊 集計パネル**: ツールバーの「📊 集計」トグル (リロード後も保持) で
  講師 × 曜日のコマ数と週計を一覧 (`teacherLoad.computeTeacherLoad` +
  `RegularSummaryPanel`)。複数講師セルは各講師に 1 コマずつ、残骸セルは
  数えない。上限超過は「3!」「5/4」の赤字 + tooltip。マスタ外の講師
  (直接入力) も「(マスタ外)」として列挙。テスト +4。
- **文言変更**: NG 違反が加わったため、ツールバーのバッジを
  「⚠ 重複 N 件 / ✓ 重複なし」から「⚠ 問題 N 件 / ✓ 問題なし」に、
  一覧パネルの見出しを「講師・教室の重複と講師NG」に変更。
- E2E に第 2 テストを追加: NG 登録 → 検出 → ジャンプ → 右クリック承認 →
  上限付き集計パネルの通し確認。

### Added (通常時間割作成: 編集ループの高速化 4 点)
- **重複一覧から該当セルへジャンプ**: 重複パネルの各行 (承認済み含む) に
  「→ 表示」。クリックで該当曜日へ切り替え、両セルへスクロールして
  一時ハイライト (点滅) する。対象が折りたたみ中のセクションにあれば
  自動で展開する。
- **Undo/Redo に「何が戻ったか」の toast** (講習 N2f 相当): セルの変更は
  「月 2限 中3 A: 数学/田中 → 空」の形で場所と現在値 → 復元値を要約し、
  toast の「表示」ボタンで該当セルへ飛べる。複数セルは
  「N コマの変更 (曜日)」、セル以外は「時限設定・講師マスタ」等の
  フィールド名で要約する (`regular-builder/historyFeedback.js` 純関数 +
  テスト 9)。表示していない曜日のセルが戻っても気付ける。
- **セルの右クリック / 長押しメニュー**: コピー / 貼り付け (コピー内容の
  プレビュー付き) / この重なりを承認 (そのセルが関わる未承認の重なりを
  まとめて承認) / クリア。時限行・クラス列のヘッダにも一括クリア
  (対象コマ数の確認ダイアログ付き、Undo 1 回で戻る)。講習の ContextMenu と
  同じ操作感 (画面内クランプ・↑↓で項目移動・Escape / 外側クリック /
  スクロールで閉じる) で、useLongPress 共用によりタッチの長押しでも開く。
- **講師のリネーム追従**: 全体設定の講師名をクリックでインライン編集。
  マスタと全タブの割当セル (複数講師 "·" 区切りの 1 名だけでも) が追従し、
  更新セル数を toast で報告する (`model.renameTeacherInProject` 純関数 +
  テスト 5)。既存名との重複・区切り文字 (中黒) 入りの名前は理由付きで
  拒否。ハイライト中の講師がリネーム / 削除で消えたら自動解除。
- あわせて: ブラウザタブのタイトルにプロジェクト名を表示 (講習と同じ、
  アンマウント時は親アプリのタイトルへ復帰)。
- E2E `e2e/regular-builder.smoke.spec.js` を追加: 上記のコンポーネント
  横断の配線 (メニュー → toast → ジャンプ → リネーム → 一括クリア) を
  実ブラウザで通しで検証。
- 校正: Undo の Ctrl+Z を select フォーカス中でも効くようにした (select に
  ブラウザ標準の undo は無く、プルダウンで教科を変えた直後の Ctrl+Z が
  無反応だった — 講習 N1d と同じ修正)。Undo ジャンプの対象セルを曜日順で
  決定的にし、メニュー項目に role="menuitem" を付与、クラス名の無い列の
  一括操作ラベルは列見出しと同じく教室名で呼ぶように。

### Added (画面リロードでビューを維持)
- ブラウザの更新 (リロード) 後も、直前に開いていたビューと選択中の講師を
  復元するようにした (従来は必ずダッシュボードに戻っていた)。保存先は
  sessionStorage なのでタブ単位 — 新しいタブ・ウィンドウで開いたときは
  従来どおりダッシュボードから始まる。講師専用ビュー (週間/月間) は
  講師なしでは復元せずダッシュボードに倒す (空画面防止)。

### Added (通常時間割作成: 本校/亀井町の表示分割と空行・空列の非表示)
- **🏫 亀井町を分ける** (新トグル・既定 ON): 教室が「亀◯◯」のクラス列を
  本校と別のセクションに分けて表示する。時限もそれぞれの建物のセルが
  実際に使うものに絞られ、時刻体系の違う建物同士で空行・空きマスが
  乱立しない。列の建物はセルの実効教室 (セル上書き → クラス既定) の
  多数決で判定 (取込タブの「列既定は本校・実コマは全部亀井町」パターン
  対応)。タブ id・セルキーは元のままの表示専用の変形で、セル編集・
  D&D 入替・重複チェック・反映はそのまま機能する。自動セクション名には
  両建物がある日だけ「（本校）/（亀井町）」を付け、手動グループの混在
  タブは亀井町側だけ「◯◯（亀井町）」グループに分かれる
  (`model.splitTabsByCampus` + `computeSections` の splitCampus、テスト 8)。
- **▤ 空行を隠す → 空行・空列を隠す**: セルが 1 つも無い時限行に加えて
  クラス列も隠すようにした (`mergedColumns.visibleClassesForDay`、テスト 3)。
  中身のある合同列 (範囲・列挙) の構成クラスは colSpan の下敷きとして
  残す。列が全部消えた学年・セクションはその曜日の表から外す。
- 校正: 講師プルダウンの「(重複)」予告 (busyByTab) は分割前の元タブで
  計算する (tab.id キーのため、分割で同じ id が 2 回現れると片方の建物の
  予告が消えていた)。

### Fixed (校正レビュー: リロード復元まわり 3 点)
- ErrorBoundary: 描画クラッシュを捕捉した時点でビュー復元情報
  (sessionStorage) を破棄するようにした。復元先のビュー自体が壊れている
  場合に「リロード → 同じビューを復元 → 再クラッシュ」の無限ループに
  入るのを防ぐ。「保存データを初期化して再読込」も sessionStorage を
  含めて初期化する。
- 通常時間割作成: 選択中の曜日タブもリロード後に復元するようにした
  (使わない曜日を復元した場合は従来どおり先頭の使用曜日に倒れる)。
- `model.classCampus` のコメントが取込仕様を不正確に説明していたのを
  修正 (取込は最頻教室を列既定にするため、列既定と実コマの食い違いは
  取込後の手直しで生じる)。

### Changed (通常時間割作成: 合同コマを Excel 風のセル結合表示に)
- 「S〜B」「SS〜C」(範囲) や **「S/AB」「S/AB/C」(スラッシュ列挙)** の
  合同列を独立した列として表示するのをやめ、**構成クラス (S・A・B…) の
  上に colSpan で結合表示**するようにした (`mergedColumns.js` +
  テスト 14)。データモデルは不変 — 合同列はクラス列のまま、セルのキーも
  従来どおりで、反映の round-trip も不変。列挙は構成クラスがすべて存在し
  表示順で隙間なく隣接する場合のみ結合 (「S/C」の間に A を挟む等は、
  跨いだ colSpan が A も合同に見えるため通常の列のまま)。
- 同一スパンの並列合同 (確認テストの複数監督) はスパンを列数で分割して
  横に並べる (5 列に 2 セルなら 3+2)。結合セルには既定教室も表示
  (列見出しが無くなるため)。
- 「範囲セルと構成クラスの個別セルが同じ行にある」「異なるスパンが
  交差する」データは結合表示できないため、その学年は従来の独立列表示に
  自動フォールバック。
- 新しい合同コマは、範囲の先頭クラスのセルにホバーで出る **⊞** から
  入力できる (空の結合セルが編集モードで現れ、確定すると結合表示になる)。
  ⊞ はスパン内に個別コマが無い行にだけ出す (入力するとクラスの二重在籍に
  なり、確定と同時にフォールバック表示へ切り替わって驚かせるため)。
- 矢印キーの移動は、中身のない結合位置をスキップして通過する。
- 校正: 並列数がスパン幅を超えるデータ (幅 0 セルの無言非表示) も
  フォールバック対象に。学年境界の太罫が並列合同の 2 セル目以降にも
  引かれていたのを先頭セルのみに修正。

### Added (通常時間割作成: 操作性の改良 5 点)
- **学年チップの並べ替え**: ドラッグ (または Ctrl+←→) で並べ替えでき、
  セクション・学年グループの表示順に反映される。独立した Undo 単位。
- **曜日チップに入力済みコマ数**: 埋まりの薄い曜日に一目で気付ける
  (設定変更で無効になった残骸セルは数えない)。
- **🖨 全曜日**: 全曜日をまとめて印刷 (曜日ごとに改ページ、各ページに
  「プロジェクト名 — 曜日・印刷日」の見出し)。印刷中だけ全曜日分の
  print 専用 DOM を描画し、afterprint で片付ける。
- **セクションの折りたたみ**: 見出しバーのクリックで開閉 (▾/▸)。表示のみで
  データ不変、印刷時は畳んでいても展開して刷られる。矢印移動は畳んだ
  セクションを飛ばす。
- **矢印キーのセクション横断**: ←→ の連続移動がセクションの端で
  次/前のセクションへ跨ぐ (行位置は移動先の時間軸に丸める)。↑↓ は
  時間軸が違うため従来どおりセクション内。
- あわせて修正: 同一セル内の 教科 ⇄ 講師 の矢印移動が、React の再レンダー
  スキップによりフォーカスが動かないことがあった (編集中セル内は DOM を
  直接フォーカスするように)。

### Fixed (通常時間割作成: 校正レビュー第 2 回)
- **印刷で背景色を刷らせる** (`print-color-adjust: exact`): ブラウザは既定で
  背景色を落とすため、「色背景 + 白文字」のセクション見出しが紙面で
  白文字だけになり読めなくなっていた (print エミュレーションでは再現
  しない)。学年色・科目カラーも紙面に残るようになった。
- **編集モードにも ✕ クリアを常設**: 表示モードの ✕ はホバー表示のため、
  タッチ端末ではクリア導線が無かった。クリア後は編集を終了して表示セルへ
  フォーカスを戻す (従来はボタンごと消えてフォーカスが宙に浮き、編集
  状態が残った)。
- **「(重複)」予告の一括計算化** (`computeBusyTeachersForTabs`): 学年数ぶん
  全セル解決 (resolveAllEntries) を繰り返していたのを 1 回に。
- 文言統一: 削除確認・設定パネルに残っていた「タブ」を「学年」へ。

### Changed (通常時間割作成: 高校・亀井町の切り分けを強化)
- **自動同居の条件を厳格化**: 「時限を 1 つでも共有したら併合」は、講座
  ごとに時刻がばらつく高校の実データで、たまたま 1 コマ重なるだけの
  無関係な学年 (本校と亀井町、マークテスト特設タブなど) を巻き込んで
  いた。**「片方の時限セットがもう片方に包含される (⊆)」場合のみ**自動で
  同じ表にする (中1 ⊆ 中3 のような同系はこれまでどおりまとまる)。
- **取込時にグループ名を自動設定**: 単一学年のタブに
  中学部 / 附属 / 高校部・本校 / 高校部・亀井町 を自動で付与
  (建物分割された「(亀)」タブは常に亀井町)。「中1-3」「高1高2」の
  ような特設タブは空のまま自動判定に任せる。学年設定でいつでも変更可。
  テスト +5 (グループ自動設定 3 / 包含クラスタリング 2)。

### Changed (通常時間割作成: セクション別テーブルに再編 — ダッシュボードと同構図)
- 「全学年を 1 枚の表に時刻統合」する方式は、実データでは学年ごとの時刻
  体系で行が乱立して一覧性を損なうため廃止。ダッシュボードの時間割
  ビューと同じく、**時間軸を共有する学年のまとまり (セクション) ごとに
  小さなテーブルを作り、2 カラムに流し込む**構図にした。
- **セクション分け** (`model.computeSections` + テスト 5): 学年設定の
  「グループ名」(任意) が最優先。未設定の学年は「使う時限を 1 つでも
  共有する学年」を推移的に自動で同居 (中1・中2・中3 は自動で 1 つの表、
  時刻の違う高校部・附中は別の表)。
- **セクション見出し**は部の配色 (DEPT_COLOR: 中学系 緑 / 高校系 金) +
  コマ数バッジ。各セクションは自分の時限だけを行に持つ。
- **取込オプション「教室の建物 (亀井町) で学年タブを分ける」** (既定オン、
  テスト 5): 教室が「亀◯◯」のコマとそれ以外の両方がある学年を
  「高2」「高2 (亀)」に分け、本校と亀井町が別セクションに分かれる。
  平日/土曜分割と併用時は「高2 (亀・土)」のように連結。
- クラス名が無い列 (取込した高校の講座列など) は教室名を見出しに表示
  (「(クラス名未設定) 401」の羅列を解消)。
- D&D 入替はセクション・学年をまたいで可能なまま。矢印キーの移動は
  セクション内で完結。印刷は縦 1 列に直しセクション単位で改ページ。

### Changed (通常時間割作成: レイアウトをダッシュボードの時間割ビュー型に)
- **学年タブでの切替をやめ、「1 つの曜日 × 全学年」の一覧レイアウトに**。
  通常時間割は学年横断で講師のやりくりを見ながら組むため、ダッシュボード
  の時間割ビューと同じ構図 (行 = 時限を時刻順に統合、列 = 学年グループ ×
  クラス) にした。学年ごとに時刻体系が違っても 1 つの時間軸に並び、
  使わない時限のマスはグレーで塞がれる。
- **曜日チップ** (ダッシュボードと同じ DAY_COLOR/DAY_BG 配色) で表示曜日を
  切替。どの学年も使わない曜日は無効表示。
- **学年グループヘッダは gradeColor** (中1 緑 / 中3 紫 / 高3 藍 など
  ダッシュボードと同じ配色)。学年の境目は太罫で区切る。
- **学年チップ**: 旧タブは設定用チップになり、クリックでその学年の設定
  (名前・学年・曜日・時限・クラス) を開閉。⚠ 重複件数 / 空 / ✨ バッジは
  チップに引き継ぎ。追加時は新しい学年の設定を直接開く (従来は先頭タブの
  設定が開く不具合があった)。
- **D&D 入替は学年をまたいで可能に** (`swapCellsAcrossTabs` 純関数 +
  テスト 5)。矢印キー移動も学年境界を越えて連続する。
- 印刷は「表示中の曜日」を刷る (見出し: プロジェクト名 — 曜日・印刷日)。

### Changed (通常時間割作成: セルをダッシュボード風の display-first に)
- セルは普段**ダッシュボードの時間割ビューと同じテキスト表示** (科目・
  講師・教室/備考が文字で並ぶ) になり、**クリック (Enter/Space) したセル
  だけがその場でプルダウン入力に切り替わる**。セルの外へフォーカスが
  移る / Enter / Escape で表示に戻る (Escape はセルへフォーカス復帰)。
- 表示セルはフォーム部品が無いため**セル全体を掴んでドラッグ入替**できる
  (⠿ ハンドルは廃止)。矢印キーは編集モードのまま隣のセルへ編集を移すので
  キーボードだけで連続入力できる。表示セル上の矢印はフォーカス移動。
- 印刷・コンパクト表示もテキストセルになり、紙面から select の枠が消えた。
- タブ / プロジェクト切替で編集状態は持ち越さない。

### Added (通常時間割作成: 編集補助 5 点 + 印刷)
- **講師プルダウンの重複予告**: その曜日・同時間帯に他のセル (タブ横断) で
  割当済みの講師へ「(重複)」を表示 (`conflicts.computeBusyTeachers`)。
  選択は妨げない (意図した重なりは承認フローで消せるため)。テスト +4。
- **セルの ✕ クリア**: セルにマウスを載せる (またはフォーカス) と右上に
  ✕ が出て、教科・講師・教室・備考を一括クリアできる。Undo で戻せる。
- **D&D 入替・✕ クリアは独立した Undo 単位**: 直前のタイピングと 800ms
  束ねされず、Ctrl+Z 一回でその操作だけが戻る (`commitWorkspace` の
  atomic オプション)。
- **表示トグルの保持**: 「▤ 空行を隠す」「🗜 コンパクト」がリロード後も
  保持される (講習の `usePersistedToggle` を共用。明示トグルの保存であり
  A18 系の自動学習ではない)。
- **教科にも「✎ 直接入力」**: マスタ外の単発教科 (「テスト対策」等) を
  セル上で直接入力できる (講師と同じ Enter 確定 / Escape 取消)。
- **🖨 印刷**: 表示中タブを window.print() で印刷 (CLAUDE.md 印刷二系統の
  window.print() 側、講習の builderPrintStyle と同型の `printStyle.js`)。
  A4 縦・全列を紙面幅に収める・曜日単位で改ページ・thead をページごとに
  繰り返し・空欄の教室/備考は紙面に出さない。印刷見出し (プロジェクト名 —
  タブ名・印刷日) 付き。テスト +8。

### Changed (通常時間割作成: UI を講習ビルダーと同じ操作感に刷新)
- **見た目を講習時間割作成と統一**: Tailwind の builder-* デザイントークンを
  regular-builder にも展開 (tailwind.config の content 拡張 +
  `.builder-root` ラップ)。ツールバー・タブバー・設定パネル・重複パネル・
  空状態ガイドも同じ質感に再スタイル。
- **単一テーブル化**: 曜日ごとに分かれていたテーブルを廃止し、講習の
  ScheduleTable と同じ「sticky クラスヘッダ (濃紺) + sticky 曜日/時限列 +
  曜日区切りの濃色帯 + 70vh スクロールコンテナ」の 1 枚の表に。
- **セル刷新**: 科目カラー背景 (講習と同じ配色・未登録科目はハッシュで
  フォールバック)。教科・講師は常時表示のテキスト 4 入力からプルダウン
  選択に変更 (typo 防止)。講師は「✎ 直接入力」でテキスト入力に切り替え
  でき、複数講師 ("·" 区切り) は確定時に `splitTeacherField` で正規化。
  教室・備考は値があるセルにだけ小さく表示。衝突セルは赤背景 +
  ⚠️重複バッジ (承認済みは除外、従来どおり)。
- **ドラッグ & ドロップでセル入替**: ⠿ ハンドル付き。空セルへは移動になる。
  コンテナ端に近づくと自動スクロール (講習 N2b と同じ)。純関数
  `swapScheduleCells` + テスト 3 件。
- **矢印キーでセル間移動**: ↑↓ で時限・曜日をまたいで移動、←→ は
  教科 ⇄ 講師 ⇄ 隣クラスへ連続移動 (講習 E1b の簡易版)。
- **タブバーを講習型に**: rounded タブ + タブ別バッジ (未承認の重複 ⚠件数 /
  マス目なし「空」/ 問題なし ✨)。ダブルクリックでタブ設定を開く。
- **🗜 コンパクト表示**: セルを縮めて全体を見渡すトグル (講習と同じ)。

### Added (通常時間割作成: 編集体験の強化 5 点)
- **▤ 空行を隠す**: セルが 1 つも無い時限行 (と空の曜日) を表示から隠す
  トグル。取込直後のタブは全時限 × 全曜日のマス目になるため、平日の列に
  土曜の時限行が並ぶノイズを一発で消せる (表示のみ、データは不変)。
- **重なりの承認**: 重複バッジをクリックすると一覧パネルが開き、現行
  データに元からある意図的な重なり (亀73 同室の個別指導など) をペア単位で
  「承認」して件数・赤枠から除外できる (解除も可)。承認は
  `project.approvedConflicts` に保存され、対象セルが動くと自動で失効する
  (保守的動作)。
- **反映の差分プレビュー**: 置き換え反映の前に「変わらず / 変更 / 追加 /
  削除」の内訳と明細 (変更はフィールド単位の before → after) を表示。
  マッチングは全フィールド一致 → 位置 (曜日×時刻×学年×クラス) の 2 段階
  (`reflect.diffReflection`)。
- **取込の平日/土曜タブ分割**: 「⬇ 本体から取込」に「平日と土曜で学年
  タブを分ける」オプション (既定オン)。平日と土日の両方にコマがある学年を
  「中3」「中3 (土)」の 2 タブに分け、曜日で時刻体系が違う学年のマス目が
  締まる。中3 の 2学期変更は分割時も正しく働く (前倒しは平日タブ、内申
  対策の午前枠は土タブへ)。
- **Undo/Redo**: ↩ ↪ ボタンと Ctrl+Z / Ctrl+Y (テキスト入力中はブラウザ
  標準の undo を優先)。連続編集 (タイピング等) は 1 つの取り消し単位に
  束ねる。プロジェクトの削除・取込もまとめて戻せる。
- テスト +13 (承認 4 / 差分 5 / タブ分割 4)。regular-builder 計 58。

### Added (通常時間割作成: 複数プロジェクト + 本体から取込 + 中3 2学期変換)
- 通常時間割作成が**複数プロジェクト**を持てるようになった。「2026 1学期」
  「2026 2学期」のような時間割案をツールバーのプルダウンで切り替えて編集
  できる (＋新規 / ⧉複製 / 🗑削除付き。削除はタブ・セルを巻き込むため
  確認ダイアログ)。保存形状はワークスペース (プロジェクト配列 +
  アクティブ id) に拡張し、旧単一プロジェクト形状は自動で引き継ぐ。
- **「⬇ 本体から取込」** (反映の逆方向) を追加。本体の時間割を選んで
  プロジェクトに変換する — 時限プールは出現時刻から自動生成、学年ごとに
  タブ化、クラス列の既定教室は最頻値から推定。同一マスの並列コマ
  (確認テストの複数監督や高3 の選択講座) は同ラベルの列を増やして保持し、
  **シード全 216 コマの取込 → 反映で欠落・変質ゼロの round-trip をテストで
  保証**。
- 取込オプション**「中3 の 2学期変更を適用する」**: 平日の最終コマ
  20:45-21:30 の内容を新 1 限 18:00-18:45 へ移動、確認テストを
  20:40-20:55 に繰り上げ、土曜に内申対策の午前枠 (10:00-11:00 /
  11:10-12:10 / 12:50-13:50 / 14:00-15:00) を空の時限として追加する。
  現行の時間割を取り込んでチェックを入れるだけで 2学期のたたき台が
  できる (`regular-builder/importTimetable`、冪等・中3 以外は不変)。
- テスト +19 (workspace 5 / importTimetable 14)。なお前項の「テスト +42」は
  テストファイル間の fixture import による二重登録込みの数で、正しくは
  +26 だった (fixture を testUtils に分離して解消)。

### Added (通常時間割作成: 講習ビルダー風の専用作成タブ)
- サイドバー「時間割管理 > 🏗 通常時間割作成」(chord `g r`) を新設した。
  講習時間割作成の操作感で通常時間割をゼロから設計する専用ビュー
  (`src/regular-builder`)。学年タブ × 「曜日 × 時限 × クラス」のマス目で
  組み、完成したら「⤴ 本体へ反映」で時間割 + コマとして書き出す。
- **時限は時刻付きで登録** (全タブ共通)。中3 の 18:00 開始と中12 の
  18:55 開始のように学年で時刻体系が違っても、タブごとに使う時限を選んで
  表現できる。クラスには既定教室を設定でき、セル側で上書きも可能。
- **セルは 教科 / 講師 / 教室 / 備考 の 4 項目**。講師・科目は入力候補
  (datalist) 付きで、講師マスタは本体のコマから一括取込できる。複数講師は
  「·」区切り (全角中点でも可)。備考に「隔週(◯◯)」「合同」等を書けば
  そのまま slot.note に反映される。
- **講師・教室の重複をタブ横断で自動チェック**。時限 id ではなく時刻の
  重なりで判定するため、開始時刻が違う学年間の衝突も検出する。該当セルは
  赤枠 + ⚠、ツールバーに件数バッジ。「👁 講師で探す」で特定講師のセルを
  強調表示できる。
- **反映は 2 モード**: 新しい時間割として作成 (名前・期間付き) / 既存
  時間割の置き換え (旧コマ削除 → 差し替え、確認ダイアログで警告)。反映後は
  ヘッダのプルダウン切替・日付ビュー・第N回カウント (期ごとリセット) に
  そのまま乗る。変換は純粋関数 (`regular-builder/reflect`) でテスト済み。
- 下書きは `appData/genyakubu-regular-builder-project` に本体データと独立
  して保存され、Firebase 設定済み環境ではクラウド同期される (クラウドへの
  書込は管理者ログイン時のみ。ローカル編集は誰でも可、反映ボタンは管理者
  限定)。
- v1 は手動入力のみ。ドラッグ移動・Undo/Redo・自動生成・スナップショットは
  今後の候補 (CLAUDE.md 参照)。テスト +26 (model / conflicts / reflect)。

### Added (期切替支援: 後期時間割を複製 + 時刻一括変換で組めるように)
- 時間割管理に**「時刻一括変換」**パネルを追加した。前期 → 後期のように
  「割当 (教科・講師・クラス) はそのまま、時刻だけ変わる」期の切替で、
  複製した時間割のコマ時刻をまとめて差し替える。対象時間割 × 学年
  (完全一致) × 曜日で絞り、変換元時刻 (件数付き候補) → 変換先時刻の
  ルールを複数指定。適用前に対象件数をプレビューし、確認ダイアログを
  経て保存する。**「中3 平日前倒し (後期)」プリセット**付き
  (20:45-21:30 → 18:00-18:45、確認テスト 21:35-21:50 → 20:40-20:55。
  旧 1・2 限は後期でも同時刻のまま 2・3 限になるため変換対象はこの
  2 種類で足りる)。ロジックは純粋関数 `utils/timeBulkEdit` に切り出し
  (テスト +15)。
- 時間割の**複製で授業セット (回数カウントの束ね) も引き継ぐ**ように
  した。複製元のコマに対応する授業セットを新しいコマ id に読み替えて
  複製する (2 コマ以上写像できたセットのみ)。
- **「第N回」は期ごとにリセット**。カウント起点を「学年グループ開始日
  (表示期間設定) と所属時間割 startDate の遅い方」にし
  (`getSlotCountStartDate`)、所属時間割の有効期間外の日はカウント
  しないゲートを追加した。前期/後期のコマが同一セット・同一コホートに
  並存しても二重カウントせず、後期は開始日から ① で数え直しになる。
  中学部開講日オリエンの 1 限判定も別の期のコマ (後期の 18:00 開始) に
  奪われない。テスト +11。
- 前期/後期の切替そのものは既存機能で行う: 時間割管理で現行を
  「2026 前期」に改名して終了日を設定 → 複製で「2026 後期」を作成して
  開始日を設定 → ヘッダのプルダウンで表示切替 (日付ビューは期間で自動)。

### Added (講習時間割: 講師別 Excel にクラス別回数まとめシート)
- 講師別 Excel の 1 枚目に**「クラス別回数」まとめシート**を追加した
  (S1)。行 = 講師、列 = 学年(タブ) × クラスの行列で、どの講師がどの
  クラスに結局何回行くかが最初のシートで分かる。座席表を毎回提出する
  運用で、必要枚数をあらかじめ印刷しておく用途。
- 右端は行の計 (= その講師が実際に行く回数)、最下段は列の計 (= クラスの
  総コマ数)。講師未割当のコマは '(未定)' 行 (グレー) に集計するので、
  割当が済んでいないことも紙面から分かる。0 回のセルは空欄。
- **確認テストの回数も併記** (S2)。各日の最終コマは授業 + 休憩 + 確認
  テスト 15 分を含む設計で、担当講師がそのまま確認テストを監督する
  (= 座席管理がもう 1 回発生する) ため、各セルに「5(テ2)」の形で
  うち確認テスト付き (最終コマ) の回数を添える。計・合計行も同表記で、
  テ表記がある場合は凡例に説明を出す。
- 合同 (複数クラス 1 コマ) は科目別シートと同じ「代表クラスに 1 回」の
  規約で集計し、外部授業 (他学年セッション) は対象外。該当がある場合のみ
  最下部に凡例を注記。B4 縦 + 先頭 3 行繰り返しの印刷既定は個人シート
  (P1) と同じ。
- **各講師の個人シートにも自分の分を掲載** (S3)。スケジュールリストの下に
  学年 (タブ) ごとの小さな表ブロック (タブ名 → クラス → 回数 `5(テ2)`) を
  縦に積み、最後のブロックの右に「計」を付ける。行くクラスだけを出すので
  コンパクトで、印刷して個人に渡った紙 1 枚で座席表・確認テストの必要
  枚数が分かる。空行で本体と切り離してあるため、学年で絞り込んでも
  ブロックは巻き込まれない。
- 集計は純粋関数 `computeTeacherClassCounts` (utils/excelExport) に
  切り出し。テスト +16 (xlsx round-trip 検証含む)。

### Added (まとめて印刷: 複数月選択 + バイト以外の講師も対象に)
- 月次予定の「📋 まとめて印刷」で**対象月を複数選択**できるようにした
  (①)。現在表示中の月を基準に前 1 か月〜後 4 か月をチェックボックスで
  選べる (既定は表示中の月のみ)。夏期講習の 7-8 月ぶんを 1 ジョブで
  出す想定で、冬期の 12-1 月 (年跨ぎ) も選択可。紙順は講師ごとに各月
  1 枚ずつ連続 (人単位で束ねて配布しやすい順)。印刷後は元の表示月に戻る。
- 同ダイアログに**「バイト以外の講師」セクション**を追加した (②)。
  常勤講師 (時間割のコマに登場するがバイト一覧に無い講師) をサイドバー
  と同じ教科別グループで表示し、バイトと混在選択できる。個人スケジュール
  なので講習コマ (講/外カード) も月次カレンダーにそのまま載る。
- 対象月候補・ジョブ名の整形は純粋関数 `buildBatchMonthOptions` /
  `buildBatchDocTitle` (utils/printStyles) に切り出し。テスト +7 件。

### Added (講習時間割: プリセット改名を登録済みセッションへ同期)
- 他学年セッションプリセットの名称/メモを後から直したとき (例:
  「予備校1限」が実は2限だった)、そのプリセット由来の登録済みセッションの
  メモも一括で追従できるようにした。プリセット編集フォームで名称/メモを
  変えると「登録済みセッション N 件のメモも更新する」チェックボックス
  (既定 ON) が現れ、保存時にまとめて更新する (Undo 1 回で両方戻る)。
- 対象は「変更前の展開ラベル (memo 優先、無ければ name) + 時刻が完全一致」
  のセッションのみ (`utils/presetRenameSync` の純粋関数)。適用後に手で
  書き換えたメモや、同名でも時刻が違う (別プリセット由来の) セッションは
  巻き込まない。時刻まで見るため「1限→2限, 2限→3限…」の連鎖リネームも
  順序を問わず安全。講師の自動NG判定は従来どおり時刻ベースなので不変。
- テスト +15 件 (presetRenameSync 7 / projectReducer 4 / AbsenceNgPanel 4)。

### Added (個人スケジュールの講習コマに回数連番 ①②… を表示)
- 個人月間スケジュール (MonthView) の講習コマカードで、科目名の直後に
  「そのクラスでその科目が何回目か」の丸付き数字を表示するようにした
  (例: 「13:00 英語②」)。番号は講習時間割作成の画面・講師別 Excel・
  配布用出力と同じ `makeSubjectOrderMarker` (クラス内を日付 → 時限順に
  走査した 1-based 連番) で導出し、紙面間で食い違わない。
- 合同 (複数クラス 1 コマ) でクラスごとに回数が違う場合は cls の並び順で
  "/" 連結して表示する (例: ３S/３A の「英語③/①」)。同一クラス×同日の
  科目重複 (builder 側の ⚠️2回 警告状態) 中は builder と同様に番号を
  付けない。講習期間の外部授業 (「外」カード) は対象外。
- `makeSubjectOrderMarker` は excelExport.ts から analysisHelpers.ts へ移動
  (exceljs 非依存の builderLessons からも使うため。excelExport チャンクの
  遅延ロードは維持)。テスト +6 件。

### Changed (講師の個人スケジュール: 既定を月間に + タグ初期値の自動化)
- サイドバーや Cmd+K から講師を選んだときに開く個人スケジュールの既定
  ビューを週間 → **月間 (MonthView)** に変更した。ヘッダの 週間/月間
  ボタンや `g w` / `g o` での切り替えは従来どおり。
- 講師を選択したタイミングで、テスト期間・特別イベントの**タグフィルタを
  担当コマから導出した初期値に自動リセット**するようにした
  (`utils/teacherTags` 新設)。判定は現在の時間割の担当コマ (隔週
  パートナー含む) からの導出のみで、利用履歴などは使わない:
  - 学校名タグは slot.subj の先頭トークン (= 学校) と照合する。素の部分
    一致だとタグ "高松" が 高松桜井/高松西/高松一 にも当たるため、先頭の
    "高松"/"第"・末尾の "高校"/"高" を落とした「核」同士で比較する
    (schoolCore)。タグ "高松" は 高松高 のみ、"第一"/"一高" は 高松一、
    "桜井" は 高松桜井 にマッチ。正規化で拾えない略称 ("高高" 等) は
    SCHOOL_TAG_ALIASES で対応。
  - "中学" / "高校" (部単位) のタグは担当コマの学年 (gradeToDept) で判定。
    学年表記 ("附中", "高1" 等) のタグは slot.grade との部分一致。
  - どのコマにもマッチしないタグ (行事名などの自由ラベル) は自動 OFF の
    対象にせず、既定の ON のまま残す。
  - 自動リセット後もチップの手動トグルは従来どおり有効 (次に講師を選択
    するまで保持)。まとめて印刷 (BatchPrint) は従来どおり現在のフィルタ
    状態のまま印刷する (講師ごとの再導出はしない)。
- テスト +11 件 (slotMatchesTag / deriveTagFiltersForTeacher)。

### Fixed (モバイル: サイドバーがスクロールできず講師一覧に届かない)
- 画面の低い端末ではメニュー群だけでサイドバーの高さを使い切り、講師一覧
  (flex:1) の高さが 0 になってスクロールもできず講師の個人スケジュールへ
  辿り着けなかった。768px 以下ではメニュー + 講師一覧を 1 つのスクロール
  領域 (`.sidebar-scroll`) に統合して全体を縦スクロール可能にした
  (overscroll-behavior: contain で背景へのスクロール連鎖も防止)。
  デスクトップは従来どおり「メニュー固定・講師一覧のみスクロール」。

### Added (講習時間割 → 個人月間スケジュールの反映)
- 講習時間割作成で組んだ講習コマを、個人の月次カレンダー (MonthView) に
  「講」バッジ付きカードとして表示するようにした。builder の project (RTDB
  `appData/builder/schedule_project`) を親アプリ側で読み取り専用に購読し
  (`useBuilderProject`)、日付ラベル "M/D(曜)" は project.updatedAt を基準に
  最も近い年へ解決して実日付に展開する (`utils/builderLessons`。冬期講習の
  年跨ぎも基準日との距離で解決。過去方向の距離は 2 倍に重み付けし、半年近く
  先に組んで以後未編集の project でも前年へ誤解決しない)。正は常に builder
  側で親アプリからの書き戻しは無く、編集も builder 側で行う (カードに
  クリック導線なし)。
- 同じ日・時限・講師・科目の複数クラス (合同) は 1 枚のカードにまとめて
  クラスを "/" 連結で表示。講師「未定」のセルは載せない。時限の時刻は
  ラベル ("1限 (13:00~)") から解析し、読めない時限は時限名で表示する。
- **講習期間の外部授業 (予備校・高校等の externalSessions) も反映する**。
  講師別 Excel のグレー行と同じ扱いで、無彩色の「外」バッジカードとして
  同じセルに時刻順で混ぜる。種別ラベルも Excel と同じ規則: メモ
  (プリセット適用時はプリセット名) を優先し、メモ未設定でも時刻が
  プリセットに一致すればその名前を表示にだけ使い (computePresetMemoBackfill
  再利用)、判別できなければ「外部」。時刻の読めないセッションは同日の
  末尾に沈める。印刷凡例に「外 = 講習期間の外部授業」を追加。
- 講習期間は通常時間割の表示範囲 (displayCutoff) の外になるのが常なので、
  講習コマはカットオフでも休講の巻き添えでも消さない。講習コマのある日は
  「未確定」プレースホルダを出さず講習の予定を紙面に載せる。月次印刷凡例に
  「講 = 講習」を追加。本人担当コマなので表示トグルの対象外 (H1b と同じ)。
- 制約: 講習コマへの代行管理は対象外 (slotId が無い。追加授業の H1d と同種)。
  builder の project は常に 1 つのため、次シーズンの project 作成を始めると
  前シーズンのコマは過去月の表示からも消える (ライブ表示専用)。
- テスト +26 件 (変換・年推定・パース 23 / MonthView 表示 3)。

### Changed (講習時間割作成: 配布用 Excel を完成版レイアウトに置換)
- 「🎒 配布用」の Excel 出力を、例年手作業で仕上げていた掲示紙面 (2025 年
  夏期の完成版 xls) を再現する**完成版レイアウト**に置き換えた
  (`distributionExport.ts` 新設)。同じ時限セットを使う学年 (タブ) を
  1 シートに横並びにし、日付ブロックを左列 → 右列の 2 段組で配置。各時限は
  「時限名+クラス別科目」「時刻+クラス別講師」の 2 行ペアで、A4 縦 1 ページ
  に収まる印刷設定を埋め込み済み。
- クラス行 + 教室行 (+ 複数学年なら学年行) のヘッダ付き。**教室行は空欄で
  出力**するので配布前に Excel 上で記入する (教室はツールで管理しない)。
  確認テスト (タブ内全クラス同一科目・講師なし) は 1 セルに結合、授業ゼロの
  日はイベント説明の書き込み欄として全結合される。
- 旧「配布用 (注記なし)」の学年グリッド形式 (buildScheduleWorkbook の
  clean オプション) は廃止。作成用の注記入り出力は従来どおり
  「全体スケジュール」で行う。

### Added (講習時間割作成: 講師別 Excel にタイトル行 + B4 縦の印刷デフォルト)
- 各講師シートの先頭に「講師名 — プロジェクト名 / 期間 (その講師の初日〜
  最終日) / 出力日」のタイトル行を追加。印刷した紙の一番上に誰のスケジュール
  かが必ず載り、2 ページ目以降にもタイトル・ヘッダ行が繰り返し印刷される。
- 全シート (各講師 + 全講師リスト) に **B4 縦**の印刷設定を埋め込み。Excel で
  開いてそのまま印刷すれば B4 縦になる。
- 両面 (長辺綴じ) は xlsx フォーマットに保存できない (OOXML の pageSetup に
  duplex が無く、プリンタドライバ固有の領域にしか載らない) ため、
  プリンタ側の既定設定で運用する。

### Added (講習時間割作成: 講師別 Excel にオートフィルタ)
- 講師別スケジュールの各講師シートと全講師リストのヘッダ行に Excel の
  **オートフィルタ**を付けた。学年(タブ) 列で「中3 だけ」「中1+中2」の
  ような任意の組み合わせに絞って確認・印刷できる (絞った状態で印刷すると
  表示行だけが紙面に載る)。外部授業行も同列の種別 (予備校・高校等) で
  同様に絞れる。全講師リストは講師名 × 学年(タブ) の組み合わせ絞りも可能。
- タブ別にシートやセクションを増やす案は、講師 × タブでシート・紙面が
  爆発するため見送り、絞り込みは Excel のフィルタに任せる方式にした。

### Added (講習時間割作成: 日付ごとの確認ステータス「OK / 要確認 / 不備あり」)
- 時間割表の日付セル (ラベル直下) に 3 つのチェックを追加。
  - **不備あり**: その日の全セル (時限×クラス) に科目+講師 (「未定」以外) が
    入っていないと自動でチェックが付き、埋まると自動で外れる (手動変更不可。
    導出は `computeIncompleteDateIds`、保存しない)。
  - **OK / 要確認**: 手動チェック (排他、再クリックで解除)。タブ (学年) × 日付
    ごとに `tab.dayStatuses` として保存され、同期・Undo・JSON 書き出しに乗る。
    不備あり中は OK を選べない (要確認は講師調整中のメモとして付けられる)。
  - 日付のプール削除時は該当ステータスも cascade 掃除。読込時は型崩れを
    正規化 (`normalizeTabDayStatuses`、version 据え置きの後発フィールド補完)。
  - テスト +17 件 (reducer / 判定 / migrate / ScheduleTable UI)。

### Added (講習時間割作成: 講師別 Excel に予備校・高校等の外部授業を統合)
- 講師別スケジュールの各講師シート・全講師リストに、講師不在・NG 設定の
  他学年セッション (予備校・高校等) を**日付 (カレンダー順) → 時刻順**で
  講習コマに混ぜて出力する (グレー行、科目欄は空欄、クラス列は「-」)。
  学年(タブ) 列には種別＝メモ (プリセット名) を出す。メモ未設定でも時刻が
  プリセットに一致すればその名前を**表示にだけ**使い (データは書き換えない)、
  判別できない場合のみ「外部」。講習コマが 1 つも無い講師は従来どおり
  シートを作らない。
- 日付が変わる行の上辺を太線にして 1 日のまとまりを見やすくする
  (各講師シート + 全講師リスト。全講師リストは講師の切り替わりにも太線)。
- プリセット適用時、メモ未設定のプリセットは**プリセット名をメモの既定値**
  として展開する。セッション一覧・自動NG ツールチップ・Excel で
  「予備校か高校か」を判別できるようにする。
- 登録済みのメモ無しセッションには、講師不在・NG パネルの
  「🏷 プリセット名をメモに一括適用」で**時刻の一致から後付け**できる
  (`computePresetMemoBackfill`)。同時刻プリセットが複数のときは
  期間・対象講師で絞り込み、判別できない分はスキップ。既存メモは
  上書きせず、Undo 1 回で戻せる。
- テスト +19 件 (excelExport / AbsenceNgPanel / presetMemoBackfill / reducer)。

### Added (講習時間割作成: Firebase 同期 — E6a)
- プロジェクトを親アプリと同じ Firebase RTDB (`appData/builder/schedule_project`)
  へ自動同期。タブレット等の別端末からも同じ時間割を開いて編集できる
  (書込は親アプリの管理者ログインが必要。未ログイン・Firebase 未設定環境では
  従来どおりローカル保存のみ)。
  - 保存は既存の debounce autosave に相乗りし、LocalStorage と同一の JSON
    **文字列**を送信 (RTDB のオブジェクト保存は空配列・空オブジェクトを刈り取る
    ため)。サイドバーの同期インジケータにも書込中として反映される。
  - 受信は `decideRemoteProject` (純粋関数) で判定: 他端末の保存は
    toast 通知つきで全置換 (Undo 履歴はリセット)、同一内容はスキップ、壊れた
    blob はローカル正で自己修復、サーバ側がより新しいスキーマ version の場合は
    上書き破壊を防ぐため同期を停止してリロードを案内。
  - 競合はプロジェクト単位の last-writer-wins (K5a のユーザ判断に整合。
    2 端末同時編集は運用上想定しない)。テンプレート・「デフォルトとして保存」は
    端末ローカルのまま。
  - テスト +36 件 (projectSync.test.ts / useHistoryStack.sync.test.jsx)。
    database.rules.json に文字列 validate を追加。
  - 校正レビュー (8 角度 + 反証検証) の反映: 単体テスト・e2e・dev を実
    Firebase から隔離 (`test.env` / `webServer.env`)、初回送信前の version
    確認 (`get()`) で書込前の stale 上書きを防止、activeTabId / updatedAt を
    同一性比較から除外 (タブ切替が他端末の Undo 履歴を消さない)、受信
    payload の templates strip、ノード削除後の再 seed 修正、seed 失敗の
    toast 沈黙化、エラー通知を「失敗エピソードごとに 1 回」へ、ゴミ blob の
    stale-client 誤判定防止、比較の cleanSchedule 対称化、stableStringify の
    共有 util 化 (`src/utils/stableStringify.js`)、JSON 読込の新 version
    ガード、連続反映 toast の間引き。

### Fixed (講習時間割作成: 日付ラベルの表記ゆれ「8/6」と「8/6(木)」を読込時に統一)
- date picker 化以前の手入力で残った曜日サフィックス無しの日付「8/6」が、
  現行形式「8/6(木)」と別の日付として日付プールに共存し、講師不在・NG
  パネル等に同じ日が 2 つ並んでいた。読込時マイグレーションに統一処理
  (`utils/dateLabelUnify.js`) を追加:
  - 同じ月日の「M/D(曜)」がプールに居る場合はそちらへ**マージ**
    (コマ・使う日選択・NG・外部コマ数・合同グループ・他学年セッション・
    プリセットの参照も付け替え。衝突時は (曜) 側を優先)。
  - 居ない場合は (曜) 付きラベル群と矛盾しない年から曜日を推定して
    **リネーム**。年が確定できないときは誤った曜日を付けず温存する。
  - 「8/6-補講」のような包含ラベルの NG キーは最長一致判定で巻き込まない。
    テスト +18 件。

### Added (講習時間割作成: ドキュメント整備 — E8a / E8b / E8d)
- **ユーザーガイド** (`src/timetable-builder/docs/USER_GUIDE.md`): 画面構成・
  初回セットアップ・CSV 一括登録・基本操作・自動作成・スナップショット/差分/
  テンプレート・出力・トラブルシュートを網羅した操作マニュアル。
- **アーキテクチャ** (`src/timetable-builder/docs/ARCHITECTURE.md`): Mermaid で
  全体構成 / 編集 1 操作の sequence / 自動生成パイプライン / データモデルを図示。
- **ROADMAP**: 冒頭に「§0 完了済み一覧」インデックス表を追加。

### Changed (講習時間割作成: 操作系の UX 仕上げ — E1a / E1b / E1e / E1f)
- **Excel 出力を dropdown 化**: ヘッダの全/個人 Excel 2 ボタンを「📊 Excel出力 ▾」
  に集約し、ボタン群を flex-wrap で折り返す (狭画面対応)。
- **フォーカス可視化**: `.builder-root :focus-visible` に太い (3px) リング + offset
  を追加。色だけに頼らず形状で認識でき、濃色ボタン上でもコントラストを確保。
- **矢印ナビの端動作統一**: セルの ← → が行頭/行末で反対端へ wrap し、移動が
  途切れない。
- **ヘッダの長押し対応**: スケジュール表のヘッダ (日付/時限/クラス) もタッチ
  長押しでメニューを開けるように。

### Added (講習時間割作成: 自動生成の live 進捗 — E2f)
- 探索の途中経過 (充填数 / 探索回数) を Worker から間引き通知し、生成中の
  ツールバーにライブ表示。`onProgress` を autoGenerator → worker → runGenerator →
  BuilderApp に配線。テスト +5 件。

### Added (講習時間割作成: タッチ操作・狭画面対応 — E1f / E1a)
- **長押しでコンテキストメニュー** (`hooks/useLongPress.js`): タッチ端末で
  時間割セルを長押しすると、右クリックと同じメニュー (コピー/貼付/クリア/
  ロック/NG 登録) が開く。500ms 判定、スクロール (10px 移動) でキャンセル、
  マルチタッチ無視。HTML5 ドラッグはタッチで発火しないため D&D と競合しない。
  テスト +6 件。
- **Toolbar の折り返し**: 狭画面でボタン群が画面外へはみ出さず段組みで
  折り返すように (`flex-wrap justify-end`)。
- オンボーディングに「タッチ端末では長押し」と追記。

### Added (講習時間割作成: NG 日時の CSV 一括取り込み — E2a)
講師の不可時間 (NG) を CSV で一括登録できるように。初期セットアップの手入力を軽減。

- **パーサ** (`utils/csvImport.js` の `parseNgCsv`): ヘッダ
  <code>name(または teacher),date,period</code>。空欄エラー集約・重複行 dedupe・
  未登録の講師/日付/時限を warning として返す。
- **reducer** (`teacher/importNg`): name 一致の講師にのみ NG を追加 (dedupe)、
  未登録 name は skip、変更なしは同参照で履歴を汚さない。
- **UI** (`ConfigModal/NgCsvImport.jsx`): 「📅 講師不在・NG」タブに折りたたみ
  パネル。paste / ファイル選択 / ドラッグ&ドロップ + ライブプレビュー。
- **テスト**: parseNgCsv 8 / reducer 3 / NgCsvImport 5。

### Added (講習時間割作成: 自動生成の手応え可視化 — E2f)
大規模プロジェクトで「生成にどれだけ時間がかかり、どこで詰まったか」が読めるように。

- **生成統計** (`autoGenerator.generateSinglePattern`): 探索回数 (iterations)・
  上限到達 (hitLimit)・最初に埋められなかったコマ (stuckSlot) を返す。
- **経過時間**: 生成中は Toolbar に「⏱ X.Xs」をライブ表示、完了後は結果
  ヘッダに総時間。
- **結果パネル**: 各案に「探索 N 回 / (上限到達) / 詰まり: 日付 時限 クラス」。
- **テスト**: autoGenerator +3 / SummaryPanel 新規 4。

### Changed (講習時間割作成: コントラストを WCAG AA 準拠に — E1e)
- **builder-orange** を #e67a00 (白背景 2.94:1 で AA 未達) → **#c2410c**
  (5.18:1) に。部分解テキスト/ボタン・warning 系の可読性が AA を満たす。
- 読めるアイコンボタン (×閉じる/削除・▲▼並べ替え) を ink-ghost (1.92:1) →
  ink-muted (5.74:1) に。ink-ghost は罫線・disabled 等の装飾用途に限定。
- **コントラスト計算** (`utils/contrast.js`): WCAG 2.x の純粋関数を追加し、
  `contrast.test.js` で「読めるテキスト」配色 21 ペアが AA を満たすことを
  回帰テスト化。テスト +27 件。

### Added (講習時間割作成: キーボード操作の完成度向上 — E1b)
マウスに頼らず設定モーダルとタブを操作できるように。

- **focus trap の共通化** (`hooks/useFocusTrap.js`): OnboardingOverlay の
  インライン実装をヘルパー化し、Escape で閉じる + Tab/Shift+Tab で dialog 内に
  フォーカスを閉じ込める。入れ子 dialog は最上位だけが応答 (LIFO)。
- **設定モーダル**: focus trap を適用し Tab が背景へ抜けないように。カテゴリ
  タブを `role="tablist"` 化し ← → / Home / End で切替 (端で wrap)。
- **学年タブ (TabBar)**: 同様に `role="tablist"` + 矢印キーで切替、
  aria-selected / roving tabindex 付き。
- **テスト**: useFocusTrap (新規 6) / ConfigModal (+5) / TabBar (+5)。

### Added (講習時間割作成: データ消失を防ぐ 2 つの保険 — E6c / E6d)
- **LocalStorage 容量監視** (`utils/storageHealth.js`): 起動時に保存サイズを
  概算し、5MB の 50% を超えていたら整理・バックアップを促す warning toast。
  通常運用 (~12KB) では発火しない閾値。テスト +13 件。
- **複数タブ競合検出** (`utils/tabPresence.js` / `hooks/useTabPresence.js`):
  `BroadcastChannel` で同一ブラウザの別タブが Builder を開いていることを検出し、
  autosave の相互上書きを防ぐため一度だけ警告。非対応環境は no-op。テスト +11 件。

### Fixed (講習時間割作成: 校正レビューでの指摘修正)
3 観点の独立レビューで見つかった不具合を修正:

- **生成パラメータの数値入力が打ち直せない (HIGH)**: controlled value が毎キー
  入力で clamp され、特に「探索回数の上限」(最小 5万) を入力できなかった問題を
  修正。ローカル draft を持ち blur / Enter で確定する方式に (スライダーは即時)。
- **修正提案「1日コマ数上限を上げる」が上限超過値を提案 (MEDIUM)**: 適用時に
  clamp される値を提案し、toast の表示と実際の適用値が食い違わないように。
- **読込検証が schedule 欠落を許容 (MEDIUM)**: `schedule` を必須化し、旧
  バージョンの migrate がクラッシュする経路を事前に弾くように。
- **NG 解除のワンクリック適用が二度押しで付け直す (MEDIUM)**: 現在 NG の
  ときのみ解除する冪等動作に。
- **テンプレート全体適用が未検証 (LOW)**: 適用前に構造を検証し、壊れた
  テンプレートはエラー通知して中断。
- **autosave の容量超過が未捕捉 (LOW)**: `localStorage.setItem` を try/catch で
  保護し、失敗時はステータス表示に反映。

### Changed (講習時間割作成: cleanSchedule の計算量を O(K) 化 — E4a)
- `cleanSchedule` を「全 (日付×時限×クラス) を展開して照合」から「既存
  スケジュールキーを走査して entity の存在を直接判定」に変更
  (O(D×P×C+K) → O(D+P+C+K))。挙動は等価。大きめのプロジェクトでの
  保存・読込・パターン適用が軽くなる。テスト +4 件 (全 1347 件 pass)。

### Added (講習時間割作成: 読込データの構造バリデーション — E3d)
壊れた localStorage / 不正な JSON によるクラッシュやデータ損失を防ぐ保険。

- **構造検証** (`utils/projectSchema.js`): `validateProjectShape` が
  tabs / config.dates・periods・classes / subjectCounts / teachers などの
  致命的な型崩れを検出。zod 等の依存は追加せず手書き (バンドル増ゼロ)。
- **起動時** (`loadInitialProject`): 検証 NG ならデフォルトにフォールバック
  して toast 通知 (従来の JSON.parse 失敗と同じ経路)。
- **JSON 取り込み** (`handleLoadJson`): 検証 NG なら適用せずエラー toast。
- **テスト**: projectSchema を新規追加、projectFactory に fallback を追記
  (計 +12 件、全 1343 件 pass)。

### Added (講習時間割作成: テンプレート機能・年度間コピー — E2d)
去年の設定を今年に流用できるテンプレート機能。プロジェクトを名前付きで
保存し、翌年などに「全体」または「講師マスタのみ」を適用して使い回せる。

- **テンプレート管理** (ConfigModal「🗂 テンプレート」タブ): 現在の
  プロジェクトを保存 / 一覧 (作成日・講師数・タブ数) / 「全体を適用」/
  「講師のみ」/ 削除。適用は確認ダイアログ + Undo (Ctrl+Z) で取り消し可。
- **保存先**: localStorage (`builder.templates`、project state とは独立)。
  snapshots は除外して保存。壊れたデータは空配列にフォールバック。
- **テスト**: templates / TemplateManager を新規追加 (計 +15 件、
  全 1331 件 pass)。

### Added (講習時間割作成: 修正提案のワンクリック適用 — E2b MVP)
E1g の修正提案のうち、機械的に確実なものをその場で適用できるように。

- **「適用」ボタン** (`Toolbar` の「設定の問題」popover): 提案を
  `{ text, action }` 構造化し、action 付きの提案に適用ボタンを表示。
  - `releaseNg`: 該当講師の手動 NG をワンクリック解除。
  - `setMaxDaily`: 1 日コマ数上限を必要値へ引き上げ。
  - 適用は単発操作なので Undo (Ctrl+Z) で戻せる。
- **テスト**: fixSuggestions を action 構造に更新、Toolbar に適用経路を追記
  (全 1316 件 pass)。

### Added (講習時間割作成: 講師の連続コマ数制約 — E2c)
「同じ講師に N コマを超える連続担当をさせない」制約を自動生成に追加。
1 日合計の上限だけでなく、連続性も指定できる。

- **設定** (⚡自動生成タブ):「講師の連続コマ数上限」を追加。0 = 制限なし
  (既定なので従来挙動を維持)。
- **solver** (`wouldExceedConsecutive` / autoGenerator): 時限の並び順を見て、
  講師を置くと連続ランが上限を超える場合は候補から外す。「未定」は対象外。
- **テスト**: teacherConstraints / autoGenerator / constants / GenerationSettings
  / projectReducer に追記 (計 +11 件、全 1314 件 pass)。

### Added (講習時間割作成: エラー時の修正提案 — E1g)
自動生成が構造的に解けない設定 (担当講師ゼロ / 科目 capacity 不足) に対し、
「では、どう直すか」の具体策を提示してデバッグ時間を短縮する。

- **修正提案** (`Toolbar` の「設定の問題」popover): 各 infeasibility の下に
  💡 で解決策を箇条書き。例:「12/25 1限 の NG を解除する: 堀上」「別の時限
  なら担当可能: 2限」「英語担当を あと 1 名 増やす」「1日コマ数上限を 6 → 9
  に上げる」など。
- **純粋関数** (`utils/fixSuggestions.js`): `suggestForNoTeacher` /
  `suggestForCapacity` / `buildFixSuggestions`。手動 NG と自動 NG を区別し、
  解除可能な NG や移動先の時限を具体的に挙げる。提示のみで自動適用はしない。
- **テスト**: fixSuggestions を新規追加、Toolbar に表示確認を追記
  (計 +12 件、全 1303 件 pass)。

### Added (講習時間割作成: 講師マスタ CSV のファイル取り込み — E2a)
講師マスタ CSV インポートが貼り付け (paste) に加えて、ファイル選択と
ドラッグ&ドロップに対応。新規セットアップ時の導入障壁を下げる。

- **ファイル選択 / D&D** (`TeacherManager` の CSV パネル):「📂 ファイルを
  選択」ボタンと textarea へのドラッグ&ドロップで CSV を読み込み。読み取った
  内容は既存の paste フロー (parse + プレビュー + 追加/更新/全置換) に合流。
- **ガード**: CSV 以外の拡張子はエラー toast、ドラッグ中は枠をハイライト。
- **テスト**: TeacherManager を新規追加 (ファイル選択 / D&D / 非 CSV ガード
  の 3 件、全 1291 件 pass)。

### Added (講習時間割作成: スケジュール差分ビュー — E1d)
保存したスナップショットと現在の状態が「どのセルでどう違うか」を一目で
確認できる機能。スナップショットと組み合わせて試行錯誤の比較ができる。

- **差分表示** (`SnapshotMenu` に「🔍 差分」トグル): 各スナップショット行で
  「このスナップショット → 現在の状態」の差分を ＋追加(緑)／－削除(赤)／
  ≠変更(橙)のサマリ + セル一覧(日付 時限 クラス: 旧→新)で表示。
- **純粋関数** (`utils/scheduleDiff.js`): `diffSchedules(from, to)` は
  セル単位で added/removed/changed を判定(subject+teacher のみ比較、
  locked は無視、空セルは未割当扱い)。`summarizeDiff` で種別件数。
- **テスト**: scheduleDiff を新規追加、SnapshotMenu に比較操作を追記
  (計 +13 件、全 1288 件 pass)。

### Added (講習時間割作成: 名前付きスナップショット — E1c)
試行錯誤しながら時間割を作る際に「いまの状態を保存 → 別案を試す →
いつでも戻す」ができる便利機能。undo/redo の単線履歴とは別に、名前付きで
複数の状態を残せる。

- **保存・復元 UI** (`SnapshotMenu`, Toolbar に同梱): 📌 ボタンから現在の
  タブの時間割を名前を付けて保存。一覧から「復元 / 改名 / 削除」。
  アクティブタブのものだけ表示し、件数バッジ付き。
- **データモデル**: `project.snapshots`(`{ id, name, tabId, createdAt,
  schedule }`)。schedule は deep copy。タブ削除時は紐づくスナップショットも
  掃除。`migrateProject` で後発フィールドとして空配列に初期化。
- **reducer**: `snapshot/save` / `apply`(記録元タブへ復元 + 切替)/
  `rename` / `remove`。`useProject` に対応 callback を追加(createdAt は
  hook 側で付与し reducer の純粋性を維持)。
- **テスト**: SnapshotMenu を新規追加、projectReducer に追記(計 +19 件、
  全 1275 件 pass)。

### Added (講習時間割作成: 自動生成の操作性向上 — E2e / E2f-cancel / E2h)
「🧙‍♂️ 自動作成」周りの操作性を底上げ。これまでハードコードだった生成
パラメータを設定可能にし、生成の中止と採用案の比較を支援する。

- **生成パラメータの UI 化** (`ConfigModal`「⚡ 自動生成」タブ / E2e):
  「生成する案の数」「講師 1 人の 1 日コマ数上限」「探索回数の上限」を
  number input + スライダーで調整可能に。`project.numPatterns /
  maxDailyHours / maxIterations` に保存し、`resolveGenerationParams` /
  `clampGenerationParam` で範囲外値を防御。reducer は
  `project/setGenerationParams` で部分更新。
- **自動生成の中止ボタン** (Toolbar / E2f 一部): 生成中に「✕ 中止」を
  表示。worker を止めて既存セルは保持したまま中断できる。
- **生成案の負荷偏り表示** (`SummaryPanel` / E2h): 各案の集計に
  「最多 / 最少 / 偏り」を中立表示 (`summarizePatternLoad`)。完全解どうしを
  講師コマ数の均等さで比較して採用案を選べる。
- **テスト**: constants / patternLoad / GenerationSettings を新規追加、
  projectReducer / autoGenerator / Toolbar に追記 (計 +32 件、全 1257 件 pass)。

### Changed (回数カウントを「コース」単位に整合 + コホートUI改善)
コース別終講日のレビューで判明した「終講日コホート」と「回数カウント
(授業セット)」の二重管理を整理。ClassSet 未登録だと 高1・2 の英数 (週2) が
曜日ごとに ①から数え直しになる問題を解消。

- **回数のコホートフォールバック** (`sessionCount.js` / `cohorts.js`):
  `resolveSetSlotIds` が ClassSet 未登録時に「コース (コホート)」単位
  (`buildSlotCohortIndex`) で束ねるようになり、終講日と回数の集計単位を
  1 つの定義に統一。高1・2 英数が 月①→木②→月③… と通算される。
  明示的な ClassSet は従来どおり優先。既存の回数挙動 (単体・セット登録済み・
  オリエン) は不変。
- **コホート内訳の表示** (`CohortCutoffEditor`): 各コホート行に曜日・科目の
  内訳を併記し、何が含まれるか確認できるように。
- **コホート対象外の可視化**: どのコホートにも属さない授業 (高1・2 の英数以外
  など、グループ終了日に従うもの) を一覧表示。
- **微修正**: 中学コホートの並びを平日コース→土曜の順に。`isEnglishOrMathSubject`
  を学校トークン無しの科目名にも対応。

### Added (コース別 終講日設定: 学校別・曜日別の表示終了日)
学年グループ (中1・2 / 中3 / 高1・2 / 高3) では 1 グループにつき終了日が
1 つしか持てず、実際の終講日が細かくズレるケースを表現できなかった問題に対応。

- **コホート導出** (`src/utils/cohorts.js`): いま入っている授業 (slots) から、
  終講日がズレる単位 = 「コホート」を自動で束ねる。
  - 高校: `学年 × 学校` (学校 = `subj` 先頭トークン。高松西 / 高松高 / 高松一 /
    高松桜井 / 東大京大医進 など)。学校ごとにテスト日程・終業が違うため。
  - 中学: `学年 × 曜日ペア` (火木 / 水金)。中3 / 附中3 も区別。同じ回数でも
    曜日が違うと終講日がズレるため。
- **UI** (`src/components/CohortCutoffEditor.jsx`): 時間割管理ビューに
  「コース別 終講日設定」カードを追加。現在のクラスをコホート単位で一覧し、
  クリックで終講日を選ぶ。設定済み件数の表示、対象授業が消えた未使用エントリの
  掃除導線も用意。
- **フィルタ** (`isSlotBeyondCutoff`): 終講日は per-slot 表示フィルタに反映。
  コホート終講日は学年グループの終了日を上書きし、未設定時はグループ設定に
  フォールバック。開始日は従来どおりグループ単位、回数計算は不変。
  `isEntireDayBeyondCutoff` はコホートによる「延長」を考慮 (短縮は per-slot 側)。
  ダッシュボード / 月次 / 確定代行ビューが反映対象。
- **スキーマ**: `DisplayCutoff` に `cohorts` (`CohortCutoff[]`) を追加
  (v13 → v14、既存データは空配列で移行)。

### Fixed (コース別 終講日: 授業の無い曜日が出る / コース単位でまとめる / 高校は英数セット)
コホートを `dayPairLabel` (火→火木 / 水→水金) で機械的にペアへ寄せていたため、
(1) 片曜日しか授業が無い学年で存在しない曜日が表示され (中1=火金 なのに
「中1 火木」「中1 水金」、附中=水 なのに「附中 水金」)、(2) 同じ生徒が
週2回通う1コース (中1=火金 / 中2=月木) が 2 行に割れて終講日を二重入力する
必要があった。

- **コース単位の導出** (`deriveCohortsFromSlots`, `partitionDaysIntoCourses`):
  中学は学年ごとに平日授業を 1 コースに束ねる (中1=火金 / 中2=月木 /
  中3=火水木金 / 附中=水)。土曜の特訓・プレップは別コース。
- **高1・2 は英数のみをセット化** (`isEnglishOrMathSubject`): 学校ごとの
  英数 (週2 で通う中核) だけを 1 コホートにまとめ、英数以外 (土曜の理科・
  古文漢文等) はコホート化しない (= グループ終了日にフォールバック)。
  ID は `H|<学年>|<学校>|英数`。高3 は従来どおり科目別。
- **ID に実曜日を埋め込む**: `M|<学年>|<曜日列>` (例 `M|中1|火金`, `M|中3|火水木金`)。
- **照合を曜日メンバーシップ化** (`findCohortCutoff`): 保存済みコホート ID の
  曜日列に `slot.day` が含まれるかで判定。`isSlotBeyondCutoff` は slot 単位の
  ままで動き、既存 ID (`M|中3|水金` / `H|高1|高松西` 等) とも後方互換。
- `slotCohortId` / `dayPairLabel` / `daysForPairLabel` は役目を終えたため削除。

### Fixed (AbsenceNgPanel のコードレビュー指摘 P1-P3 を対応)
タブ統合直後のコードレビューで判明したバグを修正。

P1 (致命的・mass action リスク):
- **end date デフォルトを単日に**: `formEndDateId` 初期値を `dates[last]` から
  `dates[0]` に修正。デフォルト範囲が全期間 (例: 14 日) から単日に縮退し、
  ユーザが他学年モードで誤って『M 名 × N 日』のセッションを大量生成する
  事故を防ぐ。
- **ALL_TEACHERS sentinel 復活**: 講師選択 state を `{ allMode, names }` に変更し、
  `allMode=true` のときは『現在の project.teachers』から動的に解決する
  (旧 NgSettings の挙動と等価)。『全選択』後に追加された講師も次の操作で
  自動的に対象に含まれる。同パターンを period 選択にも適用 (新 period 追加で
  silent miss を起こす問題も解消)。
- **日付削除時の snap を nearest に**: end date が削除された時、これまで
  `dates[last]` へジャンプして範囲を widen していたのを、`start に揃える
  (= 単日)` に変更。意図しない範囲拡張を防ぐ。
- **applyPreset の partial date update を防止**: preset の `startDateLabel` /
  `endDateLabel` が両方とも現在の dates に存在する場合のみ両方を update。
  片方しか resolve しない場合は両方を skip して toast 警告。

P2 (実バグ):
- **canApply 判定の統一**: NG / 他学年 両モードで `selectedTeacherNames.length` を
  使い、`formTeacherNames.size` と `selectedTeacherIdxs.length` の 1-render
  ズレで NG ボタンだけが矛盾して disabled になる問題を解消。
- **mode 切替時の state クリア**: external → ng 切替時に時刻/メモを clear。
  ng モード中も裏で stale な値が残って戻ったとき silent に submit される
  事故を防ぐ。
- **OK解除の確認ダイアログ**: `handleApplyNg(false)` (OK解除) を実行する前に
  対象件数を表示した showConfirm を必須化。直近 NG したばかりの設定を
  silent に全消去する事故を防ぐ。
- **action 後の toast 通知**: 他学年セッション追加 / NG / OK解除 の各 action
  で件数付きの toast を表示し、『反応してない』勘違いによる再クリックを抑止。

P3 (設計 / 一貫性):
- **expandedDates の stale key cleanup**: dates 配列が変わる度に削除済み
  id を expandedDates から除く useEffect を追加。config/setList で id が
  再利用されて新しい date が silent に折りたたまれる事故を防ぐ。
- **teacher / period 同期の membership 比較**: size 比較だけだと rename swap
  で素通りする問題を解消。`Array#every` で内容も比較する。
- **preset select を controlled に**: 旧 `defaultValue` + 直接 DOM mutation
  を `value=""` + `onChange` に変更。条件 remount 時に古い選択が再出現する
  リスクを排除。
- **applyPreset の `!= null` を `if (truthy)` に**: 空文字 (`''`) は『未設定』
  扱いとして、preset 適用時に formStartTime/Memo が誤って空に上書きされる
  のを防ぐ。
- **quick グリッド `|| ''` を `?? ''`**: `0` を空表示に潰さず、明示的な
  『0 コマ』入力と未入力の区別を保つ。
- **PresetPanel `useState(blankDraft())` を lazy init に**: `useState(() => blankDraft())`
  に変更。毎レンダーの不要な実行を排除。
- **ROADMAP の stale 参照を更新**: 削除済み NgSettings.jsx → AbsenceNgPanel.jsx
  に書き換え。

Tests: 全体 1181 件、lint 0 / typecheck 0 / build OK。
(AbsenceNgPanel 自体への直接 UI テスト追加は ROADMAP の E3e に明記済み。)

### Changed (「他学年・午前」と「日時NG」タブを統合)
旧『📅 他学年・午前』タブと『🚫 日時NG』タブを 1 つの『📅 講師不在・NG』タブに
統合した。日付ヘッダを共有し、同じ日に対するセッション登録と NG 設定を
1 つのセクションで縦に並べて見られるようになった。

新タブ (`AbsenceNgPanel.jsx`) の構成 (上から下):
1. **プリセット管理** (折りたたみ): 旧 ExternalCounts と同等
2. **統合一括登録フォーム**: 上部のラジオで「📅 他学年セッション (時刻あり)」
   と「🚫 手動NG (時限指定)」を切替。共通フィールド (講師複数 / 期間 / メモ)
   はそのまま、モード固有の入力欄だけが切替わる。プリセットは他学年モードでのみ
   表示される。
3. **日付ごとの折りたたみセクション**: 各日付ヘッダの下に
   (a) その日の他学年セッション一覧、(b) NG マトリクス (講師×時限) を縦に
   並べて表示。日付ヘッダには「他学年 N 件」「NG N 件」のバッジを併記。
4. **クイック数値入力グリッド** (折りたたみ・最下部): 時刻無しの粗い
   コマ数管理用に旧 ExternalCounts のグリッドを移植。教科グループ見出し付き。

旧ファイル `ExternalCounts.jsx` / `NgSettings.jsx` は削除。ConfigModal の
タブ ID も `external` + `ng` → `absence-ng` に変更 (旧 ID は廃止)。reducer の
action は変えていない (`teacher/toggleNg` / `setNgBatch` / `addExternalSessions` /
`teacher/addExternalSessionPreset` 等はそのまま再利用)。

Tests: 全体 1181 件、lint 0 / typecheck 0 / build OK。

### Fixed (講師グループ表示のコードレビュー指摘 P1-P4 を一括対応)
コードレビューで判明したバグを修正。

P1 (silent data corruption):
- **同名講師の禁止**: `teacher/add` / `teacher/rename` で名前重複を no-op に。
  以前は重複を許容していたため、teacherIdxByName Map の last-write-wins で
  『見ている行と書き込まれる行がズレる』状況が NgSettings / TeacherManager /
  ClassPriority で発生していた。`<tr key={t.name}>` もキー衝突を起こしていた。
- **既存重複への migration**: `migrateProject` 時に重複名を " (2)" / " (3)"
  suffix で uniq 化。古いプロジェクトを開いても自動的に修復される
  (`utils/scheduleKey.js#dedupeTeacherNames`)。
- **reducer 側の idx 検証**: `teacher/toggleNg` / `teacher/toggleSubject` /
  `teacher/toggleClassPriority` / `teacher/remove` / `teacher/rename` で
  `idx` の範囲チェックを追加。undefined や範囲外を no-op にし、
  `newTeachers[undefined]` で配列が文字列キーで汚染される事故を防ぐ。
- **group.key の sentinel 化**: `groupTeachersBySubject` の戻り値に `key` を
  追加 (`subj:<name>` / `__multi__` / `__other__`)。ユーザが教科名に
  '複数教科' や 'その他' を入れても React のキー衝突が起きない。全 6 consumer
  を `key={group.key}` に統一。

P2 (実バグ):
- **ScheduleCell の '複数教科' 罠**: subject 選択済み時は flatten モードで
  単一 optgroup に集約。未定など多教科担当が '複数教科' に分離される問題を
  解消 (新オプション `flattenIntoSingleSubject`)。
- **ScheduleCell の useMemo 漏れ**: `groupTeachersBySubject` を useMemo で
  包み、grid 内のすべての cell で毎レンダー再計算されていた問題を解消。
- **SummaryPanel の in-place sort**: `[...group.teachers].sort(...)` で
  groupTeachersBySubject の戻り配列を破壊しないように。
- **TeacherManager の InlineNameEdit unmount**: `<tr key>` を `t.name` から
  index `i` に戻し、外部リネーム中のドラフト消失を防止。

P3 (一貫性 / 設計):
- **subjectOrder 連携**: `groupTeacherNames` (本体側) が `subjects[].name`
  または明示の `subjectOrder` を尊重するように。ユーザが builder で
  リオーダした教科順が本体側 (CompareView / Sub・Adjustment・OverrideListTab)
  にも反映される。
- **CompareView 単一グループ時の flat 表示**: グループが 1 つだけのとき
  ヘッダ行を省略、`maxHeight` を 200 → 280 に拡張。
- **orphan 講師の警告再表示**: SummaryTable で project.teachers に存在しない
  名前を ⚠️ 不明な講師 ブロックで表示 (diff 前の自動 chip 化 feedback を維持)。
- **`subjects || []` フォールバック撤去**: SubstituteView で `subjects` を
  そのまま透過。子側のデフォルト引数で吸収する形にして、毎レンダーの
  新規 array literal による useMemo 無効化を防止。

P4 (低優先):
- **substring matching の長い順優先**: 短い subject 名 ('A' など) が
  長い名 ('英語' を含む '英語特訓') を hijack しないように、subjects と
  aliases ともに長い順に評価する。
- **per-group toggle の stale 解消**: ExternalCounts の『このグループを
  選択/解除』ボタン押下で `allSelected` を setState updater 内で再計算。
- **teacher.subjects 重複の dedupe**: `['英語','英語']` のような corruption
  data を Set で正規化してから length 判定 — '複数教科' 誤分類を防止。

Tests: +16 件 (reducer 8 / migration 2 / groupTeachersBySubject 3 /
groupTeacherNames 3)。全体 1181 件、lint 0 / typecheck 0 / build OK。

### Changed (講師一覧を教科ごとにグループ表示 — アプリ全体)
- すべての講師リスト・ドロップダウン・チェックボックス一覧を
  「教科 (英語 / 数学 / 国語 / 理科 / 社会) ごと → 複数教科 → その他」の
  グループ見出し付きで表示するよう統一。
- 講師ビルダー側 (project.teachers ベース) で新ユーティリティ
  `utils/groupTeachersBySubject` を導入。単一教科講師は当該グループ、
  複数教科担当 (未定など) は「複数教科」グループに 1 度だけ表示、担当無しは
  「その他」。
- 影響を受けた builder 側コンポーネント (計 6):
  - ExternalCounts: クイック入力グリッド (teacher rows) と
    複数講師チェックボックス一覧。各グループ単位の「グループ全選択 / 解除」
    ボタンも追加して複数人登録をさらに高速化
  - NgSettings: 日付ごとの NG テーブル (teacher rows) と一括設定の講師
    select (optgroup 化)
  - TeacherManager: 講師マスタ管理表 (rename / 削除 / 科目編集)
  - ClassPriority: クラス優先度設定テーブル
  - ScheduleCell: コマセルの講師ドロップダウン (optgroup 化)
  - SummaryPanel: 講師別コマ数の集計 (showSummary パネル + 自動生成案の
    集計表) を教科グループ見出し付きで表示
- 本体側 (slots ベース) では既存の `useTeacherGroups` のコアロジックを
  純粋関数 `utils/groupTeacherNames` に切り出し、任意の name 配列を
  「バイト → 英数国理社 → その他」にグループ化できるように。
- 影響を受けた本体側コンポーネント (計 4):
  - CompareView: 講師候補チップ一覧をグループ見出し付きで縦並びに
  - SubListTab / AdjustmentListTab / OverrideListTab: 講師フィルタ
    `<select>` を `<optgroup>` 化 (代行/調整/補正一覧)
- Tests: 19 件追加 (groupTeachersBySubject 9 / groupTeacherNames 10)。
  全体 1165 件、lint 0 / typecheck 0 / build OK。

### Added (他学年セッションのプリセット + 複数講師の一括登録)
- 「📅 他学年・午前」タブにプリセット管理パネル (折りたたみ) を追加。
  「予備校（早朝）= 12:25-13:35, 7/24~7/31, メモ"予備校"」のような
  時刻・期間・メモの組み合わせを保存し、詳細セッション登録フォームから
  1 クリックで時刻/期間/メモを展開できる。
- 詳細セッション登録フォームの講師欄を単一 `<select>` から **チェックボックス
  一覧** に変更し、複数講師を 1 クリックずつ選択可能。「全選択 / 全解除」
  ショートカット付き。
- 「まとめて追加」が M 人 × N 日 を 1 アクション (teacher/addExternalSessions
  既存 batch) で atomic に登録。プレビューも「M 名 × N 日 = K 件」と
  「→ 自動NG (M × N_overlap) 件」を表示。
- データ: project に `externalSessionPresets: Preset[]` を追加。
  `Preset = { id, name, startTime?, endTime?, startDateLabel?, endDateLabel?, memo? }`。
  既存プロジェクトは migrate 時に空配列で補完。
- 新 reducer actions: `preset/add` / `preset/update` / `preset/remove`。
  空文字フィールドは保存しない、startTime が無い場合は endTime も連動 drop、
  name 必須など defense-in-depth な検証を備える。
- 日付ラベル変更 (`schedule/renameHeader` 'date') 時に
  `externalSessions[].date` と `externalSessionPresets[].startDateLabel /
  endDateLabel` を cascade 更新 (孤児化防止)。
- Tests: 13 件追加 (reducer 11: preset add/update/remove + cascade,
  scheduleKey 2: migration)。全体 1146 件、lint 0 / typecheck 0 / build OK。

### Added (他学年セッション → 日時NG の自動派生)
- 「📅 他学年・午前」タブで時刻入りセッション (例: 12:25-13:35) を登録すると、
  その時間帯と重なる時限が「🚫 日時NG」タブで自動的にNG扱いになる。
  例: 堀上 7/29 12:25-13:35 の予備校 → 13:00-13:45 の中3 1限が自動NG。
  毎日NGを 1 セルずつクリックする手間を削減。
- 自動NGは保存せず計算で導出する方式 (utils/autoNg.js)。セッションを削除すれば
  NG表示もその場で消える。手動NG (teacher.ngSlots) との merge は表示・
  constraint 両方で行い、ScheduleCell の (NG) ラベル / NgSettings のグリッド /
  autoGenerator (solver) すべて手動NGと同等に扱う。
- 「📅 他学年・午前」の詳細セッション登録フォームを刷新:
  - 日付は「複数チェックボックス」から「期間 (開始日〜終了日)」のレンジに変更。
    NGタブと UI を揃え、毎日チェックする手間を削減。
  - 「時刻 (開始〜終了)」のピッカーを追加。type="time" で時刻入力を簡素化。
  - 自由ラベル欄は廃止 (時刻指定があれば自動で「HH:mm-HH:mm」を label として保存)。
  - 「→ 自動NG N 件」のプレビューを追加で、登録前に効果が見える。
- 時限ラベルから時刻を自動解析するパーサ (utils/timeRange.js) を新規。
  「1限 (13:00~13:45)」「13:00~」「12:25-13:35」など freeform を minutes に
  正規化。終了時刻が不明な場合は開始のみで重複判定 (始点 ∈ 他方範囲)。
- データ層: externalSession に optional `startTime` / `endTime` (HH:mm 文字列)
  を追加。既存セッションは未設定で表示は label にフォールバックするので
  backward compat あり。
- Tests: timeRange.test.js (25 件), autoNg.test.js (12 件), projectReducer
  (新フィールドの保存 / 空文字省略 2 件), teacherConstraints (autoNgEntries 経由 3 件),
  autoGenerator (時間重複セッションで solver が候補を外す 1 件) を追加。
  全体 1121 件、lint 0 / typecheck 0 / build OK。

### Fixed (自動NG: コードレビュー指摘の修正)
P1〜P3 を一括対応 (詳細は本変更の commit message を参照):
- **タブバッジ違反カウント**: `computeTabViolationCounts` がタブごとに
  `autoNgByTeacher` を再計算するように変更。それまではアクティブタブの
  ScheduleCell では NG 警告が出るのに、TabBar の他タブ badge が自動NG違反を
  カウントせず数字がズレていた。
- **静的検証 (実行不能スロット)**: `computeInfeasibilities` が
  `autoNgByTeacher` を受理し、全候補講師が他学年セッションで塞がれている
  スロットを『不可能』として警告するように。これまで solver が空セルを
  返す原因を事前に表示できていなかった。
- **Excel 出力の ⚠NG**: `excelExport` の 2 箇所 (シフト表 / 科目別シート) で
  自動NG (他学年セッションとの時間重複) も ⚠NG マークとして出力するように。
  これまで配布された Excel で自動NG違反が見落とされていた。
- **講師の削除/リネーム/CSV置換時の cascade**: `teacher/remove` /
  `teacher/rename` / `teacher/import (replace)` が `externalSessions` も
  cascade clean / rename するように。これまでは孤児セッションが UI に
  表示されながら自動NG派生の対象外になり状態が乖離していた。
- **フォーム検証**: ExternalCounts の `canAdd` を `parseHHmm` ベースに統一
  (regex 過剰許容を防ぐ)、`startTime` 必須化 (endTime のみの silent data loss
  防止)、`startTime < endTime` 強制 (逆転で自動NGが付かない silent failure
  防止)、エラー文言を直接表示。
- **連打による重複登録**: `handleAdd` 後に時刻とメモをクリアし、フォームが
  「処理された」視覚状態になることで意図しない二重登録を抑止。
- **複数日 atomic 登録**: 新アクション `teacher/addExternalSessions` を追加し、
  N 日選択時に 1 つの dispatch で全日登録 (履歴 push も 1 回、途中失敗時の
  半端状態を排除)。
- **`parseTimeRange` の終了のみ表記**: `~14:00` のような『終了 14:00 の意味』
  で書かれた文字列を `開始 14:00` と誤解釈する反転を防ぐため、明示的に
  `null` を返すように。
- **NgSettings の変数 shadow 解消**: tooltip 生成内で外側講師 `t` を
  `timeText` に rename し、将来 `t.name` を足したくなった時のバグ-on-touch
  を排除。
- **'未定' 講師の自動NG挙動を comment 化**: ScheduleCell で '未定' が
  placeholder として自動NG/手動NG ともに常に false 扱いになる仕様を
  明示。
- **date ID 初期値の型ドリフト**: `formStartDateId` / `formEndDateId` の
  初期値を `''` (string) から `null` に変更し、select の value を
  `?? ''` で coerce することで型混在を排除。
- Tests: 12 件追加 (timeRange 1 / analysisHelpers 3 / reducer 8)。
  全体 1133 件、lint 0 / typecheck 0 / build OK。

### Tests (Builder Test foundation: D2b)
- **D2b** Header / Toolbar / ScheduleCell の主要 3 コンポーネントに
  testing-library で UI テストを追加 (計 28 件):
  - `Toolbar.test.jsx` 9 件 (errorKeys ⚠️ ボタン / scrollToFirstError /
    progress 表示 / undo/redo disabled / 生成クリア confirm / isGenerating
    時の disabled)
  - `Header.test.jsx` 9 件 (project.name 表示 / 名前編集の Enter/Escape /
    saveStatus 表示 / プロジェクト保存ボタン / Excel 出力中の disabled +
    "⏳ 出力中..." / 完了後の toast)
  - `ScheduleCell.test.jsx` 10 件 (subject/teacher onChange / ロック切替 /
    locked 表示 / conflict ラベル / NG slot disabled / 既使用科目 disabled /
    矢印キーナビ ArrowDown/ArrowRight / config 不一致時の null return)
- ProjectContext / UIContext の Provider を直接 mock 値で wrap する形式で、
  vi.mock は最小限 (Header の `excelExport` 動的 import のみ)。
- 全体: 957 → 985 件、lint 0 / typecheck 0 / build OK。

### Changed (Builder Test foundation: D4e + D2a)
- **D4e + D2a** `hooks/useAnalysis.js` の 120 行モノリス useMemo を
  `utils/analysisHelpers.js` の 3 純粋関数 (`computeGlobalUsage` /
  `computeActiveAnalysis` / `computeDashboard`) に切り出し、テスト可能化。
  `useAnalysis` 自体は React-deps 最小化の orchestrator (44 行) に縮小し、
  useMemo を 3 段に分けて部分再計算を実現:
  - globalUsage / teacherDailyCounts は `project.tabs / combinedGroups /
    externalCounts` のみ依存 (現タブの schedule 変化で再計算しない)
  - activeAnalysis (conflict / dailySubject / subjectOrder) は
    `currentConfig / currentSchedule / globalUsage`
  - dashboard は `currentSchedule / currentConfig`
  公開 API (`{ analysis, dashboard }`) は不変なので、Toolbar / SummaryPanel /
  ScheduleCell / ProjectContext は無変更。
- `utils/analysisHelpers.test.js` 18 ケース新規:
  - computeGlobalUsage 6 件 (単純集計 / "未定" スキップ / externalCounts /
    合同グループ重複防止 / 複数タブ横断 / subject 未割当)
  - computeActiveAnalysis 7 件 (conflict / 合同グループ除外 / タブ横断 /
    dailySubjectMap / subjectOrders / 空 schedule / "未定" 除外)
  - computeDashboard 5 件 (0% / 50% / 100% / 空 config / subject 未割当)
- 全体: 939 → 957 件、lint 0 / typecheck 0 / build OK。

### Changed (Builder Quick wins: D4f / D4g / D7b)
- **D4f** Builder の「全データリセット」が `window.location.reload()` で強制
  リロードする hack を撤廃。新 reducer action `project/reset` を追加し、
  LocalStorage クリア後に `loadInitialProject()` を再実行して dispatch
  経由で state を初期化する方式に変更。挙動は等価 (user defaults があれば
  user defaults ベース、無ければ hardcoded default で再構築) で、reload に
  伴う体感ラグと React state の途切れが無くなる。`project/reset` は
  history も `[freshProject]` / `historyIndex=0` に初期化するため、Undo で
  「リセット前」に戻ってしまう事故を防止。
- **D4g** `cell/setNg` reducer case (23 行、cell 位置から講師名・date.label・
  period.label を引いて NG slot を toggle) を削除し、`useProject.js` の
  composer 内で `teacherActions.toggleTeacherNg` を呼ぶ派生 callback として
  `handleSetNg` を再定義。`teacher/toggleNg` と同じロジックを 2 箇所に
  持つ重複を解消。ContextMenu からの呼び出しシグネチャは不変。
- **D7b** 印刷システムの二系統 (`PrintButton` 直接系 / `handlePrint` popup 系)
  の所属を 7 ビューのファイル冒頭に inline コメントとして明記。Dashboard /
  WeekView / EventCalendarView / ConfirmedSubsView / MasterView が
  PrintButton 系、MonthView / ExcelGridView が popup 系。新しい印刷導線を
  足す際にどちらに寄せるか即判断できる。

### Tests
- `projectReducer.test.js`: 旧 `cell/setNg` 2 ケースを削除し `project/reset`
  2 ケースを追加 (57 件のまま)。
- `useProject.test.jsx`: `handleResetAll` 2 件 + `handleSetNg` 3 件を追加
  (36 → 41 件)。
- 全体: 934 → 939 件、lint 0 / typecheck 0 / build OK。

### Added (隔週管理タブの個別 anchor 解除)
- 隔週管理タブで「個別」マークの右に ✕ ボタンを追加 (admin のみ)。
  これまで slot ごとの個別基準日を解除するにはコマ編集モーダルまで
  掘る必要があったが、隔週管理タブから直接グローバル基準へ戻せるように。
- cascade なしの単純削除 (slot.biweeklyAnchors を `delete` するだけ) なので
  CLAUDE.md の規約に沿って即削除 + 6 秒 Undo toast を表示。Undo 中に
  別経路で再設定されていれば上書きしない安全策入り。
- `BiweeklyTab.test.jsx` を新設し、✕ ボタンの表示条件 (個別 anchor 有無、
  isAdmin、後方互換) と click → onClearSlotAnchors(id) 経路を計 5 ケース
  で検証。

### Fixed (隔週ローテーションのシフト)
- 隔週コマのローテーションが休講・テスト期間を考慮せず、休講や特訓を
  挟むと A/B 担当が片方に偏る (or 別教科側の進度が進まない) 不具合を修正。
  例: 5/1 B 週 → 5/8 休講/特訓 → 5/15 が B 週のままになっていたケースが、
  自動的に 5/15 = A 週へシフトする。毎回「隔週管理」で基準日を打ち直す
  手間が不要に。
- シフトトリガは「休講 (Holiday)」と「テスト期間 (ExamPeriod) で
  stopsClasses≠false のもの」。`stopsClasses=false` の高校テスト等は
  授業継続扱いなのでシフトしない。振替 (`ScheduleAdjustment`) は対象外。
- スキップ判定は slot 固有 (slot.day かつ scope/学年/科目キーワードが
  マッチするもののみ)。
- 手動で追加した基準日 (`BiweeklyAnchor`) は引き続き優先され、
  基準日以降のみ自動補正が走る。意図しない補正があれば手動 anchor で
  上書き可能 (従来の挙動を踏襲)。
- 関連実装: `utils/biweekly.js` (`getSlotWeekType` に `holidays` /
  `examPeriods` 引数追加、`isTeacherActiveOnDate` / `biweeklyDisplaySubject`
  も同様)、`utils/scheduleHelpers.js` (`isSlotCancelledByHoliday` と
  `isSlotCancelledForBiweeklyShift` を切り出し)、`utils/chainSubstitution.js` /
  `utils/sessionCount.js` / `hooks/useSessionCtx.js`
  (`holidays` / `examPeriods` を sessionCtx 経由で伝播)、表示系
  (`MonthView`, `WeekView`, `MasterView` / `BiweeklyTab`, `ExcelGridView` /
  `ExcelSection` / `ExcelCell`, `AbsenceWorkflowView` / `AbsenceTimetable` /
  `AbsenceSlotCard`) はそれぞれ `holidays` / `examPeriods` を thread 済み。
- `biweekly.test.js` に休講シフト・テスト期間シフトの 14 ケースを追加
  (56 件 → 76 件)。`chainSubstitution.test.js` に統合テスト 1 件追加。

### Fixed (校正レビュー 2 回目反映)
- `SubstitutePickerPopover` で新規追加した矢印キーハンドラに
  `isField` チェックが抜けており、ステータス `<select>` 内で矢印キーが
  preventDefault されてオプション切替できなくなっていた回帰を解消。
- `AdjustmentListTab` / `OverrideListTab` で 📅 / 📅→ ボタンを
  `S.iconBtn` 共通スタイルに集約した際に `marginRight: 2` を引き継ぎ忘れて
  隣接ボタンが密着していた点を修正。

### Fixed (校正レビュー 3 回目反映)
- ICS エクスポートの SUMMARY / DESCRIPTION が、隔週コマで「週ごとの
  実態」を反映していなかった点を修正。複合教科 "英/数" の隔週コマでは
  A 週担当には先頭教科 (`英`)、B 週パートナーには 2 つ目の教科 (`数`)
  を出すように `biweeklyDisplaySubject` を経由。`DESCRIPTION` の講師欄も
  パートナー側エクスポート時はパートナー名のみ表示。
- ICS に Asia/Tokyo の `VTIMEZONE` ブロックを追加 (RFC 5545 準拠、
  Apple Calendar / Outlook 等の厳格パーサ対策)。
- `SessionOverridePopover` の `value` / `displayAs` 入力 `onChange` でも
  `setError(null)` を呼ぶように修正。エラー後に値を直すと適用ボタンを
  押すまでエラー赤帯が残っていた UX を解消。
- インポート完了後に孤立データを検出した場合、データ管理モーダルを
  閉じずに残すよう変更 (孤立件数の info トースト表示と「孤立データ掃除」
  ボタンへの動線を保つため)。0 件のときは従来通りモーダルを閉じる。

### Added (校正レビュー 3 回目反映)
- `g` chord ナビゲーションに `g w` (週間) / `g o` (月間) を追加。
  WEEK / MONTH は講師選択中専用ビューなので、`selected` が `null` の
  ときは chord を no-op にして誤操作で講師選択を失わないよう保護。
  `m` キーは MASTER で取られているため、MONTH には mOnth の `o` を採用。
- `ShortcutsHelp` に `g w` / `g o` を追記。
- `S.iconBtn` 用のグローバル CSS `.icon-btn:hover` / `.icon-btn:focus-visible`
  を `App.jsx` の style ブロックに追加。一覧操作ボタンに `ICON_BTN_CLASS`
  を併用して、薄いグレーホバー背景とフォーカスリングを表示。
- `SubstitutePickerPopover` の矢印キー操作で focusIdx 変更時に
  対応 `<button role="option">` を `scrollIntoView({ block: "nearest" })`
  でスクロール表示。長い候補リストでも見えなくならない。

### Tests (校正レビュー 3 回目反映)
- `ics.test.js` に VTIMEZONE / 複合教科隔週 / DESCRIPTION 講師名の
  ケースを追加 (5 件 → 10 件)。
- `orphanCleanup.test.js` の `applyOrphanCleanup` に move / reschedule の
  `targetSlotId` updated ケースを追加 (14 件 → 16 件)。

### Fixed
- ICS エクスポートが隔週コマを毎週として出力していた不具合を修正。
  `RRULE:FREQ=WEEKLY;INTERVAL=2` を付け、隔週パートナーの ICS は最初の
  該当日を B 週側にずらして出力する。`utils/ics.js` から純粋関数
  `buildTeacherIcsContent` を切り出し、5 件のユニットテストを追加。
- 回数補正ポップオーバー (`SessionOverridePopover`) で「回数を指定 ↔
  カウントしない」を切り替えてもバリデーションエラーの赤帯が残り続けて
  いた点を修正。モード切替時に `setError(null)` する。
- 孤立データ掃除 (`utils/orphanCleanup.js`) が、`reschedule` / `move` の
  `targetSlotId` (振替先・移動先のコマ) が削除済になっているケースを
  検出していなかった。元コマが生きていれば `targetSlotId` のみ取り除いて
  テキスト情報 (targetDate / targetTime / targetTeacher) は残す部分修正の
  ロジックを追加。3 件のユニットテストを追加。

### Changed
- 授業管理 3 タブ (代行 / 時間割調整 / 回数補正) の削除コールバック
  `onDel` の引数規約を **id に統一**。`AdjustmentListTab` だけ
  `onDel(adj)` でオブジェクトを渡していたのを `onDel(adj.id)` に揃え、
  合同削除時の関連回数補正トーストは `SubstituteView.handleDelAdjustment`
  内で `adjustments.find` で再取得するように。
- `CommandPalette` (Cmd+K) のビュー候補に「週間 / 月間」を追加。空のビュー
  に飛ばさないため、講師選択中のときだけ候補に出す
  (`selectedTeacher` prop)。
- 一覧 3 タブ (Sub / Adjustment / Override) の操作列・ソートインジケータ
  を `@media print` で隠すよう `className="no-print"` を付与。フィルタ
  `<label>` に `htmlFor` を、対応する `<input>` / `<select>` に `id` を
  付与してスクリーンリーダー読み上げを改善。
- 一覧操作ボタン (✏️ / 🗑 / 📅 など) を `S.iconBtn` 共通スタイルに集約。
  最小タッチ領域 32×32px を確保 (WCAG 2.5.5 / 2.5.8 準拠)。
- 代行ピッカー (`SubstitutePickerPopover`) に矢印キー + Enter による
  キーボード操作を追加。`role="listbox"` / `role="option"` /
  `aria-activedescendant` で a11y 対応。
- データインポート完了後に `detectOrphans` を実行し、孤立データが
  含まれる場合は件数を info トーストで案内 (古いバックアップに対する
  防御策)。

### TODO / 課題
- 確認テストの教科ローテーション（英→数→国→理→社）未対応。現状は
  `subj:"確認テスト"` で固定表示。回数に応じた教科表示をサポートする
  仕組み（例: スロットに `subjectRotation: ["英語","数学",…]` を
  持たせ、セッション回数から教科を決定して表示する）が必要。

### Added
- 授業管理 (`SubstituteView`) に「時間割調整一覧」「回数補正一覧」タブを
  追加。代行一覧と同様に月 / 講師 / 種別フィルタ + 件数バッジを備え、
  欠勤振替画面で行った合同授業 / コマ移動 / 別日振替 / 回数補正 (set / skip)
  を一覧化、削除は `removeWithUndo` (6 秒間 Undo 可能なトースト) で
  取り消し可能。各行に「📅 この日に飛ぶ」ボタンがあり、欠勤振替画面の
  該当日を直接開ける。振替行は「📅→ 振替先へ飛ぶ」ボタンも併設。
- 「作成日時」列のヘッダをクリックして昇順 / 降順を切り替え可能に
  (「最近補正したものから取り消したい」操作を高速化)。
- データ管理モーダルに「孤立データ掃除」セクションを追加。コマ削除以前に
  作られた、参照先コマが存在しない代行 / 時間割調整 / 回数補正を一括で
  検出・削除できる。
- `Cmd+K` (Command Palette) の navigation 候補に「時間割調整一覧」
  「回数補正一覧」を追加し、サブタブへ直接ジャンプ可能に。
- ISO 文字列をローカルの `YYYY-MM-DD HH:MM` に整形する `fmtIsoLocal`
  ヘルパを `utils/dateHelpers.js` に追加 + ユニットテスト。
- ESLint (flat config) + Prettier + `npm run lint` / `npm run format`
- TypeScript tooling (`tsconfig.json`, `npm run typecheck`) and initial
  migration of `src/utils/schema.ts` with full type annotations. Shared
  domain types in `src/types.d.ts`.
- Vitest + 44 unit tests covering `data.js`, `utils/biweekly`, and
  `utils/schema`. New `npm run test` / `test:watch` scripts.
- GitHub Actions CI workflow running lint, typecheck, test, build.
- `ErrorBoundary` at app root with a graceful fallback screen.
- Toast system (`useToasts` + `ToastContainer`) with success/error/info
  variants for feedback on save/delete/import/export.
- `useConfirm` promise-based replacement for `window.confirm()` using
  the in-app Modal, including a `danger` tone.
- Import loading state and structural validation of export bundles via
  `validateExportBundle` and `migrateExportBundle`.
- Cascade delete for substitutes when their referenced slot is removed.
- Schema versioning (`schemaVersion`, `exportedAt`) in export bundles.
- Design tokens in `src/styles/tokens.js`, exposed as CSS custom
  properties on `:root` via `main.jsx`.
- Web manifest (`public/manifest.webmanifest`) and meta tags
  (description, OpenGraph, theme-color, favicon) in `index.html`.
- `CONTRIBUTING.md`, `CHANGELOG.md`.

### Changed
- ダッシュボード既定ビューを「時間割」に変更。localStorage 未設定ユーザは
  起動時に時間割表示で開く（既存ユーザは保存済みビューを尊重）。
- 時間割表示の月〜土曜タブに、viewDate を含む週の日付 (M/D) を併記。
  曜日クリック時に viewDate も連動し、第 N 回バッジが正しい日付基準で
  算出される。
- 並列スロット集約を導入: 同一 `(day, time, grade, cls, subj)` で担任
  だけ異なる複数スロット（例: 中3 火/木 確認テスト 藤田 + 大屋敷）を
  閲覧用途では 1 コマにまとめ、時間割表示で担任を「藤田・大屋敷」と
  併記。`buildSessionCountMap` の回数カウントも並列スロットを 1 回と
  して集計するよう `activeSlotsOnDay` に重複除去を追加。`utils/
  parallelSlots.js` を新設。代行モード・管理モードでは個別スロットを
  保つため集約を適用しない。
- `src/App.jsx` split from a 1,710-line monolith into a ~500-line shell
  + 14 component files under `src/components` and `src/components/views`,
  plus `src/hooks/useLocalStorage.js` and related utilities.
- `Modal` now traps focus (Tab / Shift+Tab), auto-focuses on open, and
  restores focus on close. Adds `role="dialog"`, `aria-modal`,
  `aria-labelledby`.
- `Sidebar` is a semantic `<nav>` with `aria-label` and shows an empty
  state when search filters out all teachers.
- Emoji-only buttons across the app now carry `aria-label`s.
- `SlotForm` wires proper `<label htmlFor>` / `<input id>` pairs and
  emits `role="alert"` on inline errors.
- `HolidayManager` adds date validation and toast feedback.
- Several hot paths memoized: `SectionColumn.byTime`, Sidebar per-
  teacher slot counts, App header day-count summary, Dashboard section
  grouping, `SlotCard` / `StatusBadge` wrapped in `React.memo`.
- `MasterView` hover state now uses React state via a memoized
  `MasterSlotCard` component instead of `querySelector` DOM mutation.
  Its filter chain collapses from 4 sequential filters into a single
  pass.
- `useLocalStorage` surfaces load/save failures through an `onError`
  callback; the app displays a toast on quota exhaustion instead of
  silently dropping writes.
- Root `README.md` replaced with a project-relevant description (was
  the GitHub Skills template).
- `LICENSE` copyright holder updated from "GitHub, Inc." to the project
  owner.

### Removed
- 6 GitHub Skills tutorial workflows under `.github/workflows/`.
- `gh-pages` npm dependency and `deploy` script (Actions handles
  deploys).
- Dead `DayBlock` and `SubBadge` components and two unused variables.
- Unused `@types/react*` type declarations (re-added indirectly through
  TypeScript tooling when appropriate).

### Fixed
- コマ削除時に、関連する `adjustments` (合同 / 移動 / 振替) と
  `sessionOverrides` も cascade 削除されるように修正
  (CLAUDE.md 「cascade ありは confirmedRemove」ルールに準拠)。
  合同授業の吸収側として参照されているケースは host 側を存続させ、
  該当 id だけを `combineSlotIds` から取り除く丁寧な処理。確認
  ダイアログとトーストには「削除」と「合同からの除外」を区別表示し、
  件数のミスマッチを防止。
- 一覧から欠勤振替画面に飛んだ際、`date` を lazy 初期化して
  「今日 → 目的日」のチラつきを回避。
- 合同削除時、その日の関連回数補正が孤立して残るケースを `info`
  トーストで案内。
- Several list components used `key={i}` (array index); replaced with
  stable IDs to prevent React reconciliation bugs when lists mutate
  (SlotCard in DayBlock, SectionColumn, WeekView, MonthView cells,
  MonthView teacherSubs).
